# 機能拡張 仕様書（料金表・シリーズ・note連携・プリント販売・ギャラリー配置）

> Claude Code 向け実装仕様。既存の admin-enhancement-spec.md / -v2.md / design-spec.md の続編。§0 既存パターン厳守。
> 全項目確定済み。すべて「秋が管理画面から細かく調整できる」ことを前提とする。

---

## 0. 前提（厳守）

- DBクエリは `withRetry(() => db....)` でラップ
- データ更新後は `qc.invalidateQueries`
- 新規 settingsキー追加時は3箇所セットで更新（admin.tsx previewPayload / provider.tsx DB適用useEffect / provider.tsx handlePreviewMessage）
- スキーマ変更時は `cd packages/web && bun run db:push`（ecosystem に起動時ビルド・db:push 組込済み）
- 型付きAPIクライアント（lib/api.ts）経由。fetch直叩き禁止
- 全書き込みは res.ok / assertOk チェック、失敗時はエラー表示（無音失敗にしない）
- ロード中は空状態メッセージを出さない（isLoading でガード）

---

## グループG: ギャラリー配置の「制御されたランダム」化【最優先】

### 方針

- **サイズ = 秋の手動指定（S/M/L、既存）**。編集判断なので自動化しない。
- **配置のリズム = 抜け感のための制御ランダム**。きっちり詰めず、意図的な余白で「抜け」を作る。
- 「ランダム」と「崩れ」は紙一重。無秩序なランダムにしない＝下記ルールで制御する。

### G1. 制御ランダム配置のルール

1. **シード固定**: 写真IDの並び（sortOrder）をシードにした疑似ランダム。**同じ写真構成なら毎回同じ配置**になる。リロードごとに変わらない（酔い・CLS防止）。
2. **意図的な余白セル（抜け）**: グリッドに空セルを挿入して抜けを作る。ただし
   - 連続させない（空白が2つ続かない）
   - 隅に寄せすぎない
   - 行頭・行末ばかりに偏らせない
3. **緩急パターン**: L（大）の後には S/M を置くなど、サイズの並びにリズムを持たせる。大が連続しすぎない。
4. **行優先は維持**: 既存の「上から横に読む順」（行優先）は崩さない。sortOrder 通りの読み順を保ったうえで、サイズと余白でリズムを作る。
5. **スマホ配慮**: 列数が減る（2列）ため、抜け（空白セル）は控えめに。狭い画面でスカスカにならないよう、モバイルでは余白量を自動で減らす。

### G2. 配置調整を Settings に追加（秋が調整できる）

新規セクション「ギャラリー配置」を Settings に追加（D1 に従い折りたたみ）。以下のキーで調整可能にする。

| settingsキー | 内容 | 範囲 | 既定 |
|---|---|---|---|
| `galleryGapScale` | 写真同士の間隔（余白）の倍率 | 0.5〜2.0 | 1.0 |
| `galleryEmptyRate` | 抜け（空セル）の挿入頻度。0で抜けなし＝詰める | 0〜0.3 | 0.1 |
| `gallerySizeVariation` | サイズの緩急の強さ（S/M/Lの差をどれだけ強調するか） | 0〜1.0 | 0.5 |
| `galleryColumns` | PC時の基本列数 | 2〜4 | 3 |
| `galleryColumnsMobile` | スマホ時の列数 | 1〜2 | 2 |
| `gallerySeed` | 配置のシード値。「配置をシャッフル」ボタンで変更（気分で配置を変えたいとき） | 整数 | 1 |

- 「配置をシャッフル」ボタン: `gallerySeed` を新しい乱数に更新するだけ。これで配置パターンが変わる（写真は同じ）。秋が「今の配置イマイチ」と思ったら押して別パターンを試せる。
- 全項目ライブプレビューに即反映（§0 の3箇所同期）。
- スマホ/PCはプレビュー幅切替（実装済み）で両方確認できる。

### G3. 受け入れ基準
- [ ] 同じ写真構成ならリロードしても配置が変わらない（シード固定）
- [ ] 抜け（余白）が連続せず、隅に偏らない
- [ ] Settings から間隔・抜け頻度・列数・シードを調整でき、プレビューに即反映
- [ ] スマホで抜けが自動的に控えめになり、スカスカにならない
- [ ] 「配置をシャッフル」で別パターンに変わる

---

## グループH: 撮影依頼の料金表・プラン一覧

### H1. 料金プランの管理（秋が自由に編集）

撮影依頼（ポートレート / ファッション 等）向けの料金表。管理画面から項目を自由に追加・編集・削除・並べ替えできる。

**スキーマ: 新規テーブル `pricing_plans`**
```ts
id, title (例 "ポートレート"), price (例 "¥15,000〜"),
description (含まれる内容・自由記述), features (箇条書き・改行区切り or JSON配列),
note (補足), sortOrder, isPublished (公開/下書き)
```

**管理画面: 新規タブ または Settings 内セクション「料金プラン」**
- プランの追加・編集・削除・ドラッグ並べ替え
- 各プラン: タイトル / 価格 / 説明 / 含まれるもの（箇条書き） / 補足 / 公開トグル
- 公開サイトには isPublished=true のものだけ sortOrder 順に表示

**公開側: 料金ページ または About/Contact 内のセクション**
- プランをカード or 表で表示。design-spec の余白主導・静かなUIに合わせる
- 料金の下に問い合わせ導線（Contactへ）

**API**: `GET /pricing`（公開・published のみ）、`POST/PATCH/DELETE /admin/pricing`、`POST /admin/pricing/reorder`

### H2. 受け入れ基準
- [ ] 管理画面でプランを追加・編集・削除・並べ替えできる
- [ ] 公開トグルで下書き/公開を切り替えられる
- [ ] 公開サイトに公開プランだけが順番通り表示される

---

## グループI: シリーズ / 作品集のまとまり

### I1. シリーズ機能（"Still, life" のような作品群を独立して見せる）

写真を「シリーズ」にまとめて、シリーズ単位で見せられるようにする。秋の作家性（コンセプト）を伝える核。

**スキーマ: 新規テーブル `series`**
```ts
id, slug, title (例 "Still, life"), subtitle, statement (シリーズの説明・コンセプト文),
coverPhotoId (表紙写真), sortOrder, isPublished
```
**写真とシリーズの紐付け**: `photos` に `seriesId`（nullable）を追加。1枚は1シリーズに所属（または無所属）。

**管理画面: 新規タブ「シリーズ」**
- シリーズの作成・編集・削除・並べ替え
- 各シリーズ: タイトル / サブタイトル / ステートメント / 表紙写真選択 / 公開トグル
- 写真をシリーズに割り当て（インスペクタに「シリーズ」選択を追加、または一括割り当て）

**公開側**:
- シリーズ一覧ページ（各シリーズの表紙＋タイトル）
- シリーズ詳細ページ（`/series/:slug`）: ステートメント＋その写真群をギャラリー表示（グループGの配置ルールを流用）
- ナビゲーションに「Series」を追加するかは任意（Settings でトグル可にする）

**API**: `GET /series`・`GET /series/:slug`（公開）、`POST/PATCH/DELETE /admin/series`、`POST /admin/series/reorder`

### I2. 受け入れ基準
- [ ] シリーズを作成し、写真を割り当てられる
- [ ] シリーズ詳細ページでステートメント＋写真群が表示される
- [ ] 表紙写真・公開状態・並び順を管理画面から調整できる
- [ ] カテゴリ（既存）とシリーズは別軸として共存する（カテゴリ=分類、シリーズ=作品群）

---

## グループJ: note 最新記事の自動表示

### J1. note RSS 連携

秋が note に投稿すると、サイトに最新記事が自動で表示される（二重管理しない）。

**実装**:
- note のRSSフィード（`https://note.com/{username}/rss`）をサーバ側で取得・パース。
- サーバでキャッシュ（例 30分〜1時間TTL）して都度フェッチしない。note側への負荷とサイト表示速度の両立。
- 取得項目: タイトル / リンク / 公開日 / サムネイル（あれば） / 抜粋。
- **API**: `GET /note-posts`（キャッシュ済みを返す）。RSS取得失敗時は空配列＋エラーにしない（落ちないこと）。

**管理画面: Settings に「note連携」**
- `noteUsername`（note のユーザー名。既存メモリでは `chi_aki_zip`）
- `noteShowCount`（表示件数、既定3）
- `noteEnabled`（表示ON/OFF）

**公開側**:
- トップ or About に「Journal」「最新の投稿」セクションとして数件表示。各記事は note へリンク（新規タブ）。
- design-spec に合わせた静かな見せ方。サムネイル＋タイトル＋日付程度。

### J2. 受け入れ基準
- [ ] note ユーザー名を設定すると最新記事が自動表示される
- [ ] 記事クリックで note の該当記事に飛ぶ
- [ ] RSS取得に失敗してもサイトが壊れない（セクションが消えるだけ）
- [ ] 表示件数・ON/OFFを管理画面から変えられる

---

## グループK: プリント販売（外部リンク）

### K1. 外部ストア（BOOTH等）へのリンク

サイト内ECは作らず、外部ストアへ誘導する。

**管理画面: Settings に「プリント販売」**
- `printStoreUrl`（BOOTH等のストアURL）
- `printStoreLabel`（リンク文言、既定「プリントを購入する」）
- `printEnabled`（表示ON/OFF）
- `printDescription`（任意の説明文。サイズ・価格の概要など）

**公開側**:
- About or 専用の小さなセクションに、説明文＋外部ストアへのボタン。
- 過去にLightbox内CTAは押し付けがましく削除した経緯があるため、**Lightbox内には置かない**。About/フッター等の控えめな位置に。

### K2. 受け入れ基準
- [ ] ストアURLを設定するとリンクが表示される
- [ ] ON/OFF・文言・説明を管理画面から変えられる
- [ ] 未設定時は何も表示されない（空状態で崩れない）

---

## データモデル追加まとめ

### 新規テーブル
- `pricing_plans`（H1）
- `series`（I1）

### 既存テーブル変更
- `photos` に `seriesId`（nullable, I1）

### siteSettings 追加キー
```
# G ギャラリー配置
galleryGapScale, galleryEmptyRate, gallerySizeVariation, galleryColumns, galleryColumnsMobile, gallerySeed
# J note連携
noteUsername, noteShowCount, noteEnabled
# K プリント販売
printStoreUrl, printStoreLabel, printEnabled, printDescription
# I シリーズ（ナビ表示トグル等）
seriesNavEnabled
```
※ 新規キーは §0 の3箇所同期を必ず行う。

### 新規API
```
GET  /pricing                     公開・published のみ
POST/PATCH/DELETE /admin/pricing
POST /admin/pricing/reorder
GET  /series , GET /series/:slug  公開
POST/PATCH/DELETE /admin/series
POST /admin/series/reorder
GET  /note-posts                  キャッシュ済みRSS
```

---

## 実装フェーズ（推奨着手順）

| フェーズ | 項目 | 理由 |
|---|---|---|
| **R1** | グループG ギャラリー制御ランダム配置＋調整UI | 既存の見た目の不満解消・最優先 |
| **R2** | グループI シリーズ機能 | 作家性の核・"Still, life" を見せる |
| **R3** | グループH 料金プラン | 仕事につながる導線 |
| **R4** | グループJ note連携 / グループK プリント販売 | 外部連携・運用を楽に |

---

## 全体の受け入れ基準

- [ ] ギャラリーが制御ランダムで「抜け感はあるが崩れない」配置になる
- [ ] ギャラリー配置（間隔・抜け・列数・シード）を管理画面から調整でき、プレビュー即反映
- [ ] 料金プラン・シリーズ・note・プリントすべて管理画面から細かく調整できる
- [ ] 各機能は未設定・空の状態でもサイトが崩れない
- [ ] 新規API/設定が §0 ルール（withRetry / 3箇所同期 / res.okチェック）に準拠
- [ ] スマホ/PC両方でプレビュー確認でき、レスポンシブで破綻しない
