# 2026-07-21 決定ログ（続き: 土台強化の3方向監査）

## タスク: 「土台部分の強化」— 3方向監査（オーナー指示）

オーナー確認により対象を3つに分解: ①管理画面の日常運用フロー(Codex) 
②決済/インフラの耐障害性(Codex) ③サイトの質感・デザイン(design-critiqueスキル)。
①②はCodexにread-only監査を依頼、③は実際にスクリーンショットを撮り
コントラスト比を実測(javascript_tool)して批評。実装はせず、次セッション向けに
task-queue.mdのT-10(データ整合性・要オーナー判断)/T-11(デザイン)へ登録。

- ①管理画面日常運用(Codex): P0×1(30日自動削除が復元を確認せず永久消失)、
  P1×5(purge deletedAt未確認/purge順序/重複アップロード/保存中入力消失×2/
  カテゴリ削除の非一体性)、P2×2(候補編集競合/ヒーロー並び替え競合)
- ②決済/インフラ耐障害性(Codex): P0なし。P1×5(R2/DB不整合3件・デプロイ切替中
  競合・重複確認と登録の分離は①と重複)、P2×3(派生画像孤児・purgeエラー無視・
  withRetry対象外の起動時ALTER TABLE)
- ③デザイン(design-critique+実測): 総評は良好(雑誌的佇まい達成)。ただし
  SNSリンク・Journal日付(opacity 0.3→実測2.41:1)・ローマ字名(0.4→3.34:1)・
  Journal抜粋(0.38→3.13:1)がWCAG AA(4.5:1)未達と実測で判明。
  opacityトークンの3段階整理、LP CTAの主副差別化も提案。

登録: T-10(データ整合性、要オーナー判断タグ。既存purge挙動の"バグ修正"だが
写真の永久消失に関わるため着手前に優先順位をオーナー確認する運用とした)、
T-11(デザイン・アクセシビリティ、昼推奨で次セッション実装可)。
コード変更はゼロ。決定ログ+task-queue登録のみで本セッションを閉じる。

## タスク: T-10 P0+P1の実装（オーナー承認「直そう」。実装=Codex / レビュー=Claude）

- 実装(Codex): 新規ファイル photo-integrity.ts(純粋関数群: purgeEligibility /
  unsharedPhotoStorageKeys / uploadedPhotoStorageKeys /
  withUploadRegistrationCompensation / purgeDbThenStorage) + 
  photo-integrity.test.ts + photo-integrity-wiring.test.ts(ルート配線の
  文字列検査) + saved-draft.ts(draftAfterSuccessfulSave) + saved-draft.test.ts。
  index.ts/admin-tabs.tsxへ結線。
  1. [P0] 自動削除(lazy retention)ループの各件で、既存のcreateSerialQueue
     (runPhotoIntegrityMutation)経由+purgePhotoFromDb内のトランザクション内
     再読み込みにより、復元済み写真を二重に保護(直列化+再確認の多層防御)
  2. [P1] purgeEligibilityがdeletedAt===null かつ expiredBefore未指定を
     "not-trashed"として400を返すよう実装(通常写真の直接purge拒否)
  3. [P1] purgePhotoFromDbをDB行削除→トランザクションcommit→R2削除の順に変更
     (purgeDbThenStorageで明示)。DB確定後のみ実画像削除
  4. [P1] 共有参照確認(sharer検索)をDB削除と同一トランザクション内に統合
  5. [P1] withUploadRegistrationCompensation: DB登録失敗時、アップロード済み
     R2オブジェクトのみ補償削除(uploadedPhotoStorageKeysでphotos/prefix限定)
  6. [P1] /admin/photos登録時、body.fileHashをトランザクション内で再確認
     (早期チェックは楽観的、これが正本)。既存fileHashカラム・既存の
     アップロード時サーバー計算値を再利用(新規マイグレーション無し)
  7. [P1] ProfileTab/SettingsTabの保存: mutationFn開始時にform snapshotを
     submittedとして固定、成功時はdraftAfterSuccessfulSave(submitted, current)
     で保存済み分のみクリアし保存中の追加編集を保持
  8. [P1] カテゴリ削除: 写真の未分類への一括更新とカテゴリ削除を同一
     db.transaction内に統合
  9. [見送り] デプロイ切替中の複数コンテナ競合: 新規マイグレーション/外部ロック
     なしでは中途半端な対応の方が危険と判断、未実装(理由報告あり、妥当と判断)
- レビュー(Claude): 全差分を精読。restore/soft-delete/duplicate/purge/registerの
  全runPhotoIntegrityMutation呼び出し箇所が非ネスト(直列キューのデッドロック
  リスクなし)であることを確認。soft-delete/restoreに404チェックが新規追加
  された副次効果(存在しないIDへの操作が黙ってok:true→404化)は正しい改善と判断。
  fileHash再確認がクライアント側の実際のアップロードレスポンス転送
  (admin.tsx:3421,3450)と正しく配線されていることを確認。
  修正指示・追加修正なし(そのまま承認)。
- 検証: bun run check 510 pass / 0 fail(Codex実行+Claude独立再実行)。
  bun run smoke 35 passed(admin必須)。

## タスク: T-11 P1（プロフィールページの低コントラスト解消。実装=Codex/レビュー=Claude）

- 実測に基づき、profile.tsx内の10箇所(英字名/セクション見出し4/SNSリンク3/
  Journal日付/Journal抜粋)を opacity 0.30〜0.40 → 0.55 へ統一。
  背景rgb(18,18,18)・文字rgb(232,232,232)で実測5.26:1、WCAG AA(4.5:1)達成。
  既にページ内の本文で使われていた値(0.55)に寄せたため、新しいトークンは
  増えていない。hover色は変更なし。
- Layout.tsx:508の footerOpacity はクレジットではなく任意のContact誘導リンクと
  判明。本番は文言未設定で非表示・footerOpacity=1のため対象外と判断(妥当)。
- レビュー(Claude): 10箇所の差分を確認、指示どおりhover色に触れていないこと・
  スコープ外への変更が無いことを確認。追加テスト(実際のDOM上のclassName検証)も
  適切と判断。追加修正なし。
- 検証: bun run check 511 pass / 0 fail(Codex実行+Claude独立再実行)。
  公開ページのみでadmin非接触のためsmoke対象外。
- design-critiqueスキルはCodex実行環境になかったため、Claude側の実測値と
  既存デザイン規則を基準に実装(報告どおり)。
- T-11残: opacityトークンの3段階整理(P2)・Portfolio Kit LPのCTA差別化(P2)・
  Galleryタブ視認性(P2)。次セッションへ。
