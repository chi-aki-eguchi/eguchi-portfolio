# Codex Phase A 独立デバッグ調査 — 2026-08-05

> **ARCHIVED — 2026-08-20。もう読まない。**
> 後継: 後継なし（指摘は実装済み）
> 移した理由: 指摘した3件は 2026-08-06 に実装・テスト強化まで完了している

Claude Code が4巡したあと、届いていない領域を Codex（Sol / high, read-only）に
独立調査させた記録。**原文は `scratch/codex-debug-findings.md`。**

`docs/agents/codex-workflow.md` の規則により、**Codex の「未解決の反論」は
要約も書き換えもしない。**各項目へ Claude の判定（採用 / 不採用 / オーナー判断待ち）
と理由を1行だけ足す。この記録を実装する者にも読ませる。

## Claude が独立に確かめたもの

記録をそのまま信じない（`docs/agents/measuring.md`）。影響の大きい2件を実測した。

| 主張 | 実測結果 |
|---|---|
| 1-4 派生画像が補償削除から漏れる | **事実。**`photo-integrity.ts:36` が `startsWith("photos/")` で絞るが、実キーは `thumbs/` `medium/`。既存テストも派生キーを `photos/...` と書いており構造が違うので検出不能（`photo-integrity.test.ts:55-57`） |
| 1-9 Service のリンクに `safeHref` 未適用 | **事実。**`pages/service.tsx` に `safeHref` の使用が0件。`href={link.url}` を生で出す |

残りは Codex の file:line 付き報告のまま。**未実測のものを「確認済み」として扱わない。**

## 1. 確認済み事実 — 判定

| # | 内容（要旨） | 判定 | 理由 |
|---|---|---|---|
| 1-1 | Current State と Git は一致 | 採用 | 前回の停止指摘を受けて修正済み |
| 1-2 | API入力検証が不統一。正しいJSONの `null` が500 | 採用 | 400にすべきものを500にしている |
| 1-3 | 壊れた画像が500になる | 採用 | 利用者が直せる入力不備をサーバー故障として返している |
| 1-4 | 派生画像がR2に孤立する | **採用（最優先）** | Claude が実測で確認。画面から見えず溜まり続ける |
| 1-5 | 写真以外の並べ替えに競合検査が無い | 採用 | 写真だけ `expectedIds` 照合があり他4種に無い |
| 1-6 | 一括操作が途中まで保存される | 採用 | 表示は全体失敗、DBは部分成功で食い違う |
| 1-7 | 回転してもfocal pointを変換しない | オーナー判断待ち | 90度変換か中央リセットかは**仕様の選択** |
| 1-8 | Lightbox再試行tokenが `srcSet` に付かない | 採用 | ただし Lightbox は「壊さない」対象。着手前に `docs/checklists.md` |
| 1-9 | Service リンク・フォント名の検証漏れ | **採用（優先）** | Claude が実測で確認。設定値が公開ページのリンクとCSSへ直結 |
| 1-10 | 画像キャッシュに上限・破棄が無い | 採用 | 古い変換結果を掴み続け、今後のデバッグ結果を信用しにくくする |
| 1-11 | Service下書きを自分で上書きする | 採用 | 下書き保護という目的を満たしていない |
| 1-12 | typecheck 成功 / 679 pass 0 fail | 採用 | Claude 側の実測と一致 |

## 2. 推測 — 判定

| # | 内容（要旨） | 判定 | 理由 |
|---|---|---|---|
| 2-1 | 中サイズ無し経路で再試行が効かない可能性 | 採用（要実機確認） | 確認方法まで書かれている。1-8とまとめて扱う |
| 2-2 | スワイプ後の互換clickで2枚進む可能性 | 採用（要実機確認） | 実機でしか出ない類。Lightbox変更時に必ず見る |
| 2-3 | 300MB画像でメモリ圧迫の可能性 | オーナー判断待ち | 確認に保存先スタブが要る。**優先度は低いと考える** |
| 2-4 | 2タブ競合で順位重複の可能性 | 採用 | 1-5の実害面。ローカルDBで再現できる |
| 2-5 | 危険URL・壊したCSSの実害度 | 採用 | 1-9を直せば確認自体が不要になる |

## 3. 未解決の反論 — **Codex 原文のまま。判定を1行だけ追加**

1. **Claudeの20件の修正そのものを誤りと断定できる回帰は見つからなかった。** `origin/main..HEAD` の今回差分は主に公開画面・スマホadminで、1-2〜1-11の多くは今回の4巡より前からある。
   - → **採用。** Claude の修正への回帰指摘は無し、という結論をそのまま受け取る。

2. **ただし「API故障・空データ96項目がContact以外OK」という結論を、API層全体が安全という意味には広げられない。** Current Stateの検証対象は画面から見える故障・空データ（`task.md:61-69`）。正しいJSONの `null`、型違い、偽装画像、途中だけ成功する一括操作は今回実際に別経路として見つかった。
   - → **採用。指摘のとおり Claude の書き方が広すぎた。** Current State を「画面から見える範囲」と限定する形へ直す。

3. **「写真ビューアのrole / フォーカストラップ / Escは正しい」という判断自体には異論なし。** ただしそれはネイティブdialogの基本操作の話（`task.md:44-49`）であり、今回の再試行URLとtouch/pointer競合までは保証しない。
   - → **採用。** Claude の確認範囲はネイティブ dialog の基本挙動までだった。1-8 / 2-2 は別問題として残る。

4. **写真の並べ替えだけを根拠に、全並べ替えが競合安全とは扱えない。** 写真には `expectedIds` と全件照合があるが、他4種にはない。より簡単な代案は、新設計を増やすより既存の `photo-reorder-safety.ts` の検査方式を共通化して再利用すること。
   - → **採用。代案も採用。** 新設計を足さず `photo-reorder-safety.ts` を共通化する方針で進める。

## 4. 推奨 — 着手順と判定

| 順 | 推奨 | 判定 | 備考 |
|---|---|---|---|
| 1 | 派生画像の補償削除キー修正＋実キー構造の回帰テスト | **実装済み 2026-08-05** | オーナーが「ぜんぶ行こう」と許可。`photo-integrity.ts` を、送られたmasterから導ける2キーだけ受け付ける形へ（元より厳しい）。誤ったキー構造のテストも実構造へ修正 |
| 2 | API入口の共通入力検証（null/型違い/巨大ID配列を400へ） | **実装済み 2026-08-06** | 入口ミドルウェアで非オブジェクト本文を400へ。ID配列は `parseIdList()`（上限5000）。multipart と content-type 無しは意図的に対象外 |
| 3 | 壊れた画像を400/415/422へ。派生2件も補償削除付きに | **実装済み 2026-08-06** | 壊れた内容は `UnreadableImageError` 経由で422。派生2件の補償削除は推奨1で実装済みと再確認 |
| 4 | 並べ替え4種を写真と同じ競合拒否へ寄せる | **実装済み 2026-08-06** | `1a83251` で4種とも実装済み。今回10テストを再実行して成功 |
| 5 | 回転時のfocal point | **実装済み 2026-08-06** | オーナー決定「一緒に回す」。`focal-rotation.ts` + `rotateFocalPoint()`。一括・単体の両方、回転と同じUPDATEで |
| 6 | 画像キャッシュの版更新・上限・破棄 | **実装済み 2026-08-06** | `images-v2` / 300件上限 / 206を保存しない。sw.js に初めてテストを付けた |
| 7 | Service リンクの `safeHref`、フォント名のCSS安全化 | **実装済み 2026-08-05** | `service.tsx` の2経路と `ensureCustomFont` を修正。37アサーションのテスト付き |
| 8 | Lightbox の `srcSet` 再試行と 1スワイプ=1移動の回帰テスト | **実装済み 2026-08-06** | 両方とも実在した。合成clickで1スワイプが2移動、`srcSet` が素のままで再試行が空振り |
| 9 | Service下書きを `sessionStorage` 優先へ | **実装済み 2026-08-06** | `c477384` で実装済み。DB値と異なる未保存下書きを使う画面テストを再実行して成功 |

**Claude の見立て**: 7 → 1 → 3 → 9 の順が費用対効果が高い。7と9は副作用が小さく単独で完結し、
1と3は「見えないところで壊れ続ける」種類。2と4は範囲が広いので、まとまった時間が取れる時に。


## 実装状況（2026-08-05 時点）

オーナーが「ぜんぶ行こう」と指示。副作用の小さいものから着手した。

- **推奨1（派生画像の補償削除）→ 実装済み。**R2への操作は行っていない。
  既存 cleanup へ渡すキーの計算を直しただけ
- **推奨7（Serviceリンク・フォント）→ 実装済み**
- **2026-08-06 に 推奨2〜6・8・9を実装済み。**推奨3は `dc3ebe4`。
  推奨4・9は記録より先に `1a83251` / `c477384` で完了していたため、実物を再検証した
- 推奨5 はオーナーが「一緒に回す」を選択

## 追試 — 12件のテストが実装を本当に守っているか（2026-08-06 / Claude / HEAD 1e9c531）

**測り方.** 製品コード（`*.test.*` 以外）を1件ぶんだけ戻し、テストは現行のまま
`bun test ./src` を実行する。落ちれば「そのテストは実装を守っている」。
戻せない場合（後続commitと衝突）は、実装を1点だけ壊して同じことを測る。
ベースラインは 705 pass / 0 fail。

| commit | 内容 | 結果 |
|---|---|---|
| `45f4ad5` `2553903` `c7e2655` `1a83251` `5f8be81` | 2026-08-05 に確認済み | ✅ |
| `c477384` | サービス下書きの保持 | ✅ 1 fail |
| `d7daf99` | 存在しない記録への保存を拒否 | ✅ 2 fail |
| `f77bf49` | カテゴリ一括編集の atomic 化 | ✅ 1 fail |
| `8343911` | ヒーロー一括選択の atomic 化 | ✅ 1 fail |
| `087f591` | 設定値の検証 | ✅ 2 fail（テストの import ごと壊れる） |
| `2051e48` | メタデータ一括保存 | **△ 部分的** |
| `a3d7017` | サムネイル部分アップロードの後始末 | **△ 部分的** |

### △2件の中身 — ソース文字列テストは実装を守らない

`batch-photo-metadata-wiring.test.ts` と `thumbnail-upload-integrity-wiring.test.ts` は
`index.ts` を**テキストとして読み、部分文字列があるかを見ているだけ**。
文字列を残したままロジックを壊す実験（すべて 705 pass / 0 fail のまま素通り）:

- **M3**: `case "metadata"` の `.set(metadata)` を `.set({})` に。
  → 一括メタデータ編集が**何も保存しなくなる**が、テストは通る
- **M4**: `uploadAllOrCleanup` の cleanup 引数を no-op に。
  → 片方だけ成功したサムネが**R2に残り続ける**が、テストは通る
- **M2**: `buildBatchPhotoMetadataPatch` の `typeof field !== "string"` を
  `continue` に。→ 型違いを 400 で弾かず**黙って無視する**が、テストは通る
  （これは wiring ではなく、単体テストの分岐漏れ）

守られている側も測った。**M1**（`uploadAllOrCleanup` 本体から `cleanup` 呼び出しを削除）は
1 fail で検出された。つまり**部品の単体テストは本物、`index.ts` への配線だけが文字列頼み**。

### ここから出る作業

引き継ぎ項目2（`series-public-visibility.test.ts`）は**単独の問題ではなく、この類型の3件目**。
同じ形のテストを一緒に扱う。M2 の分岐漏れも同じ機会に埋める。
