# 2026-07-20 決定ログ（Fable5 セッション: 問題洗い出し + Codexデバッグ + 修正）

## タスク: サイト・コードの問題点洗い出し（オーナー指示）

- 方針: 監査はHaiku（低コスト小型モデル）3体に分担（セキュリティ / パフォーマンス / §0規約遵守）。
  本番サイトの死活・表示確認は自分でcurl+実ブラウザ実施。深いバグ探しはCodex（codex exec read-only）へ。
- 結果:
  - 本番: 全主要ルート200、コンソールエラー0、OG画像の日本語描画OK（Handoff 53の「本番反映確認」消化。
    push はオーナーが実施済みで、¥30,000単一プランは反映済みだった）
  - セキュリティ監査: HIGH/MEDIUMなし
  - パフォーマンス監査: 指摘0件
  - §0監査: 素fetch 3件（うち実質違反1件=admin-demo、FormData 2件は技術的に必要と判断し現状維持）
  - Codexデバッグ: P1×3 + P2×2 を検出（全件コードで裏取りし実在確認）

## タスク: Codex指摘5件の修正

1. **service-config: migration金額誤検知（P1）** — isLegacySelfPlan を「¥10,000 かつ 名称署名(自分で立てる|self)」の
   AND条件に変更。金額だけの一致で正当なカスタム2プラン（例: Basic ¥10,000 + Premium ¥30,000）を
   既定文面で上書きする事故を防ぐ。旧プラン名を独自変更していた保存データは取りこぼすが、
   その場合は「旧2プラン表示が残る」だけでデータ破壊は起きない安全側に倒した（Codexも妥当と確認）。
2. **service-config: points要素の型チェック漏れ（P1）** — isPlan に every(string) を追加。壊れたプランのみ除外し、
   同居する正常プランは残す（filter挙動）。回帰テスト2件追加。
3. **画像in-flight共有abort（P1）** — resizeInFlight を {promise, controller, waiters} に変更。
   各リクエストは joinInFlightVariant で待ち、中断は「全waiterが去った時のみ」共有側へ伝える。
   掃除は共有Promise決着時に移動（従来は最初のリクエスト完了時でエントリが早消えし二重変換の余地）。
   Codex再指摘P1対応: 決着時に必ず controller.abort() し、timeout後も待ち行列に残る処理を打ち切る。
   - 見送り(理由付き): 実行中sharpの中断（キャンセルAPIなし・従来も完走仕様）/
     timeout後の同一キー二重変換抑止（旧実装も許容。抑止すると直後の正当な再リクエストが即404になり可用性低下）
4. **admin-demoキャッシュ残留（P2）** — デモ終了時に queryClient.removeQueries() で共有キャッシュを破棄。
   fetch復元だけではstaleTime内（設定60s・一覧5分）の通常ページがデモ内容を表示し続けるため。
5. **/admin/demo のHTTP 404（P2）** — SPA_STATIC_PATHS に追加（LPから2箇所リンクされる実在ページ。
   HTML自体は表示されるがstatusが404で、監視・リンク検査が「存在しないページ」扱いしていた）。

- あわせて §0監査の指摘1件を修正: admin-demo.tsx の素fetch → 型付き api.settings.$get() へ。

## 検証

- `bun run check`: 488 pass / 0 fail（回帰テスト2件+known-pathsテスト拡張を含む）
- `bun run smoke`: 35 passed / 0 failed（admin接触のため修正前後2回実施、いずれも全green）
- Codexレビュー: 1周目P1 1件 → 追修正 → 2周目 **APPROVED**（見送り判断2点も承認）

## 要相談・申し送り

- FormDataアップロード2箇所（admin-tabs.tsx:897, 6262）の素fetchは技術的に必要なため現状維持。
  規約の「fetch直呼び禁止」に例外注記を足すかはオーナー判断。
- Stripe側3点とSNS告知はオーナー作業のまま（Handoff 53から変更なし）。

## タスク: /start を決済後ランディングとして整備（オーナー指示・同日追記）

- 背景: Stripeは決済完了画面を出さず /start へリダイレクトする運用と判明。
  purchase-thankyou.md の文面はStripe画面用ではなく、①/startのお礼表示と
  ②24時間以内に送る案内メールのテンプレートに役割変更。
- 実装: /start?thanks=1（またはcheckout_session_id付き）で着地した時だけ
  「ご購入ありがとうございます・お支払いは完了しています」を表示（素の/startは
  従来どおり=LPからの下見に誤表示しない）。JP|EN切替でクエリを引き継ぐ。
  「素材を送る」メールに送付項目リスト（名前/写真/プロフィール/ドメイン）の
  下書きを差し込み。purchase-thankyou.md を新導線に合わせて更新。
- 検証: check 490 pass / 0 fail（回帰テスト2件追加。banner出し分け・メール下書き）。
  admin非接触のためsmoke対象外。localhostゲートで/startは実ブラウザ確認不可
  （既知挙動・Handoff 52）のためjsdom描画テストで担保。
- lint学び: role="status" はoxlintのprefer-tag-over-roleで不可。静的表示なので
  role自体を外して解決。
- オーナー作業: StripeのAfter payment設定のリダイレクト先を
  https://akieguchi.com/start?thanks=1 に変更（?thanks=1が肝）。

## タスク: /start への導線追加（実装=Codex・オーナー承認の役割反転 / レビュー=Claude）

- 背景: /start・/start/en はどのページからもリンクされておらず、URL直打ちか
  決済後リダイレクトでしか到達できなかった（オーナー指摘）。
- 実装(Codex): LP料金セクションの注意書き直下に「購入後の流れを先に見る」
  （EN: Preview what happens after purchase）の控えめなテキストリンクを追加。
  購入CTAより目立たない・可視性ゲート内（配布先に漏れない）・日英出し分け。
  既存の描画テストに日英リンクの検証を追加。
- レビュー(Claude): 差分16行を確認。ゲート内配置・言語出し分け・トーンとも問題なし。
- 検証: bun run check EXIT=0・490 pass / 0 fail（Codex実行に加えClaudeでも再実行）。
  admin非接触のためsmoke対象外。
- 補足: 本番で /start?thanks=1 にお礼が出ないのは ed0d757 未pushのため（正常）。

## タスク: 「はじめに」画面のCodex調査（read-only・実装は次セッション=T-9）

- 背景: 販売一本化後、購入者の唯一の頼りは管理画面「はじめに」（オーナー実例:
  父は旧セルフ設置で全滅→「はじめに」からは使えた）。5時間制限が近いため、
  今日は調査のみCodexに委任し、実装はT-9として次セッションへ。
- Codex結論: 土台は良いが「唯一の頼り」には重大な抜け2点。
  P0-1: 0/5でも「セットアップ完了」を押せて、以後「はじめに」へ戻れなくなる
  （最も危険な途中離脱。admin.tsx:1090）
  P0-2: 5段階の手順が納品前提と食い違い「まだ技術的な公開作業が必要」と誤解させる。
  主目標は「写真1枚→トップ表示確認」の最短化（admin.tsx:953）
  P1×5: ステップ番号と次の行動 / 「あとで」の再開位置 / 読み込み失敗の案内 /
  完了判定と説明の一致 / 購入者向けエラーの技術用語除去（admin-i18n.tsx:3067）
  P2×1: 「公開の裏側」等の隔離 + 日英×スマホ導線テスト
- 処置: docs/agents/task-queue.md に T-9 として登録（詳細はCodex出力の表を上記要約）。

## タスク: T-9主要部の実装 + /start・thanks丁寧化（実装=Codex×2本 / レビュー=Claude）

- オーナー指示: 「はじめに」と /start(thanks含む)をもっと丁寧に。ドメイン未所持の
  不安解消を明示。Codexをガッツリ使う。SNS以外の前回宿題は完了済み。
- Codex実装①(はじめに): 必須導線を3項目(写真→トップ写真→トップページ確認)に短縮、
  名前等は「あとで整える」へ。未完了時の完了確定ブロック。折りたたみ時の進捗+次の
  作業表示。トップページを開いた事実を完了条件化。エラーの技術用語除去。
  Claudeレビューで実バグ1件検出→修正(メインボタン経由だと確認フラグが立たず
  永遠に完了できない)。check 491 pass / smoke 35 passed → e0f29a8
- Codex実装②(/start): ドメイン安心セクション新設(「持っていなくても大丈夫」
  +住所の平易な説明+本人名義+実費は購入前確認)、thanksを「すぐ何もしなくて大丈夫」
  の安心文言へ、メール下書きにもドメイン案内、「納品後の最初の一歩」を新しい
  はじめに導線と整合。check 492 pass / 0 fail(admin非接触でsmoke対象外)
- T-9残: 読み込み失敗時の案内(P1,M) / 「公開の裏側」隔離+日英スマホ導線テスト(P2,M)
  → task-queue の T-9 に残タスクとして明記。
