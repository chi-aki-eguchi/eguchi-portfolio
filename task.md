# Task Log

<!-- CURRENT_STATE_START -->
## Current State — 2026-08-31 JST（現れる尺を上げた）

- **Status:** 動きの直し5巡目。**push 済み・本番反映済み（`89d460e`）。**
- **Current owner:** Claude Code / **Handoff readiness:** ready
- **Branch:** `main` / **HEAD:** `SELF`

### 今日の流れ（同じ依頼を5回受けて、毎回ちがう場所が原因だった）

| commit | 原因だった場所 |
|---|---|
| `5877091` | カーブが時間を**見えない所へ捨てて**いた（`--ease-reveal` 新設）|
| `ce2dc63` | ライトボックスは DOM から外していて transition が**走れなかった**|
| `bbbec72` `fab5804` | admin の段差は当たり先が中身ではなく**骨組み**だった |
| `490a227` | 開いた瞬間、**器が自分を組み立て直している**のが見えていた |
| `89d460e` | **尺そのものが足りなかった。カーブを直した後だから効く**（旧カーブでは伸ばした分の3割しか見えなかった）|
| `afe642e` | **「触った反応は速いままにする」という判断そのものが間違いだった** |

### 本番の実測（`89d460e`・PC 1440px）

     628ms  写真 見え始め        ← 写真が先
    1274ms  ナビ 見え始め        ← 器は 0.65 秒後
    1786ms  写真 出そろい
    2116ms  ナビ 出そろい
    ヒーローの寄り引き 1.045倍 → 1倍（半分 1170ms / 9割 1795ms）

**ヒーローはこの日まで、サイト中で唯一「動きも現像も」持っていなかった。**

### 検証

- `bun run check` = **1146 pass / 0 fail（EXIT=0）**
- `bun run smoke` = **351 passed / 0 failed（EXIT=0）**
- `public-motion-tempo.spec.ts` が尺を見張る（**前のめりなカーブへ戻すと落ちる
  ことを、一度戻して確認済み**）
- **オーナーの目での確認待ち。**「まだ速い / 遅い」はここでしか決まらない

### 触った反応（`afe642e` で撤回した判断）

「押して待たされるのは鈍さ」という理屈が当てはまるのは**待たされる動きだけ**。
写真の影も札も誰も待たせていない。**何も塞いでいないのに速い動きは安い。**

    写真の影 400→750ms / 札 350→620ms / ライトボックス 500→850ms
    ページを移る 250ms `ease-exit` → 420ms `ease-reveal`（`PAGE_EXIT_MS`）

**踏んだ穴:** 共有の目盛り（`--dur-hover/caption/lift`）を直接伸ばして smoke が
2件落ちた（ナビの下線 scaleX 0.947 / admin のダイアログが動いて押せない）。
**器と道具はそれを使っている。**`--dur-photo-hover` / `--dur-photo-caption` を
写真専用に立てて分離。**「一つの語彙、二つの拍」を混ぜない。**

### 次にやること

- **オーナーが本番を触る。**もう一段上げるも、1つ前へ戻すも、数値1組の入替え
- **B-23**: `/gallery` の骨組みが 61px 押し下げられる。出所まで測ってある
- **案1: プレビューを Settings 以外のタブへ。**承認済み・未着手

### 触ってはいけない範囲

- 本番DB・Turso・R2・Railway・環境変数・`.env` / `shotAt` の保存方法 / 公開API応答形
- Lightbox は 2026-08-31 にオーナー承認で触れた。**次も承認が要る**
- 動きの正本は `docs/specs/design-spec.md` §6。**duration ではなく
  「目に見えている時間」で決める**（測り方も同節）
<!-- CURRENT_STATE_END -->
<!-- CURRENT_STATE_END -->
<!-- CURRENT_STATE_END -->

---

## 過去記録

過去 Handoff（133本）と過去 Current State は `docs/archive/task-handoffs.md` へ移した。
**このファイルには Current State だけを置く。**履歴を戻さない。
新しい Handoff が必要なときは、archive 側の末尾へ追記する。
