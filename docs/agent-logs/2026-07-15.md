# 2026-07-15 決定ログ

## T-2: Service機能の設定ゲート化 — 実装=Codex/レビュー=Claude(2周)

- **実装**: 新settingsキー `servicePageMode`("" = legacyホスト判定 / "on" / "off")。
  判定は共有純関数 `resolveServiceVisibility`(shared/service-visibility.ts、旧
  web/lib版を統合)に集約し、公開ナビ・/serviceルート(未表示時はトップへ
  redirect)・OGP(未表示時はNot Found+noindex)・adminタブ/⌘Kパレット/モバイル
  ナビ・admin Settingsのトグル(自動/表示/非表示)まで一貫適用。§0の4箇所同期済み。
- **レビュー1周目でP1**: 本番の siteUrl 設定が空のため、localhost(dev/smoke)で
  adminのServiceタブが消え、smoke3検査が黙ってskip(31→28 passed)+ローカル開発
  不能に。→ adminのタブ表示に限り `import.meta.env.DEV` では常に表示する
  フォールバックで解消(公開側の判定は不変)。smokeは31 passedに復帰。
- **既定値の安全性**: servicePageMode未設定ではlegacyホスト判定に落ちるため、
  akieguchi.com本番の見た目は無変更。配布先はドメイン不一致で自然にOFF。
- **検証**: `bun run check` exit 0(406 pass)、`bun run smoke` 31 passed/0 failed。
- **オーナーへの申し送り**: 本番の siteUrl 設定が空だった。管理画面Settingsで
  `https://akieguchi.com` を設定しておくことを推奨(急ぎではない)。
- **push**: していない。

## T-3: フッターのテンプレート購入クレジット — 実装=Codex/レビュー=Claude(指摘なし)

- 新settingsキー2つ: `templateCreditLabel`(既定 "Site template by Aki Eguchi"、
  空=非表示) / `templateCreditUrl`(既定 https://akieguchi.com/service 暫定、
  オーナー決定: 後でServiceページを整えたら差し替え)。台帳登録により
  live previewも自動対応(JS_PREVIEW_KEYS=台帳全体)。
- フッター最下部・コピーライト直下に一段控えめな1行。URLは http/https のみ許可
  (`httpHrefOrNull` 純関数+テスト。javascript:等は弾いてリンク無しテキスト化)。
  admin Settingsに文言/URL入力欄。renderテスト追加。
- 検証: `bun run check` exit 0(409 pass)、`bun run smoke` 31 passed(Codex側と
  claude側の両方で実行)。レビュー指摘なし(1周クリア)。
- push: していない。

## T-4: 配布テンプレート用OGPカードの自動生成

- **方針**: `akieguchi.com` の既存JPEGを維持し、サービスホストではない配布先だけ
  settings由来の1200x630 PNGへ切り替える。ホスト判定は既存
  `isServiceSiteUrl()`、Service公開判定は既存`resolveServiceVisibility()`を再利用した。
- **生成**: 紙色とink色、細い枠、中央セリフ体のSVGを純粋関数で組み立て、既存の
  sharpでPNG化。サイト名・英名・副題は既存`composeBaseTitle()`で合成し、空なら
  `Photography`を使用する。
- **配信**: `/og-default.png`をfaviconと同じプロセス内キャッシュ+
  `settingsVersion`失効で配信。`Cache-Control: no-cache, must-revalidate`、
  `Content-Type: image/png`。`Content-Encoding`は設定していない。
- **迷った点**: Serviceの表示ONとサービスホスト判定は別概念。配布先でServiceをONに
  してもオーナー画像を出さないため、画像選択だけはホスト判定を独立して使った。
- **検証**: `cd packages/web && bun run typecheck`成功、`bun test ./src`成功
  (421 pass / 0 fail)、`bun run check`成功、
  `DATABASE_PROVIDER=postgres bun run build`成功、`git diff --check`成功。
  生成見本を目視し、1200x630内で文字・枠が切れていないことを確認した。
- **要相談**: なし。admin未変更のためsmokeは対象外。
- **push**: していない。git commitもしていない。

### T-4 レビュー・統合(claude-driver)

- レビュー指摘なし(1周クリア)。ホスト判定による既存カード温存、
  og:image:type のPNG追随、socialImagePath素通しを確認。
- 検証: `bun run check` exit 0(421 pass)、`bun run smoke` 31 passed/0 failed、
  ローカル実サーバで /og-default.png(1200x630 PNG・35KB)を目視 —
  「江口秋 | Aki Eguchi | Photography」が紙色+細枠+セリフ体で描画。
  トップの og:image は従来どおりヒーロー写真(無変更)を実測確認。
- **申し送り**: SVG→PNGの文字描画はサーバのフォントに依存する。次回デプロイ後、
  本番の /og-default.png を1度目視して日本語グリフが出ることを確認すること
  (Railwayコンテナのフォント欠如でtofu化する可能性が理論上ある)。
- push: していない。

## T-5: 配布先サイトの更新手順整備 — 実装=Codex/レビュー=Claude(指摘なし)

- オーナーフィードバック反映後の初タスク: 依頼文はゴール+境界のみの短い形式。
  成果物の設計(新規2文書+既存4文書の更新、10分手順、リリース条件、
  DBマイグレーション有無での可否判定)はCodexが自律的に構成した。
- 1回目の実行が2時間ハング(CPU 0%・成果物ゼロ)し、killして再発注。以降、
  長時間のcodex execには15分ウォッチドッグを標準添付する運用にした。
- レビュー: ローカル事実(/api/healthのstatus+build、migrateログ文言、
  リポジトリ名、drizzle-postgres/)を全て実物照合、Railwayの操作記述
  (Deploy Latest Commit/⌘K/Redeploy/Rollbackの意味・テンプレート更新通知)は
  公式docsをWebFetchで照合し全一致。指摘なし。
- 検証: コード変更なしのためcheck対象外(git diff --checkのみ)。
- push: していない(本項)。※T-1〜T-4はオーナーがpush済み・本番反映確認
  (build=88bd42d7)。

## 本番稼働確認(T-1〜T-4、オーナーpush後・build=88bd42d7)

- favicon: 本番の apple-touch-icon.png / favicon-v2.svg が動的生成に切替済み、
  プロフィール写真が正しく描画されることを目視確認。✅
- /og-default.png: **P1 — 全文字が豆腐化**(日本語・英字とも)。Railwayコンテナに
  フォントが皆無で、sharp(librsvg)のtext描画が全滅するため。ローカルMacでは
  OSフォントで描画されていた。実害は現時点ゼロ(このカードは非サービスホスト=
  配布先専用で、akieguchi.comのog:imageはヒーロー写真のまま)。→ T-6として修正。
