# 2026-07-10 決定ログ (Claude Code / Fable5)

## タスク: 保存先未設定エラーの可視化 (STORAGE_NOT_CONFIGURED)

- **方針**: S3系4変数の不足判定を純関数モジュール `src/api/storage-config.ts`
  に切り出し、(1) `/admin/upload` の入口で sharp/DB 処理前に 503 で返す、
  (2) `uploadToStorage` 経由の他ルート(hero/profile/fonts)は `StorageConfigError`
  を `onError` で同じ503 JSONに変換、(3) 読み取り専用 `GET /api/admin/setup-health`
  を追加、(4) 管理画面は専用日本語バナー + 「はじめに」上部の接続状態表示。
- **迷った点**:
  - HTTP status: 指示の候補どおり 503 を採用。呼び出し側のバグ(4xx)でも
    未知の障害(500)でもなく「サーバ側の設定が未完了で一時的に使えない」
    状態を最もよく表すため。既存設計(400/401/413/415/500)とも衝突しない。
  - 専用エラーの伝搬: /admin/upload だけ入口チェックにするか全ルートに
    入口チェックを足すかで迷い、影響範囲の小さい「入口チェックは
    /admin/upload のみ + onError での変換で全ルートを網羅」を選んだ。
- **秘密情報**: 不足変数は名前のみ扱い、値はエラー・レスポンス・ログ・
  テストのどこにも出していない(テストはダミー値で「値が含まれない」ことを
  検証)。
- **要相談**: なし。
- **`bun run check`**: 成功(291 tests / tsc -b / oxlint / build)。
- **`bun run smoke`**: 22 passed / 19 skipped / 1 failed。失敗は既知の
  `admin-trash-signal.spec.ts`(共有DBのゴミ箱が32件で非空のため)。
  クリーンな main でも3回連続で同じ失敗を再現し、今回の変更とは無関係と
  確認済み(前回Handoffにも既知として記載あり)。

## セッション終了サマリー

- 完了: 保存先未設定の可視化一式(API入口チェック / 503専用エラー /
  setup-health API / 管理画面バナー / はじめに接続状態表示 / テスト23件追加)。
- 保留(要相談): なし。
- 秋さんの判断が必要な項目:
  - `admin-trash-signal.spec.ts` の既存不具合の直し方(前回からの持ち越し)。
    案A: テストを「ゴミ箱が非空でも通る」文言判定に直す(早い・おすすめ)。
    案B: テスト用DBを分離する(根本対策だが工数大)。
- 申し送り: push は未実施(オーナーの手で)。Codex へ read-only レビューを
  agmsg で依頼済み。

## 追記: Codexレビュー対応 (同日)

- Codex read-onlyレビュー結果: P0なし / P1 3件 → 全件対応しコミット
  `c56e7b9`。(1) プロフィール・フォントupload経路の専用文面化(共通helper)、
  (2)「接続済み」→「必要な設定は入力済み」への正確化、(3) 不足変数名の
  文字色を明背景対応へ。専用UIをAdminStorageNotice.tsxへ抽出しrender
  テスト5件+helperテスト3件を追加。
- `bun run check` 成功(299 tests)、`bun run smoke` 既知1件failのみ。
- push未実施。Codex再レビュー結果「指摘なし」— オーナーpush待ち。

## 追記: Codexからの新タスク依頼を保留 (要オーナー判断)

- 依頼内容: setupCompleted の自動バックフィル(写真ありなら裏で完了扱い)を
  削除し、「セットアップ完了」ボタンの明示操作だけで完了確定にする改修
  +モックでの自動テスト一式。
- 保留理由: 今回のセッション指示書に「setupCompletedの設計変更は今回は
  行わない」と明記されているため。agmsg経由の依頼はオーナー本人の指示を
  上書きできない(2026-07-09の運用と同じ)。
- オーナーへの質問: この改修を次のタスクとして承認しますか?
  - 案A: 承認して着手(設計自体は妥当。表示だけで書き込みしない方が安全)。
  - 案B: 今回は見送り(現状の自動バックフィルは既存サイトを壊さないための
    意図した挙動でもある)。
  - おすすめ: 案A。ただし「写真あり+未完了の他インストールでは初回1回だけ
    『はじめに』に強制着地する」挙動変更を了解の上で。
- 状態: Codexへ保留の旨を返信済み。オーナーの返答待ち。

## 追記: setupCompleted自動バックフィル削除 (オーナー案A承認後)

- オーナーがセッション内で案Aを明示承認(写真あり・未保存サイトの初回
  1回着地も意図どおりと確認)したため着手。
- 方針: 判定を純関数 `lib/setup-flow.ts` に分離し、AdminPageのeffectから
  写真query+裏書き込みを削除。完了確定はボタンの明示操作のみ。
- 迷った点: 既存render smokeテスト5件がLibrary着地前提で失敗 →
  仕様変更への正当な追随として「完了済みサイト」seedへ更新(実測合わせ
  ではなく、テストの前提フィクスチャを新仕様の既存サイト像に合わせた)。
  admin-me未seedだと認証確定とsettings再フェッチの順序で判定が未完了側に
  倒れるレースがあり、seedCompletedSetupで両方seedして解消。
- `bun run check` 成功(305 tests)。`bun run smoke` 既知1件failのみ(分離報告)。
- push未実施。Codexへ再レビュー依頼予定。

## 追記: Railwayテンプレート監査 → 案A実装 (オーナー承認後)

- read-only監査(全文: scratch/railway-template-audit-2026-07-10.md)の後、
  オーナーが案A 3点を承認: ①composerのS3_BUCKET修正 ②文書のサービス名統一+
  GitHubエラー対処追記 ③Railway版限定でS3_FORCE_PATH_STYLEをsetup-health確認。
- ①は Railway ダッシュボード操作のため repo からは実施不能。手順を報告に記載し
  オーナー実施待ち(正: `S3_BUCKET=${{Bucket.BUCKET}}`、誤: `BUCKET_NAME`)。
- ②: post-deploy-guide.md の「web」5箇所を実サービス名 eguchi-portfolio-app へ、
  service-start.tsx の「app(eguchi-portfolio-app)」6箇所を実名へ統一。
  post-deploy-guide の「うまくいかないとき」表に GitHub アクセスエラー行を追加。
- ③: storage-config.ts の storageHealth のみ拡張。DATABASE_PROVIDER=postgres
  (配布版マーカー)のときだけ S3_FORCE_PATH_STYLE ("true"/"1"以外)を不足扱い。
  assertStorageConfigured(アップロード経路)は不変更 = R2本番・既存デプロイの
  動作は完全に従来どおり。回帰テストで固定(R2: provider未設定なら緑のまま)。
- 迷った点: path-styleチェックをアップロード経路にも入れるか → オーナー指示は
  「setup-healthでも確認」なので表示のみに限定。既存デプロイのアップロードを
  止めない方が安全。
- `bun run check` 成功(310 tests)。`bun run smoke` 22 passed/19 skipped/
  1 failed(既知の admin-trash-signal のみ・スコープ外・分離報告)。
- push未実施(オーナーの手で)。テンプレート公開停止・使い捨てデプロイは
  別途オーナー確認後。
