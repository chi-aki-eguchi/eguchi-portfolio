# 写真管理・グリッド強化 仕様書

> Claude Code向け。既存仕様書群の続編。§0既存パターン厳守。
> 全項目確定済み。ループで随時実装・追加していく前提。

---

## 0. 前提（厳守）

- DBクエリは `withRetry(() => db....)` でラップ
- データ更新後は `qc.invalidateQueries`
- 新規settingsキー追加時は3箇所セットで更新
- スキーマ変更時は `cd packages/web && bun run db:push`
- 全書き込みはres.ok/assertOkチェック、失敗時エラー表示
- ロード中は空状態メッセージを出さない

---

## グループL: シリーズ写真表示バグ修正【最優先・即対応】

### L1. バグ内容
シリーズに割り当てた写真が `/series/:slug` に表示されない。

### L2. 調査・修正箇所
- `GET /series/:slug` のAPIが `photos.seriesId` で絞り込んでいるか確認
- `photos` テーブルの `seriesId` カラムに実際に値が入っているか確認
- インスペクタからシリーズを割り当てたとき、`PATCH /admin/photos/:id` に `seriesId` が正しく渡っているか確認
- 空文字 `""` を `null` に変換しているか確認（`seriesId: "" → null`）
- 公開側 `series-detail.tsx` がAPIレスポンスの `photos` フィールドを正しく使っているか確認

### L3. 受け入れ基準
- [ ] インスペクタでシリーズを選択・保存すると、そのシリーズ詳細ページに写真が表示される
- [ ] シリーズ未割り当て（null）の写真はどのシリーズにも出ない

---

## グループM: 写真の一括操作強化

### M1. 現状の一括操作
- Cmd+A / Shift+クリックで複数選択（実装済み）
- 一括カテゴリ変更（実装済み）
- 一括削除（実装済み）

### M2. 追加する一括操作（全項目）

管理画面 GalleryTab のツールバーに、複数選択時に以下を全部表示：

| 操作 | UI | 挙動 |
|---|---|---|
| 公開/非公開 | トグルボタン2択 | 選択写真の `isPublished` を一括更新（※isPublished未実装なら追加） |
| フィーチャー（トップに出す） | トグルボタン2択 | `hero_photos` への一括追加/削除 |
| シリーズ割り当て | ドロップダウン | 既存シリーズ一覧＋「割り当て解除」 |
| カテゴリ変更 | ドロップダウン | 既存（拡張は不要） |
| 表示サイズ S/M/L | セグメントボタン | `displaySize` を一括更新 |
| 削除 | ボタン | 論理削除（既存） |

- 一括操作後は選択を維持したまま結果をトーストで通知（「N枚を更新しました」）
- 失敗時は失敗分のみエラートースト
- 全操作はres.okチェック・assertOk済みであること

### M3. 写真フィルター強化

現状のフィルター（カテゴリ・ゴミ箱）に以下を追加：

- **シリーズでフィルター** — 「シリーズ未割り当て」を含む全シリーズを選択肢に
- **表示サイズでフィルター** — S / M / L
- **フィーチャー済み** — hero_photosに登録済みの写真だけ表示

フィルターはカテゴリと組み合わせ可能（AND条件）。

### M4. インスペクタの改善

- シリーズ選択に「割り当て解除」オプションを追加（現状、解除手段が不明確）
- 現在の割り当てシリーズ名を表示
- 写真のステータス（フィーチャー済み / シリーズ名 / カテゴリ）をインスペクタ上部にバッジで表示

### M5. 受け入れ基準
- [ ] 複数選択時にM2の全操作がツールバーに表示される
- [ ] 各一括操作が正しく動作し、結果をトーストで通知する
- [ ] シリーズ・サイズ・フィーチャーでフィルタリングできる
- [ ] インスペクタでシリーズ割り当て解除ができる

---

## グループN: グリッドレイアウトの種類選択

### N1. 選べるグリッドタイプ（6種類）

管理画面 Settings「ギャラリーレイアウト」セクションから選択。ページ（ギャラリー・シリーズ・トップ）ごとに個別設定可能。

| ID | 名前 | 説明 | 特徴 |
|---|---|---|---|
| `mosaic` | モザイク（現行A） | S/M/Lサイズ混在・制御ランダム配置 | 雑誌的・こなれ感 |
| `stagger` | ずらし大（現行C） | 1枚ずつ大きく・左右互い違い | 没入感・作家性 |
| `grid` | 均等グリッド（D） | 全部同サイズ・等間隔整列 | 端正・ミニマル |
| `scroll` | 縦スクロール1枚 | 1枚ずつ縦に大きく並ぶ | 写真集・全集中 |
| `editorial` | 雑誌見開き（B） | 左右対称・余白多め・大小対比 | GISELe的 |
| `freeform` | フリーフォーム | S/M/Lを秋が指定、配置は自動最適 | 最大の自由度 |

### N2. 設定項目

新規settingsキー:

```
galleryLayout        # ギャラリーページのレイアウトタイプ（既定: mosaic）
seriesLayout         # シリーズ詳細ページのレイアウトタイプ（既定: mosaic）
topWorksLayout       # トップページのWorksセクション（既定: stagger）
```

管理画面での切り替えUI:
- 各ページごとにタイル型ボタン（アイコン＋名前）で視覚的に選択
- 選択するとプレビューに即反映（ライブプレビュー）
- 各タイプにツールチップで特徴説明

### N3. 各レイアウトの実装仕様

**mosaic（モザイク）** — 既存の制御ランダム実装をそのまま使用。galleryGapScale・galleryEmptyRate等の調整スライダーも継続。

**stagger（ずらし大）** — 既存のトップWorksセクションの実装を汎用化。奇数番目は左寄り・偶数番目は右寄り。写真は列幅の75〜90%。

**grid（均等グリッド）** — 全写真を同サイズ（S/M/Lを無視）で等間隔整列。列数は galleryColumns の設定を使用。シンプルなCSS Grid。

**scroll（縦スクロール1枚）** — 写真を1枚ずつ縦に大きく表示。幅100%または80%。写真間に余白たっぷり。キャプション（タイトル・撮影情報）を写真の下に表示。写真集・ポートフォリオブック的な見せ方。

**editorial（雑誌見開き）** — 2枚1組でペアを作り、左に大・右に小（または左右対称）で並べる。ペアの間に大きな余白。奇数枚のとき最後の1枚は中央配置。

**freeform（フリーフォーム）** — S/M/Lを秋が指定した通りに配置。制御ランダムの「ランダム」部分をなくし、サイズ順に詰めるだけ。最もシンプルで予測可能。

### N4. ループで随時追加するパターン候補

以下は今後のループで調査・実装を追加していく:
- **Polaroid風** — 白枠付き・少しランダムに傾いた配置
- **タイムライン** — 撮影日順に時系列で並べる（date軸のスクロール）
- **比較スライダー** — 2枚の写真を左右スライダーで比較（Before/After用途）
- **フルブリード** — 1枚が画面幅いっぱい、次の写真はスクロールで
- **コラージュ** — 重なり・角度をランダムに（スナップ写真的な雰囲気）

各パターンを実装したら管理画面の選択肢に追加していく。

### N5. 受け入れ基準
- [ ] 6種類のレイアウトを管理画面から選択できる
- [ ] ページごと（ギャラリー・シリーズ・トップ）に個別設定できる
- [ ] 選択するとライブプレビューに即反映される
- [ ] 各レイアウトがPC・スマホ両方で崩れない

---

## グループO: 写真管理の追加機能（随時追加）

ループで見つかり次第実装する機能候補。優先度順。

### O1. 写真の複製
- インスペクタに「複製」ボタン
- メタデータ（タイトル・カメラ・レンズ・カテゴリ・シリーズ）を引き継いで新規登録
- R2の画像はコピーせず同じURLを参照（ストレージ節約）
- 用途: 同じ写真を複数カテゴリ・シリーズに見せたいとき

### O2. 写真の一括メタデータ編集
- 複数選択して「一括編集」を開くと、共通項目（カメラ・レンズ・フィルムタイプ）をまとめて入力できるパネルが出る
- 空欄のまま保存しても上書きしない（変更した項目だけ更新）

### O3. 写真の並べ替えショートカット
- キーボードで選択中の写真を上下に移動（既存の↑↓ボタンに加え、Ctrl+↑↓でも動かせるように）

### O4. 最近アップロードした写真フィルター
- 「直近7日」「直近30日」でフィルタリング
- 新しく入れた写真を素早く確認・整理するため

### O5. 写真の使用状況表示
- インスペクタに「この写真の使用箇所」を表示
  - ヒーローに設定中: ✓
  - シリーズ名: ○○
  - カテゴリ: ○○
  - トップのWorks: 表示順N番目

### O6. スマートアルバム（自動グループ化）
- 条件を設定すると自動で写真を集める仮想フォルダ
- 例: 「カメラ=PENTAX 67」「フィルム」「未分類」「最近追加」
- シリーズ・カテゴリとは別の軸で探せる
- 設定はJSON等でflexible

---

## データモデル追加

### siteSettings 追加キー
```
galleryLayout, seriesLayout, topWorksLayout
```

### photos テーブル（要確認・未実装なら追加）
```
isPublished: integer（0/1, 既定1）  # 公開フラグ（カテゴリ/シリーズとは別）
```

### 新規API
```
POST /admin/photos/batch          # 一括操作（M2）
  body: { ids: number[], operation: "publish"|"unpublish"|"feature"|"unfeature"|"series"|"category"|"size", value?: string }
```

---

## 実装優先順

| 優先 | 項目 | 理由 |
|---|---|---|
| ★★★ | L1 シリーズ写真バグ修正 | 今すぐ壊れてる |
| ★★★ | M2 一括操作全項目 | 写真が増えるほど必須 |
| ★★ | N1〜N4 グリッド6種類 | 作品の見せ方の核 |
| ★★ | M3 フィルター強化 | 整理のしやすさ |
| ★ | O1〜O6 追加機能 | ループで随時 |

---

## ループでの進め方

- L1（バグ）→ M2（一括操作）→ N（グリッド）→ M3/M4 → O系の順で進める
- グリッドのパターン（N4候補）は1つずつ実装・追加。完成したら管理画面の選択肢に追加してリリース
- O系はループの余力で随時追加。使ってみて「いる/いらない」は後で判断
