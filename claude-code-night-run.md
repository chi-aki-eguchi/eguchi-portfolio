起動したら /night-run スキルを実行してから作業を開始すること。
# Claude Code 夜間自走指示

## 基本ルール
- CLAUDE.mdを最初に読んでプロジェクト構造を把握してから着手
- 確認なしで進めてOK。判断に迷ったらスキップして次へ
- 1つのタスクに30分以上ハマったらスキップ
- 各フェーズ完了後に `git push`
- 完了・スキップ・発見した問題を NIGHT-RUN-LOG.md に追記

## 禁止事項
- DBスキーマの変更（Drizzleマイグレーション）
- R2バケットの操作
- 環境変数の追加・変更
- 新機能の追加（既存機能の改善・バグ修正のみ）

---

## フェーズ1：申し送り事項の解消

### BUILD_IDのstale問題
- 現状：BUILD_IDが `20260615` で固定されていてデプロイのたびにズレる
- 修正：ビルド時に `RAILWAY_GIT_COMMIT_SHA`（Railwayが自動提供）を注入する仕組みに変更
- vite.config.ts の define に `__BUILD_ID__: JSON.stringify(process.env.RAILWAY_GIT_COMMIT_SHA ?? new Date().toISOString())` を追加

### font preloadと:rootフォント不一致
- settings の既定フォント値を確認し、:root のフォント指定と一致させる

### 多重送信ガード（admin.tsx）
- バッチ操作ボタン・ヒーロー設定ボタンで連打すると多重送信が起きる
- isSubmitting stateでボタンをdisabledにして防ぐ
- admin.tsxが大きいので慎重に、影響範囲を最小限にして実装

---

## フェーズ2：UX・見た目の品質向上

Playwrightで実際にブラウザを操作しながら以下をチェック・修正。

### アニメーション・トランジション
- ページ遷移がカクつく箇所を修正
- 画像の読み込み完了時のフェードインが不自然な箇所を修正
- スクロールアニメーションのタイミング・イージングが不揃いな箇所を統一

### テキスト・タイポグラフィ
- 長い英単語・URLが折り返されず画面外にはみ出す箇所（break-words）
- 行間・文字間がページごとにバラついている箇所を統一
- モバイルで文字が小さすぎる箇所

### 画像表示
- アスペクト比がおかしい箇所
- サムネイルのobject-positionが顔・被写体から外れている場合はcenterに統一
- シリーズ一覧の表紙画像のサイズが揃っていない箇所

### ローディング・フィードバック
- 操作後のフィードバック（トースト・スピナー）が出ない箇所
- ローディング中にレイアウトがガクッとずれる（CLS）箇所

---

## フェーズ3：SEO・メタデータの最終確認

- 全シリーズページのog:imageが正しく設定されているか確認・修正
- 写真のalt属性が空のものを洗い出してシリーズ名+連番で補完
- JSON-LDの内容に不正なフィールド・空フィールドがないか確認

---

## フェーズ4：コード品質

- tsc -b でエラー・warningが出ていれば修正
- oxlint でエラーが出ていれば修正
- bun test で失敗しているテストがあれば修正
- 不要なconsole.logが残っていれば削除

---

## 最終

- `bun run build` で最終確認
- `git push`
- NIGHT-RUN-LOG.md に今夜のサマリーを追記
  - 修正した問題一覧
  - スキップした項目と理由
  - 次回への申し送り
