# 2026-07-11 決定ログ (Driver: Claude Code / Fable5)

## タスクA: /service/start v2(実測ベース再設計)

- **選んだ方針**: 2026-07-10 の使い捨て実デプロイ実測(Codex/オーナー実施)を正とし、
  通常導線を3ステップ(Configure→ADMIN_PASSWORD→Save Config→Deploy / Online待ち+
  Generate Domain / admin/loginで写真1枚)に短縮。トラブル対応は `<details>` 折りたたみ
  「困ったときだけ」3項目(GitHubエラー / アップロード不能 / 相談スクショ)へ集約。
- **迷った点**: 旧10節ガイドを残すか。→ 実測で「通常は詰まらない」ことが確認できた
  ため置換。Variables のコードブロックと Logs 手順は折りたたみ内に保全(初回サポート
  で実際に使った実績があるため削除しない)。
- **helper分担**: Haiku helper 1体(read-only)に現行文言と composer 実態の矛盾表を
  作成させ、設計・実装・検証は Fable 本体が実施(指示どおり)。
- **post-deploy-guide.md の最小更新理由**: ②「変数一覧から ADMIN_PASSWORD を探す」
  ③「全部が緑になれば完了」が実測(Configure/Save Config 必須、Save まで Deploy が
  disabled、Bucket は empty のまま = 正常)と矛盾するため、その2節のみ書き換え。
  他の節は不変更。
- `bun run check`: 成功(323 tests / 0 fail)。

## タスクB: 配布版Postgresのreorder失敗(42804)修正 + ロック復帰UX

- **経緯**: 当初仮説は sessionStorage の librarySort による reorderLocked。作業中に
  オーナーが先輩側の実症状(「並び替えに失敗」表示・矢印操作でも失敗)を確認し、
  保存経路(POST /admin/photos/reorder)の失敗が本命に変わった。
- **原因特定(推測修正の禁止に対応)**: PostgreSQL の型解決ルールで「THEN 側が全て
  未型付けパラメータの CASE」は text と解決され、integer 列 sort_order への代入が
  42804 で失敗する。SQL文面の生成確認(Codex実施)では正しく見えるが、サーバ側の
  実行時型推論で落ちる。**PGlite(実Postgres/WASM)で再現し、CAST 修正で解消する
  ことも実行確認した**(scratch/pg-reorder-repro/repro.ts, verify-fix.ts)。
  SQLite/libSQL は動的型付けのため本番 Turso では一度も発現しない=「先輩側だけ」
  の説明がつく。
- **選んだ方針**: `api/reorder-sql.ts` に CASE 組み立てを共通化し
  `THEN CAST(? AS INTEGER)` で両方言とも整数に固定。photos/categories/series/
  pricing/hero-photos の5エンドポイント全てに適用(同一パターンのため)。
  失敗時ログはテーブル名+エラーコード/メッセージのみ(ids・秘密値なし)。
- **迷った点**: 5箇所への1行修正 vs 共通ヘルパー化。→ ヘルパー化を選択。
  dialect 生成テストで CAST の存在を固定でき、将来の reorder 追加でも同じ轍を
  踏まないため。
- **UX側**: reorderLocked の判定を pure 関数 `reorderLockReason` に切り出し、
  原因別の文言+「並び替えできる状態に戻す」1クリック復帰(librarySort=manual +
  clearLibraryFilters。写真データ不変更)+ドラッグ/タッチ矢印の案内を追加。
  `draggable={!reorderLocked}` で見かけ上のドラッグ可能も解消。
- **テストの学び**: render テストで librarySort に "newest" を seed しても
  サニタイズ effect が正規リスト外の値を "manual" に戻すため無効。正規値
  "createdAt-desc" を使用(テスト内コメントに明記)。
- **PGlite の隔離**: `bun add` が workspace root の package.json / bun.lock に
  依存を追加してしまった(Codex 指摘)。両ファイルを revert し、
  scratch/pg-reorder-repro/ に専用 package.json を置いて隔離。最終差分に
  PGlite は含まれない。
- `bun run check`: 成功(323 tests / 0 fail)。
- `bun run smoke`: 22 passed / 19 skipped / **1 failed = 既知の
  admin-trash-signal.spec.ts のみ**(2026-07-10 Handoff と同一・今回変更起因 0件)。

## 要相談 / 未検証

- **実Postgres(先輩環境)での最終確認は未実施**(push もしていないため)。push →
  Railway 反映後、先輩側で並び替えを1回試してもらう必要あり。修正前に Railway
  Logs に `42804` / `is of type integer but expression is of type text` が出ていた
  はずなので、突き合わせれば根本原因の裏も取れる。
- admin-trash-signal.spec.ts の既存不具合は別チケットのまま。

---

# タスク: スマホ操作性の大幅改善(admin優先 + 公開サイト) (Driver: Claude Code / Fable5)

## 監査(実ブラウザ・Playwright)

- 390x844 / 375x667 で公開5ページ+Lightbox+admin9タブ+編集ドロワーの計36枚を
  スクショ化し、横はみ出し・32px未満タップ領域・fixed要素を計測
  (scratch/mobile-audit/、gitignored)。
- 結果: 横はみ出しは全画面0px。問題は (a)上部2段ナビの構造、(b)小さい
  タップ領域(categories/series/pricingの↑↓13px・削除13px・写真カード24px・
  hero28px・inspector close24px・公開ギャラリーフィルタ21px高 等)に集中。

## P0-1: adminモバイルナビ再設計

- **問題**: 上部2段横スクロールは (1)アクティブタブが画面外へ流れる(setupタブで
  実測: サイトグループ5タブ目が見切れてハイライトも見えない)、(2)画面最上部で
  片手の親指が届かない、(3)グループ行とタブ行で103pxを常時消費。
- **案の比較**: (a)9タブ下部バー=幅に収まらず却下 / (b)ハンバーガードロワー=
  全機能が2タップ奥+上部到達で却下 / **(c)採用: 下部固定バー3グループ+
  ボトムシート**(48px行でタブ一覧+active表示。単一タブの「写真」グループは
  直接切替)。
- **実装**: `admin-mobile-nav.tsx` 新設(AdminMobileTopBar/AdminMobileTabBar)。
  fixed ではなく admin-main(flex列)の最下行として配置し、z-index/キーボード/
  save bar との重なりを構造的に回避。iOS URLバー対策で admin シェルを
  100vh→100dvh 化。safe-area(--sai-top/bottom)対応。
- 未保存ガード(requestTab)・galleryUploading 中の無効化は旧ナビと同一契約。

## P0-2: タップ領域44px化(pointer: coarse 限定)

- styles.css に `admin-tap`(44px)/`admin-tap-sm`(40px)を追加。タッチ端末のみ
  min-width/height を持ち上げ、デスクトップの見た目は不変。
- 適用: 写真カード回転/移動(6)・hero(3)・categories/series/pricing ↑↓(6)・
  削除(3)・編集(2)・Sortセレクト・?ボタン・inspector close・inspector内の
  S/M/L/回転プリセット/見せる中心/一括操作バー。
- **CSSの罠2件**(将来の担当者向け):
  1. グローバル button リセット(`button:not(.absolute):not([class*="bg-black"])`、
     specificity (0,3,1)+一部!important)が新ナビの min-height/色を潰す →
     三重/四重クラスで対処(既存 .admin-btn-primary と同じ手法)。色は button
     でなく内側 span で描き分け。
  2. Tailwind の `!min-h-0` は @layer utilities 内の !important で、unlayered の
     !important より優先される → `!min-h-0` を `admin-compact`(unlayered
     normal+三重クラス)に置換して解決。

## P1

- inspector(写真編集ドロワー)に背景タップclose+backdrop暗転を追加。
  **未保存の編集がある間は背景タップで閉じない**(photoEditFormChanged 判定。
  Codexレビュー指摘を採用 — 広いclose面で編集消失の事故率が上がるため。
  X/保存/破棄の明示操作のみで閉じる)。
- 公開側: gallery の Photos/Series・カテゴリ・Film/Digital・Retry、service の
  Open に `tap-target`(疑似要素で±8/11px拡張、gap-x-6内で非重複)を追加。
  見た目は不変。

## テスト

- 新規: `admin-mobile-nav.render.test.tsx`(シート開閉・タブ選択・setup到達・
  アップロード中無効化)、`scripts/smoke/admin-mobile-nav.spec.ts`(mobile:
  下部バー→シート→Series遷移+44px実測 / desktop: 下部ナビ非表示+サイドバー
  表示。非書き込み)。
- **既存テスト更新(仕様変更への追随・検証の緩和なし)**:
  - pages.render.test.tsx 4件: 「グループボタン=先頭タブへ直接遷移」→
    「グループ→シート→タブ選択」に操作経路を変更。2クリック到達・未保存
    ガード発火という検証内容は不変。navGroup/sheetRow ヘルパー追加
    (buttonWithText だと「サイトで確認」に先にマッチするため)。
  - admin-sidebar-indicator.spec.ts: モバイル幅でのタブ移動経路のみ新ナビに
    変更。検証対象(リサイズ後のインジケータ追従)は不変。

## 検証結果

- `bun run check`: 成功(typecheck / lint / 325 tests 0 fail / build)。
- `bun run smoke`: **24 passed / 21 skipped / 1 failed = 既知の
  admin-trash-signal.spec.ts のみ**(2026-07-10から継続・今回変更起因0件)。
- 改善後の実ブラウザ再計測: 監査対象画面で32px未満のタップ領域 **0件**
  (改善前: categories 14件・gallery 12件など)。

## 波及ファイル(10ファイル・大きめ変更はオーナー明示許可済み)

新規3: admin-mobile-nav.tsx / admin-mobile-nav.render.test.tsx /
scripts/smoke/admin-mobile-nav.spec.ts。
変更7: admin.tsx / admin-tabs.tsx / gallery.tsx / service.tsx / styles.css /
pages.render.test.tsx / admin-sidebar-indicator.spec.ts。
API/DB/settings/画像パイプラインには触れていない。

---

# タスク: Inspector ×の未保存編集ガード (Driver: Claude Code / Fable5)

- **目的**: 写真編集パネル(Inspector)の × / Escape で未保存の編集が無言で
  消える経路をなくす(前タスクの背景タップ保護と一貫させる。Codex経由の
  オーナー依頼)。
- **選んだ方針**: `requestCloseInspector` を追加。編集なし=即閉じ。編集あり=
  既存の `confirmDialog`(キャンセル / 主ボタン)を再利用して
  「保存していない編集があります。保存せずに閉じますか？」+「保存せず閉じる」。
  新規モーダルは作らず、文言はタブ移動時の unsavedConfirm と同系に揃えた。
- **Escape も同じガードを通す**: × だけ守っても Escape が無言破棄のままでは
  穴が残るため。confirmDialog 表示中の Escape は Modal 側が閉じる(=編集続行)。
- **stale closure 対策**: Library の keydown effect の依存配列に `editForm` を
  追加(タイトル入力中に Escape しても最新の下書きで未保存判定されるように。
  従来は依存に無く、古い editForm で判定して素通りする)。
- **触れていない閉じ経路(意図的)**: Save成功後の閉じ・破棄ボタン・タブ移動
  (既存 unsavedConfirm)・Logout は従来通り。矢印キーでの写真切替
  (navByOffset→openInspector)は編集中の下書きを置き換える既知の残課題として
  記録のみ(スコープ外・要相談)。
- **検証**:
  - render テスト追加(pages.render.test.tsx): 未編集×=即閉じ /
    未保存×=確認表示→キャンセルで下書き保持→「保存せず閉じる」で閉じる。
  - `bun run check`: 成功(326 tests / 0 fail)。
  - `bun run smoke`: 24 passed / 21 skipped / 1 failed = 既知の
    admin-trash-signal のみ(今回起因0件)。
  - 390x844 実ブラウザ: 上記3経路+下書き保持を確認(probe4-confirm.png)。
    ※監査スクリプトの座標クリックがビューポート外/imgインターセプトで失敗
    しただけの疑似陽性があった — 実アプリの再オープンは正常。タイルは
    `button[aria-label=<filename>]` を直接クリックするのが正。

---

# タスク: Inspector 写真切替時の下書きガード (Driver: Claude Code / Fable5)

- **目的**: 未保存の下書きがある状態で別写真へ切り替えると下書きが無言で
  置き換わる P1(前タスクで記録した残課題)を、b17c7cc の ×/Escape 保護と
  同じ方式で塞ぐ(Codex経由のオーナー依頼)。
- **入口の洗い出し(grep: openInspector 呼び出し4箇所)**:
  1. `handlePhotoClick` 通常クリック(別タイル選択) — ガード対象
  2. `navByOffset`(矢印キー) — ガード対象
  3. Enterキー(カーソル写真を開く) — ガード対象
  4. `duplicatePhoto` onSuccess(複製を自動オープン) — 下書きがある時だけ
     自動オープンを抑止(複製自体は成功・トースト表示。書き込み完了後に
     「移動しますか」と聞くのは不自然なため確認ではなく抑止を選択)
- **実装**: `guardInspectorSwitch(target, proceed)` — 下書きなし or 同一写真は
  即 proceed。下書きあり+別写真は confirmDialog
  「保存せずに別の写真へ移動しますか？」+「保存せず移動」。キャンセル時は
  選択・カーソル・プレビュー・スクロールを含め一切動かさない(副作用を
  proceed に閉じ込め)。`openInspectorFor` — 同一写真の再オープンで editForm が
  保存値にリセットされる無言消失も防止(同一 id は no-op)。
- **モバイルでの発見**: 390x844 ではインスペクタ+backdrop が全面を覆うため
  「開いたまま別タイルをタップ」という入口は物理的に存在しない(backdrop の
  未保存保護が先に効く)。タイルクリック経路はデスクトップ幅で検証。
- **既知として分離**: Escape で確認が出る際に選択解除が先に走る既存挙動は
  今回混ぜない(挙動不変)。
- **検証**:
  - render テスト追加: clean矢印=確認なし移動 / dirty矢印=確認→キャンセルで
    写真・入力・選択維持→破棄で移動 / dirtyタイルクリックも同3経路。
  - `bun run check`: 成功(327 tests / 0 fail)。
  - `bun run smoke`: 24 passed / 21 skipped / 1 failed = 既知の
    admin-trash-signal のみ(今回起因0件)。
  - 実ブラウザ: desktop 1440x900 で6経路、mobile 390x844 で矢印2経路の
    計8チェック全PASS(probe5-switch-confirm.png)。
  - プローブの学び: `fill` 直後は input にフォーカスが残り矢印キーは
    typingガードで無視される(正しい挙動)。検証時は blur が必要。
    packages/web/.env は6/1の古い値 — ADMIN_PASSWORD はルート .env が正。

---

# 追記: 440e0b7 のコミット後検証と Codex レビュー結果 (Driver: Claude Code / Fable5)

- **経緯**: 前セッションが 440e0b7 をコミットして停止。引き継ぎ後、HEAD を
  独立に再検証した。
- **再検証**:
  - `bun run check`: 成功(326 tests / 0 fail、skip/todo なし)。
    ※前回記録の「327」は誤記とみられる(現行スイートは32ファイル326件)。
  - `bun run smoke`: 24 passed / 21 skipped / 1 failed = 既知の
    admin-trash-signal のみ(今回起因0件)。前セッションの記録と一致。
- **Codex read-only レビュー(2026-07-11 07:03Z 受領)**: 指摘なし(P0/P1なし)。
  guardInspectorSwitch の副作用封じ込め・openInspectorFor の下書き保持・
  複製自動オープン抑止・confirmDialog の背面操作遮断を確認済みとの回答。
  **440e0b7 は owner push 可**。
- **要オーナー対応**: `git push`(エージェントは実施しない)。

---

# タスク: スマホLibraryの2列コンタクトシート化 (Driver: Claude Code / Fable5)

- **目的**: スマホadminで写真が1枚ずつしか見えない(thumbSize 220px直参照で
  375/390px幅のLibraryが1列)問題の解消。Codex経由のオーナー最優先要望。
- **方針**: 画面幅ではなく実測grid幅(libraryGridMetrics.gridWidth)を基準に、
  2列を割る時だけ「2列に収まる実効幅」へ縮める純関数
  `effectiveLibraryThumbSize` を導入。PCのslider/保存値は不変。
  grid CSS・仮想化・キーボード列数・表示ゲートの参照を実効幅に統一。
- **迷った点**: gridColsの旧式 `floor(w/(thumbSize+3))` はgap8pxと不一致の
  近似だった。列数解釈のずれは矢印移動を壊すため、auto-fill minmaxと同式
  `floor((w+gap)/(size+gap))` へ修正(通常サイズ帯では結果同一)。
- **Codexレビュー P1対応**: pointer:coarseはadmin-tap-smを40px角へ拡大
  (styles.css)するため、⇤前次⇥4個+gap12px=172pxが2列カード(167px)から
  はみ出す。実寸根拠の定数(coarse=180/fine=120)で先頭/末尾ジャンプを
  出し分ける `showLibraryJumpButtons` を追加。前/次で並び替え機能は維持、
  PCの4ボタンは不変。
- **下限の判断**: 2列化で120pxを割る極端な狭さでは1列を維持
  (LIBRARY_MIN_EFFECTIVE_THUMB=120。ボタン・バッジが物理的に収まらないため)。
- **検証**:
  - 純関数テスト8件(375→167px/390→175pxで2列、巨大化なし、下限、coarse境界)。
  - 新mobile smoke(hasTouch=pointer:coarse再現): 375x667/390x844で2列以上・
    横はみ出し0・ボタンのカード内収まり・ジャンプ非表示・タップ→Inspector
    開閉(非書き込み)。desktopはminmax(220px)+slider表示の不変を固定。
  - `bun run check`: 成功(334 tests / 0 fail)。
  - `bun run smoke`: 26 passed / 1 failed(既知のadmin-trash-signalのみ)。
- **要オーナー対応**: push(Codexがオーナー明示許可で実施と連絡あり。
  Claudeはautonomy-rulesに従い実施しない)。本番反映後にスマホ実機で
  admin Libraryの2列表示を確認。
