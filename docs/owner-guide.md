# 秋さん向け・AI運用手引き

作成: 2026-07-08。これ1枚で「AIへの頼み方 → 報告の読み方 → 公開（push）→ 困った時」が回るようにした短い手引きです。詳しいルールは各リンク先にあります（読まなくても運用できます）。

---

## 1. セッションの始め方

1. `docs/agents/task-queue.md` を開く（Sonnet 用の指示書が並んでいます）。
2. やりたいタスクの指示書を **1枚だけ** 丸ごとコピーして、Claude Code に貼る。
3. 指示書の先頭のタグを見る:
   - **[夜間ラン可]** … 貼って寝てOK。朝に報告を読む
   - **[昼推奨]** … 動かして構わないが、様子を見られる時間帯に
   - **[要オーナー判断]** … AIが途中で「案A/案B」を出して止まります。返事が必要
   - **[Codexレビュー必須]** … 完了後に「Codex にレビューを頼んで」と一言足す

キューに無い依頼をするときは、やってほしいこと+「docs/agents/autonomy-rules.md に従って」とだけ書けば大丈夫です。

## 2. 完了報告で見るべき3点

AIの報告が長くても、この3つだけ確認すれば十分です。

1. **`bun run check` は成功したか**（コードの健康診断。「273 tests / 成功」のような行）。admin を触ったなら **`bun run smoke` も成功したか**（実際のブラウザで管理画面を動かす検査）。
2. **「push したか」= していない、になっているか**。push（公開）はいつも秋さんの手で行います。AIが push した報告があれば異常です。
3. **「要相談」「オーナー判断待ち」の項目**。ここだけは秋さんの返事がないと先へ進みません。選択肢2〜3個+おすすめ付きで書かれているはずです。

## 3. push（公開）の儀式と本番確認

push = 本番サイトの更新です。AIの報告で check/smoke 成功を確認してから:

```sh
git log origin/main..main --oneline   # これから公開される変更の一覧を見る
git push                              # 公開（Railway が自動でビルド・反映）
```

push 後、数分待ってから:

1. https://akieguchi.com/ を開き、トップ・ギャラリー・管理ログイン画面が普通に表示されるか見る。
2. 報告の「push後の確認推奨」に書かれた箇所を見る（そのタスクで変わった場所です）。
3. 表示がおかしい時は慌てずスクショを撮り、次のセッションで AI にそのまま貼る。

## 4. 困った時

| 状況                                                       | 対応                                                                                                                                     |
| ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| AIが「三振（同じ失敗3回）で中断」と報告                    | 正常な動きです。その項目は「要相談」リストに入っています。急ぎでなければ放置してよく、急ぎなら「Codex に相談して」と次のセッションで指示 |
| 「要相談」が溜まってきた                                   | 週1回くらい、要相談リストをまとめて眺めて「やる/やらない/後で」を返事すれば十分                                                          |
| AIの報告が理解できない                                     | 「専門用語なしで3行で言い直して」と頼む。それでも不明なら放置してOK（push しなければ本番は変わりません）                                 |
| 本番の表示が壊れた                                         | まず `git log -5 --oneline` を AI に見せて「直前の push が原因か調べて」と指示。原因調査は読み取りだけなので安全                         |
| AIが妙なこと（大量削除・push・設定変更）をしようとしている | 「止めて」と打てば止まります。その内容を task-queue の [要オーナー判断] として書き直させる                                               |

## 5. やってはいけないこと（秋さん側）

- check/smoke 成功の報告を確認せずに push しない
- 「要相談」を承認するとき、**復元できない操作**（完全削除・上書き）は一呼吸置く。「これは元に戻せる?」と AI に聞いてから
- リポジトリのルート直下に .md ファイルを増やさない（AI が置いたら指摘してOK。文書は docs/ 配下がルール）
- `.env`（パスワード類）の中身を AI のチャットに貼らない

## 6. もっと詳しく

- AIの自律ルール（判断表・禁止事項）: `docs/agents/autonomy-rules.md`
- タスクの指示書キュー: `docs/agents/task-queue.md`
- 高リスク作業の検査表: `docs/checklists.md`
- 直近の総点検結果（何が健全で何が課題か）: `docs/specs/audit-2026-07.md`
- AIどうしの役割分担（Sonnet=実装 / Codex=レビュー専任）: `AGENTS.md`「役割分担」
