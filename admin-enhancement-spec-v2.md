# 管理画面 強化仕様書 v2（確定版・写真管理・編集UI刷新）

> AIコーディングエージェント（Claude Code）向け実装仕様。
> 既存の admin-enhancement-spec.md（P1〜P4）の続編。§0 の既存パターンは引き続き厳守。
> 本仕様は全項目確定済み。記載通りに実装してよい。判断が必要な細部は各項目の注記に従う。

---

## 0. 前提（admin-enhancement-spec.md §0 を継承）

- DBクエリは必ず `withRetry(() => db....)` でラップ
- データ更新後は `qc.invalidateQueries`
- 新規 settingsキー追加時は3箇所セットで更新（admin.tsx previewPayload / provider.tsx DB適用useEffect / provider.tsx handlePreviewMessage）
- スキーマ変更時は `cd packages/web && bun run db:push`（ecosystem に自動実行は組込済み）
- 型付きAPIクライアント（lib/api.ts）経由。fetch直叩き禁止

---

## グループC: 写真管理を Lightroom / Bridge に近づける

### C1. 重複アップロード検知【確定】

**目的**: 同じ写真の二重登録を防ぐ。Lightroom の「読み込み済みの疑いがある写真」と同等の体験。

**方式**: SHA-256 コンテンツハッシュ。ファイル名が違っても中身が同じなら検知できる。

**スキーマ変更**:
```ts
// photos テーブルに追加
fileHash: text("file_hash"),  // nullable（既存写真は次回バックフィルまで null）
```

**実装**:
- アップロード時、クライアントで各ファイルの SHA-256 を計算（`crypto.subtle.digest`）。
- `POST /admin/upload` にハッシュを渡す、またはサーバ側で sharp 処理前のバッファから算出。
- 既存 `photos.fileHash` と照合。一致したら**その1枚だけスキップ**し、残りは通常通り登録。
- 完了トーストに「重複スキップ: N枚（ファイル名）」を表示。既存の結果トースト（成功/失敗/スキップ）に統合。
- 既存写真の `fileHash` バックフィル: 起動時 or 管理画面のボタンで R2 から読んで一括計算（B3のwidth/heightバックフィルと同じ要領。任意実行でよい）。

**受け入れ基準**: 同一画像を再アップロード→「重複スキップ: 1枚」と表示され登録されない。中身が同じでファイル名違いも検知される。

**判定方式（確定）**: 中身ハッシュ（SHA-256）一本。ファイル名一致は見ない。名前が違っても中身が同じなら重複として扱う。

### C2. グリッドの範囲選択【確定】

**現状**: Cmd+A 全選択、個別クリック選択、一括カテゴリ変更は実装済み。
**追加**: **Shift+クリックで連続範囲選択**（Bridge/Finder と同じ）。直前に選択した写真から Shift+クリックした写真までを一括選択。
- 一括操作に「一括削除」も追加（論理削除なのでゴミ箱から復元可能＝安全）。

### C3. グリッドのキーボードナビゲーション【確定】

**目的**: マウスを使わず写真を見て回れる（Bridge感）。
- 矢印キー（←→↑↓）でグリッド内の選択写真を移動。
- Enter で選択中の写真のインスペクタを開く。
- Space でクイックプレビュー（大きく表示。Lightbox流用でよい）。
- 既存の `?` キーのショートカット一覧モーダルに、これらを追記。

クイックプレビュー（Space）も含めて実装する。

### C4. ドラッグ&ドロップでアップロード【確定】

**現状確認が必要**: 今アップロードがファイル選択ダイアログのみなら、グリッド領域への**ドラッグ&ドロップ投入**を追加。Lightroom/Finder からそのまま放り込める。
- ドロップ領域を視覚的に明示（点線枠＋「ここにドロップ」表示）。
- 既存の並列アップロード（3件）・重複検知（C1）・失敗リトライをそのまま通す。

### C5. メタ情報の表示（フィルムらしさ）【確定】

撮影情報をグリッドやLightboxで表示する。項目は D3 で確定した構造化フィールド（カメラ / レンズ / フィルムorデジタル）を使う。
- width/height は保存済み、EXIF撮影日の初期補完も実装済み。
- フィルム・マニュアルレンズはEXIFが取れないことが多い → 手入力前提（取れたら初期値に入れる）。
- 公開側（Lightbox等）での表示は、値が入っている項目だけ控えめに出す（未入力項目は非表示）。

---

## グループD: 編集UIの刷新（Settings / インスペクタ）

### D1. Settings 全セクション デフォルト折りたたみ【確定】

**問題**: Settingsを開くと複数セクションが最初から開いていて圧迫感がある。目的の項目に辿り着きにくい。
**仕様**:
- 全セクションを**デフォルト折りたたみ**にする。見出しだけ縦に並び、クリックで開く。Lightroom の右パネル（各モジュール折りたたみ式）と同じ。
- 既定で開くセクションは**設けない**（すべて折りたたみで開始）。
- 開閉状態は React state で保持（リロードで全閉じに戻ってよい）。

### D2. 「Labels / Text」セクションの整理【確定】

**正体**: このセクションは「サイトに一度だけ出る固定文言」の編集。中身は:
- `filterAllLabel` — ギャラリーのカテゴリフィルタ「All」ボタンの表示名
- `contactLabel` — コンタクトページのメール直リンクのラベル
- `Footer Text` — フッター文言
- `Hero Subtitle` — トップのヒーロー下のサブテキスト

**仕様**:
- セクション名を「Labels / Text」→「**サイト文言**」に変更（中身が一目で分かる名前に）。
- D1により**デフォルト折りたたみ**（普段触らないため）。
- **各項目に「どこに表示されるか」の補足を1行添える**。例:
  - filterAllLabel → 「ギャラリー上部のフィルタ最左ボタン」
  - contactLabel → 「お問い合わせページのメールリンク文言」
  - Footer Text → 「全ページ下部のフッター」
  - Hero Subtitle → 「トップの作家名の下に表示」
- これが秋の「いつ使うの?」を解消する核心。

### D3. インスペクタ（写真ごとの編集）項目の整理【一部確定・一部相談】

**現状の項目**: Title / Meta / Description / Category / Display Size(S/M/L)

**【確定】Meta を構造化フィールドに分割**:
従来の自由記述 `Meta` を廃止し、以下の構造化フィールドに置き換える。
- `camera` — カメラ（例: PENTAX 67）
- `lens` — レンズ（例: SMC Takumar 105mm f/2.4）
- `filmType` — 「フィルム」/「デジタル」の2択（銘柄は持たない）

実装詳細:
- スキーマ: `photos` に `camera: text`、`lens: text`、`filmType: text`（いずれもnullable）を追加。旧 `meta` 列は当面残してよい（移行中の保険。新規UIからは触らない）。
- EXIF が取れる場合は camera/lens の初期値に補完。取れなければ空（フィルム・マニュアルレンズ前提）。
- すべて任意入力。必須にしない。

**【確定】入力値のプリセット保存（Lightroom のメタデータプリセット相当）**:
- 一度入力した camera / lens の値を保存し、次回から候補として選べるようにする。毎回打たなくて済む。
- 実装: `site_settings` に `metaPresetsCamera` / `metaPresetsLens` を JSON配列で保存（例: `["PENTAX 67","Leica M6"]`）。新しい値を保存するとリストに追加。入力欄は datalist 等でサジェスト＋直接入力も可。
- filmType は2択固定なのでプリセット不要。

**【確定】各項目に「どこに表示されるか」を補足表示**:
- Title → 「Lightbox・SEO・alt に使用」
- camera/lens/filmType → 「Lightbox の撮影情報として表示」
- Category → 「ギャラリーのフィルタ分類」
- Display Size → 「ギャラリーグリッドでの表示サイズ」
- Description → 「Lightbox の写真説明」

**Description の扱い（確定）**: 残す。使わない写真は空のままでよく、公開側は値があるときだけ表示する。

### D4. Typography の棚卸し【要・秋の仕分け】

**問題**: 項目が多い割に欲しいものが無い。
**やり方**: 秋が「実際に変えたことある項目／一度も触ってない項目」を仕分ける。それを元に:
- 触ってない項目 → 折りたたみの奥（「詳細設定」サブセクション）へ移動。表からは隠す。
- よく使う項目 → 各要素グループの先頭に。

**【確定】グルーピングは「調整軸別」の2階層に再設計**:
従来の要素別（Hero / Nav / Body…）をやめ、**まず調整軸を選び → その中で対象（ページ・文字種）を選ぶ**構成にする。

```
Typography
├─ 大きさ
│   ├─ ヒーロー名 / サブタイトル
│   ├─ ナビゲーション
│   ├─ セクション見出し
│   ├─ 本文
│   └─ フッター
├─ 色
│   ├─ ヒーロー名 / サブタイトル
│   ├─ ナビゲーション
│   └─ （以下同様）
└─ 間隔（字間・行間）
    ├─ ヒーロー名
    ├─ 本文（字間・行間）
    └─ （以下同様）
```

- 各軸セクションは D1 同様に折りたたみ。
- 既存のCSS変数・settingsキーはそのまま流用し、**UIの並べ替えのみ**（データ構造は変えない）。
- 「変えたい内容（大きさ/色/間隔）」から入って、次にどの要素か、の導線にする。

**【確定】未使用項目の整理**: 秋が普段触らない細かい項目（極端に使用頻度の低い不透明度系など）は、各軸の末尾に「詳細設定」サブ折りたたみとしてまとめ、表からは隠す。どれを隠すかは実装時に秋へ確認（初期は全項目表示でよい。運用後に仕分け）。

**【確定】足りていない項目を新規追加**:
- **基準フォントサイズの一括スケール** — 個別に弄らず全体の文字を少し大きく/小さく。新規キー `globalFontScale`（範囲 0.85〜1.15、既定 1.0）。`@media` のモバイル縮小率と併用可。
- **リンクのホバー色・下線の有無** — ナビ/本文中リンクの所作。新規キー `linkHoverColor`、`linkUnderline`（on/off）。

### D5. プレビューのPC/スマホ幅 切替【確定】

ライブプレビュー（iframe）は実装済み。**プレビュー上部に「PC幅 / スマホ幅」トグル**を追加。iframe の幅を切り替えてレスポンシブの最終確認が管理画面内で完結する。

---

## グループE: 閲覧側のデザイン（見る人の体験）

### E1. ファーストビュー（ヒーロー）の表示モード切替【確定】

初手のインパクトを管理画面から調整できるようにする。

**【確定】表示モードを選択式にする**:
- 新規 settingsキー `heroMode`: `"carousel"`（複数枚を順に表示・現状）/ `"single"`（1枚絵を大きく見せる）の2択。
- single の場合、表示する1枚は hero_photos の先頭（または別途指定）。スクロールで作品へ誘導。

**【確定】ヒーローの高さを調整可能にする**:
- 新規 settingsキー `heroHeight`: ビューポート高に対する割合（例 60〜100vh、既定 100vh）。スライダーで調整。
- モバイルでは別途上限をかけてよい（極端に高すぎないように）。

**【確定】文字の可読性**: 作家名と写真のコントラストを確保。明るい写真で白文字が飛ぶ場合に備え、ヒーロー画像に薄いグラデーションオーバーレイ（下方向に暗く）をかけるオプション、または文字に控えめなテキストシャドウ。`heroOverlay`（on/off, 既定on）で制御。

これらは Hero タブ または Settings に追加し、ライブプレビューに反映させる。

### E2. 余白と「間」の設計【確定】

高級感・作家性は余白で決まる。
- ギャラリーグリッドの写真間隔、セクション間の縦余白を**意図的に広めに**する。
- 情報密度を下げ、1枚1枚が呼吸する「作品集」の見え方に寄せる。
- 具体値は実装時に現状を見て調整（現状が詰まっていれば 1.5〜2倍程度の余白に）。

### E3. トランジションの質【確定】

ページ遷移・写真フェードインのイージングを `cubic-bezier(0.16, 1, 0.3, 1)`（ease-out-expo系）に統一、duration を 500〜700ms に。ゆっくりした動きで品を出す。現状が素早いままなら調整。

### E4. srcset で解像度出し分け【確定】

画像配信APIは任意幅対応（〜2400px）。`<img>` に srcset/sizes を付け、大画面は高解像度・モバイルは軽量に。
```tsx
srcSet={`${url}?w=400&q=80 400w, ${url}?w=800&q=80 800w, ${url}?w=1200&q=80 1200w`}
sizes="(max-width: 768px) 50vw, 33vw"
```
高解像度ディスプレイでの第一印象が大きく変わる。

### E5. About / Profile の作り込み【今回は土台のみ】

作家性が最も出る場所だが、**今回は中身（文章・選定）には踏み込まず、レイアウトの受け皿だけ整える**。
- ステートメント、使用機材、フィルムへのこだわりを置ける**セクション構造**を用意（空でも崩れないように）。
- 実際に何を書くか・どの写真を載せるかは別途相談（秋の文章が必要なため後日）。

### E6. 販売・依頼の導線【今回は土台のみ】

- 過去にLightbox内の「撮影を依頼する」CTAは削除済み（押し付けがましかった）。
- 今回は**導線を置ける場所だけ用意**（Contact / フッター等）。実際に出すか・価格表示・EC化などの方針は後日相談。
- 現時点で新たなCTAを目立つ位置に追加はしない。

---

## グループF: 発見されやすさ（SEO・シェア）【確定・低優先】

- 構造化データ（JSON-LD: Person / ImageGallery）を追加。
- sitemap.xml を生成。
- 画像 alt の充実（Title→meta→filename フォールバックは実装済み）。
- OGP動的注入は実装済み。

---

## 実装フェーズ（推奨着手順）

| フェーズ | 項目 | テーマ |
|---|---|---|
| **Q1** | C1 重複検知 / D1 折りたたみ / D2 サイト文言整理 | 最優先・体験への影響大 |
| **Q2** | D3 インスペクタ整理 / D4 Typography棚卸し / D5 プレビュー切替 | 編集UIの完成度 |
| **Q3** | C2 範囲選択 / C3 キーボード / C4 D&D | 写真管理の快適さ |
| **Q4** | E2 余白 / E3 トランジション / E4 srcset | 閲覧側の質 |
| **Q5** | E1 ヒーロー / E5 About / E6 導線 / F SEO | 仕上げ・要相談多め |

---

## データモデル追加まとめ（確定）

### schema.ts（photos テーブル）
```ts
fileHash: text("file_hash"),   // C1 重複検知（SHA-256, nullable）
camera: text("camera"),        // D3 撮影情報（nullable）
lens: text("lens"),            // D3 撮影情報（nullable）
filmType: text("film_type"),   // D3 "フィルム"|"デジタル"（nullable）
// 旧 meta 列は当面残す（移行中の保険・新UIからは触らない）
```
要マイグレーション（`cd packages/web && bun run db:push`）。

### siteSettings 追加キー（すべて string / JSON文字列）
```
# D3 メタ値プリセット
metaPresetsCamera   # JSON配列 例: ["PENTAX 67","Leica M6"]
metaPresetsLens     # JSON配列
# D4 Typography 追加
globalFontScale     # 0.85〜1.15（既定 1.0）
linkHoverColor      # 色
linkUnderline       # "on"|"off"
# E1 ヒーロー
heroMode            # "carousel"|"single"（既定 carousel）
heroHeight          # vh割合（既定 100）
heroOverlay         # "on"|"off"（既定 on）
```
※ 新規キーは §0 のルール通り3箇所（previewPayload / DB適用useEffect / handlePreviewMessage）に必ず追加。

---

## 受け入れ基準（全体）

- [ ] 同一画像の再アップロードが重複として弾かれ、件数が通知される（ハッシュ判定）
- [ ] Settings の全セクションがデフォルト折りたたみ（既定で開くものは無し）
- [ ] 「Labels / Text」が「サイト文言」に改名され、各項目に表示場所の補足がある
- [ ] インスペクタの撮影情報が camera / lens / filmType の構造化フィールドになっている
- [ ] camera / lens は過去入力値をサジェストから選べる
- [ ] 各編集項目に「どこに表示されるか」の補足がある
- [ ] Typography が「大きさ / 色 / 間隔」の軸別2階層になっている
- [ ] 全体フォントスケール・リンクのホバー色/下線を設定できる
- [ ] ヒーローのモード（カルーセル/1枚絵）・高さ・オーバーレイを設定できる
- [ ] プレビューでPC幅/スマホ幅を切り替えられる
- [ ] グリッドで Shift+クリック範囲選択・矢印キー移動・Space プレビューができる
- [ ] グリッドにドラッグ&ドロップで写真を追加できる
- [ ] 主要画像が srcset で解像度出し分けされる
- [ ] About / 販売導線は受け皿のみ用意され、中身（文章・新規CTA）は追加していない
- [ ] 既存の §0 ルール（withRetry / 設定キー3箇所同期）に準拠している
