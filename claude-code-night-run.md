# Claude Code 夜間自走指示（6/18 3:10am〜8:10am）

## コンテキスト
- プロジェクト: `~/eguchi-portfolio-app`
- 本番: akieguchi.com（Railway、git pushで自動デプロイ）
- DB: Turso / ストレージ: Cloudflare R2 / 分析: GA4
- スタック: Hono + React 19 + Drizzle/Turso + Bun + Tailwind CSS 4
- 管理画面: /admin（パスワード: autumn00180）
- CLAUDE.mdを最初に読んでプロジェクト構造を把握すること

## 基本ルール
- **自走モード**: 確認なしで進めてOK。判断に迷ったらスキップして次へ
- **デプロイ**: 各フェーズの終わりにまとめて `git add . && git commit && git push`
- **破壊的変更禁止**: DB スキーマ変更・R2 バケット操作・環境変数の変更はしない
- **既存機能を壊さない**: 変更前に `bun run build` が通ることを確認、変更後も必ず確認
- **ログを残す**: 各タスク完了時に `NIGHT-RUN-LOG.md` に何をしたか・結果を追記
- **時間配分**: 5時間。1つのタスクに30分以上ハマったらスキップして次へ

---

## フェーズ1: ヘルスチェック（〜30分）

まず全体の健康状態を確認する。

1. `bun install` → 依存関係の整合性確認
2. `tsc -b` → TypeScriptエラーを全て洗い出す
3. `bun run build` → ビルドが通るか確認
4. `bun run dev` で起動 → 主要ページをcurlで叩いて200が返るか確認:
   - `/` (トップ)
   - `/about` 
   - `/series` (シリーズ一覧)
   - `/contact`
   - `/admin` (管理画面)
   - `/sitemap.xml`
   - `/robots.txt`
   - `/api/notes` (note RSS)
5. コンソールに出るwarning/errorを全て記録

**→ 見つかった問題は全てNIGHT-RUN-LOG.mdに記録。クリティカルなものはこのフェーズで修正。**

---

## フェーズ2: TypeScript / コード品質（〜60分）

6. tsc -b で出たエラーを全て修正
7. 未使用import・未使用変数の削除
8. any型をできる限り適切な型に置換
9. React コンポーネントのprops型が正しく定義されているか確認
10. 非同期処理のエラーハンドリング漏れを修正（try-catch / .catch()）
11. console.log のデバッグ出力が本番コードに残っていたら削除

**→ 修正のたびに `bun run build` で壊れてないか確認**

---

## フェーズ3: SEO / メタデータ強化（〜60分）

12. 全ページの `<title>` タグが適切か確認・改善
    - トップ: 「江口秋 | Aki Eguchi Photography」的な
    - シリーズ個別: シリーズ名が入っているか
    - About / Contact も適切か
13. OGPタグ（og:title, og:description, og:image）が全ページで正しく出力されているか確認
14. JSON-LD（構造化データ）の内容を確認・改善
    - Photographer / ImageGallery / WebSite スキーマが適切か
15. `<img>` タグの `alt` 属性が空のものを洗い出す
    - DB内の写真データにtitleがあればそれを使う
    - なければ「シリーズ名 - 写真N」のような汎用altを入れる
16. sitemap.xml に全公開ページ・画像が含まれているか確認
17. robots.txt が適切か確認（/admin はdisallowされているか等）
18. canonical URL が全ページで正しいか確認

---

## フェーズ4: パフォーマンス改善（〜45分）

19. 画像の lazy loading が適切に設定されているか（ファーストビュー以外はlazy）
20. CSSの不要なスタイル / 重複をチェック
21. JavaScript バンドルサイズの確認（vite buildの出力を見る）
    - 大きすぎるチャンクがあれば dynamic import で分割検討
22. フォントの読み込み最適化（preload / font-display: swap）
23. Lightbox コンポーネントの画像プリロード戦略を確認

---

## フェーズ5: UI / UX 微調整（〜60分）

24. モバイル表示の確認（レスポンシブ）
    - ギャラリーの各レイアウトがモバイルで崩れてないか
    - ナビゲーションのハンバーガーメニュー動作
    - Lightbox のスワイプ・ピンチズーム
    - 文字サイズ・余白がモバイルで適切か
25. ダークモード対応状況の確認（対応している場合のみ）
26. アクセシビリティ
    - キーボードナビゲーション（Tab / Enter / Escape）
    - Lightbox のEscキーで閉じる・矢印キーで移動
    - フォーカス管理（モーダル開閉時）
    - color contrast（テキストと背景のコントラスト比）
27. 404ページが存在するか・適切なデザインか
28. ローディング状態の表示（画像読み込み中のプレースホルダー等）

---

## フェーズ6: 管理画面の改善（〜45分）

29. 管理画面の各機能が正常に動作するか一通り確認
    - 写真一覧表示
    - シリーズ管理
    - 設定画面
    - バッチ操作
30. 管理画面のUIで使いにくい部分があれば改善
    - ボタンの視認性
    - フォームのバリデーション表示
    - 操作後のフィードバック（トースト等）
31. 管理画面のエラーハンドリング（API失敗時の表示）

---

## フェーズ7: コード整理 / DX改善（残り時間）

32. ファイル・フォルダ構成の整理（不要ファイルの削除）
33. 環境変数の使用箇所を確認（ハードコードされた値がないか）
34. README.md の更新（セットアップ手順・構成図が最新か）
35. CLAUDE.md の更新（今夜の変更を反映）
36. コメントの追加（複雑なロジックに説明を）

---

## 最終: デプロイ & レポート

37. 全変更を `bun run build` で最終確認
38. `git add . && git commit -m "夜間自走: [主な変更内容]" && git push`
39. NIGHT-RUN-LOG.md に最終サマリーを書く:
    - 修正した問題の一覧
    - 改善した項目の一覧
    - スキップした項目とその理由
    - 次回やるべきこと（発見した課題）

---

## 禁止事項（再掲）
- ❌ DBスキーマの変更（Drizzleマイグレーション）
- ❌ R2バケットの操作（写真の削除・移動）
- ❌ 環境変数の追加・変更
- ❌ package.jsonの依存関係の大幅な変更（メジャーバージョンアップ等）
- ❌ ルーティング構造の大幅な変更
- ❌ デザインの大幅な方向転換（フォント・カラー・レイアウトの根本変更）
- ❌ 新機能の追加（既存機能の改善のみ）
- ❌ 30分以上同じ問題にハマること（スキップして次へ）

## 許可事項
- ✅ TypeScriptエラーの修正
- ✅ SEO改善（meta・alt・sitemap・JSON-LD）
- ✅ パフォーマンス改善（lazy loading・バンドル最適化）
- ✅ UI/UXの微調整（レスポンシブ・アクセシビリティ）
- ✅ コード品質の向上（型・エラーハンドリング・不要コード削除）
- ✅ 管理画面のバグ修正・UX改善
- ✅ ドキュメント更新
