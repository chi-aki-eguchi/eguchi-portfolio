{
  "name": "sandbox-app-template",
  "private": true,
  "packageManager": "bun@1.3.5",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/web && bunx vite",
    "start": "node scripts/start-pm2.mjs",
    "deploy:runable:legacy": "bash docs/archive/deploy.sh",
    "build": "turbo build",
    "build:web": "cd packages/web && bunx vite build",
    "lint": "oxlint . --deny-warnings --no-error-on-unmatched-pattern",
    "lint:fix": "oxlint . --fix --deny-warnings --no-error-on-unmatched-pattern",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:tools": "node --test .claude/hooks/credit-status.test.mjs scripts/ai/handoff-tools.test.mjs scripts/ai/check-wiki-freshness.test.mjs scripts/ai/vault-context.test.mjs scripts/start-pm2.test.mjs",
    "check:postgres-schema": "bun packages/web/scripts/check-postgres-schema.ts",
    "check:wiki-freshness": "node scripts/ai/check-wiki-freshness.mjs",
    "check": "bun run check:postgres-schema && bun run check:migrations && bun run typecheck && bun run lint && bun run test && bun run test:tools && bun run build && bun run check:wiki-freshness",
    "smoke": "playwright test --config scripts/smoke/playwright.config.ts",
    "db:generate": "turbo db:generate",
    "db:migrate": "turbo db:migrate",
    "db:push": "turbo db:push",
    "db:studio": "turbo db:studio",
    "kill:port": "lsof -t -i :${PORT:-4200} | xargs kill -9",
    "check:migrations": "bun packages/web/scripts/check-migrations.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@tursodatabase/api": "^1.9.2",
    "@types/bun": "latest",
    "@types/sharp": "^0.32.0",
    "oxlint": "^1.66.0",
    "playwright": "^1.61.0",
    "pngjs": "^7.0.0",
    "turbo": "^2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1023.0",
    "@libsql/client": "^0.17.2",
    "cloudflare": "^5.2.0",
    "detect-libc": "^2.1.2",
    "js-yaml": "^5.2.1",
    "pm2": "^7.0.1",
    "sharp": "^0.34.5",
    "ws": "^8.21.0"
  }
}
