# 決定ログ 2026-07-08 — Claude Code (Fable5)

セッション: ①訪問者の動線設計 → ②管理画面の操作フィードバック改善(直列)

## Part 0: ギャラリーホバー演出の確定(保留項目1の決着)

- **タスク名**: ギャラリーホバー A/B/C の確定
- **選んだ方針**: 案C(動きなし・影だけ深くなる)を採用。
  `.photo-card:hover` の浮き(-4px)と、写真の拡大(1.03倍)+明度補正(+4%)を削除。
  影の深化(`--shadow-photo-hover`)のみ残した。
- **迷った点と理由**: オーナーは3案の差を体感できなかったため、指示どおり
  「最も実装が単純で保守しやすい案」で判断した。
  - 案A(現状維持)は編集作業ゼロだが、ルールが2箇所(カードのtransform+imgのscale/filter)に
    分かれ、LQIPのブラー解除フィルタと transition-duration を共有する繊細な作りで保守コストが高い。
  - 案Bも img 側の scale ルールが残るため構造は案Aと同じ。
  - 案Cは transform・filter への関与が完全に消え、hover は box-shadow 1プロパティだけになる。
    写真の色・大きさを一切変えないため写真家的な懸念(明度が変わる)も同時に解消。
  - 付随して不要になった打ち消しルール(`.photo-card-quiet:hover` と
    `.collage-card .photo-card:hover` の `transform: none`)も削除し、コメントの古い記述を更新。
- **触らなかったもの**: collage レイアウトの額縁ごと動く hover(-3px + 1.015倍)は
  別の意図的デザイン(額と写真が一体で動く)なので維持。tile-caption のフェードも
  「演出A/B/C」の対象外(タイトル表示の機能)なので維持。
- **要相談**: なし
- **bun run check**: 成功(tsc -b / lint / test / build)

## Part 1 Phase 1: 訪問者動線の監査

- **タスク名**: 全公開ページの動線監査(3ペルソナ・スクショ付き)
- **選んだ方針**: ローカル dev(本番と同一DB・読み取りのみ)を Playwright で
  375px/1440px 巡回し、リンク棚卸し JSON+スクショ+レポートを
  `scratch/journey-audit-2026-07/` に保存。本番は公開ページ2枚のみ視覚確認。
- **最重要発見**: **公開写真が0枚**(gallery / series 2件 / hero ローテ全て空)。
  検索・SNSどこから来ても作品を1枚も見られない。意図的な準備中か公開し忘れかは
  本番DB不可触ルールのため未確認 → 要オーナー確認(P0)。
- **他の主要発見**:
  - Contact は素のフォームのみ。設定済み contactIntro がフォーム表示時に出ない
    コード構造(死に設定)。料金プラン機能は実装済みだが0件登録で非表示。
  - /service は価格+Stripe決済リンク付き販売ページとして既に本番公開中
    (タスク指示の「未商品化・気配段階」と食い違い)→ 要相談。
  - 直接DBクエリ(写真の非公開分布の確認)は本番資格情報のため権限却下 →
    公開APIレベルの事実のみで監査を成立させた。
- **bun run check**: 対象外(コード変更なし)

## Part 1 Phase 2: 改善提案(承認待ちで停止)

- 提案書: `scratch/journey-audit-2026-07/proposals.md`(文言はトーン違い2〜3案付き)、
  モック: `shots/proposal-contact-m.png`(実ページにDOM注入して撮影・コード変更なし)。
- 承認必須(🛑)として保留: フォーム添え書き/依頼の流れ/placeholder/送信後文言/
  about→gallery導線/Lightboxシリーズリンク/serviceの扱い/需要測定。
- **例外として実施した1件(1-a)**: contactIntro がフォーム表示時に描画されない問題の修正。
  - 迷った点: 「新セクション追加は承認必須」に当たるか。オーナー自身が管理画面で
    設定済みの文言が表示されないのは設定機能のバグであり、新規文言ゼロ・
    1条件ブロックの追加のみで可逆、と判断して実施(autonomy-rules §3「壊さない方」)。
    成功画面では非表示にして送信後の静けさを維持。
  - 検証: bun run check 成功、実ブラウザ375pxで表示確認
    (`shots/after-contact-intro-m.png`)。
