# Task Log

<!-- CURRENT_STATE_START -->
## Current State — 2026-09-25 JST / 動き（モーション）とUXの強化（試作、オーナー確認待ち）

- 依頼: 「モーションデザイン、UI、UXを強化したい」。静けさを保ち、写真を見る体験に意味のある動きだけを足す。**main へは未 push。ブランチ `claude/akieguchi-motion-design-s9odvq` で目視確認を待つ。** 判断は `docs/specs/motion-2026-09-25.md`。
- 写真集（book）: 頁が画面に入ると写真が「現像」される／トップの扉の名前が一字ずつ組み上がる（その訪問で1度）／頁の移り変わりは持ち上げずにその場で濃く。全デザイン共通: ビューアが押した写真そのものから開き、閉じると元の場所へ戻る。動きを減らす設定では移動を使わず、濃淡は残す。
- 既存の不具合も修正: 写真集でビューアを前後へ送ってから閉じると、開いた位置へ戻されていた。
- 2026-09-25 検証（この環境）: typecheck・lint・build 成功。単体テストは変更前後とも同じ約26〜30件が失敗（contact-reference.ts の正規表現が Bun 1.3.5/1.3.11 の両方で SyntaxError、ほか実行ごとに揺れる render テスト）。変更前の main でも同じく失敗するので今回の差分とは無関係、未解決。smoke の結果は下に追記。

## Current State — 2026-09-23 JST / 写真集の管理画面「作品ごとの作業台」（オーナー承認、main へ push）

- オーナーが管理画面の3案から A を選択。見た目と使い心地も同時に作り直す。**pushはオーナーの目視確認まで待つ。**
- `siteDesign = "book"` のときだけ作業台の器（作品／写真の一覧／サイト）。いつもの構成（配布版の既定）は今までの管理画面のまま。判断は `docs/specs/admin-workbench-2026-09-23.md`。
- 検証（2026-09-23）: `bun run check` 成功（製品1483件）、全体 smoke 579成功・失敗0。手元の見本 API（本番は GET のみ）でドラッグの順番・⌘Z・非公開と元に戻す・取り込みを実ブラウザで確認。いつもの構成の管理画面は変更前と同じ。
- 追加（オーナーの6点）: 何番目へ・作品の並べ替え・取り込み媒体の自動振り分け・暗い表示・右の欄の文字・まとめて選ぶ／探す。2000枚の見本で計測（PC: 500枚の作品のコマ 0.7秒・写真まで 1.3秒、200枚まとめて操作 0.2〜0.3秒。スマホ CPU 1/4: 1.2秒・1.8秒、公開目次 1.2秒）。
- 2026-09-23 検証: `bun run check` 成功（1485件）。全体 smoke 579成功・180スキップ・失敗0（別セッションの smoke が終わるのを待ってから実行）。
- 見た目を写真集にするのはオーナーが管理画面で保存したとき（コードは既定 classic のまま）。

## Current State — 2026-09-23 JST / 公開サイトの骨格「写真集」（オーナー承認、main へ push）

- 依頼: 「AIっぽい」公開サイトの骨格を作り直す。オーナーが3案から A「写真集をめくる」を選び、目次をベタ焼き（フィルム／デジタルは記録どおり）に、制作サービスへの入口は残す、を追加。**見た目をオーナーが確認するまで push しない。**
- 設定 `siteDesign`（既定 classic）に book を追加。トップ＝扉と章、作品ページ＝1画面1枚と頁番号 `#p-NN`、`/series`・`/work`＝全作品のベタ焼き、器＝左の余白のメニュー。判断と原因の一覧は `docs/specs/photobook-structure-2026-09-23.md`。
- 検証（2026-09-23）: `bun run check` 成功（製品1475・ツール60・番人48・build）。全体 smoke 579成功／180スキップ／失敗0。book の表示は本番の公開APIを GET だけで読む確認用サーバーで PC 1440px・スマホ 390px を確認、横はみ出し0。
- 同じブランチで smoke の起動役が Vite の終了を待たずに抜けていたのを修正（`494b3b1`）。未変更の origin/main でも `launcher.test.ts` が毎回失敗していた。
- 2026-09-23 追加修正: 写真の頁は文字なし・画面いっぱい近く・均等な縁、写真の頁ではメニューを引っ込める。トップの写真は専用設定 `bookCoverPhotoId`（管理画面で選択）。スマホは文字だけの柱と縦書きの扉。進み具合の線はオーナー判断で「なし」。
- オーナー承認のうえ main へ push。**コードを入れただけでは見た目は変わらない**（既定 classic）。本番を写真集にするのは、オーナーが管理画面「各ページの構成 → サイト全体の骨格 → 写真集」を保存したとき。戻すのは同じ所で「いつもの構成」。
- 次: 管理画面の見直し（AIっぽく見える原因と使いにくい点 → 3案 → 別ブランチで試作）。

## Current State — 2026-09-20 JST / 写真セレクト便 v1.1 の仕上げ

- 写真中心の一覧、必要時だけ開くコメント、比較候補のトレイ、拡大画面のサイドパネルと写真一覧、返送前の選択確認へ改善。作成画面も設定と写真を整理。
- 未入力・意図しない0枚回答の防止と、クリップボード不可時の手動コピーを追加。Chromium・WebKitのオフライン往復、320pxの拡大画面、12/300枚上限、原本不変を再検証。
- BOOTHへ製品版・無料版のv1.1 ZIPを別々に割り当て、商品名・説明の公開保存を確認。公開サイトも同じ製品画面へ更新する。
- ユーザー指示：SNSはまだ使用しない。投稿・個別送信なし。実注文・銀行入金・月額費用の回収は未確認。

## Current State — 2026-09-20 JST / 写真セレクト便 v1.1 の公開候補

- 商品自体に2枚比較、拡大中コメント、キーボード選択、送った閲覧HTMLからの再開を追加。旧案件JSONを保持し、Chromium・WebKitのオフライン往復、300枚上限、原本不変を検証。
- 公開済みの自作写真6枚で、登録不要の操作見本を作成。製品説明・依頼方法の記事・共有画像・無料版から購入への導線を更新。所有者サイトの既存フッターに入口を追加し、顧客サイトには表示しない。
- 紹介ページとガイドで、既存GA4の閲覧・体験・購入クリックを記録する。ソフト・写真入りデモ・購入者の閲覧HTMLには解析を入れず、写真・コメント・任意のURL値を送らない。GA集計反映と実注文は未確認。
- 検証：製品17単体テスト、両エンジンの往復、15画面幅・ページの表示、サイトの関連9テスト、本番build、フッターとContactの隔離smoke2件。新版ZIPは製品300枚・無料12枚で分離。
- この候補の公開はBOOTHのv1.1ファイル割り当てと内容一致を確認してから行う。商品編集のChrome接続が応答せず、ネイティブ操作もユーザーのタブ切替と競合したため、操作時間を確保できるか確認中。反映状態の正本はタスク成果物の「品質改善-公開状況.json」。販売・入金の実績は未確認。

## Current State — 2026-09-20 JST

### 写真セレクト便のBOOTH販売を接続

- BOOTHのメール認証・売上受取先登録を確認し、ショップと商品を一般公開。販売先は https://photo-select-bin.booth.pm/items/8872013 。匿名の閲覧で製品版3,980円・体験版0円、製品版のカート合計3,980円とレジのログイン入口まで確認した。
- 既存の公開説明ページに製品購入ボタンを追加。予定価格を税込価格へ更新し、購入にはBOOTHログインが必要で、ソフト利用時は登録不要と明記した。製品・体験版の配布ZIPは変更していない。
- 製品ZIPと無料ZIPはBOOTHで別の選択肢へ割り当て済み。管理側からの再ダウンロードと原本の完全一致を前回確認。実顧客の決済・購入後ダウンロード・銀行着金・収益達成は未確認。自分で注文を確定して実績を作る操作はしていない。
- 今回は静的HTMLの文言とリンクだけ。390/1440px表示とリンクを確認し、本番反映結果は成果物・Obsidianへ記録する。前回のソフト往復・上限・公開分離・全体検証結果を引き継ぐ。

## Current State — 2026-09-19 JST

### 写真セレクト便の無料体験版（BOOTH販売は未接続）

- AI利用料を賄うためのダウンロード商品。オーナーの `/tools/photo-select-bin.html` から、PCで写真選定を往復できる12枚までの無料体験版・使い方案内を提供する。Portfolio Kitの顧客サイトには案内を表示せず、同じ配布パスも404にする。
- 本リポジトリに含めるのは無料版と公開説明だけ。300枚の製品版ZIP、本人用の収支画面、保守用ソースは公開していない。価格3,980円は販売予定で、BOOTHのログイン・出品・購入者向け配布確認は残っている。売上は未確認。
- 2026-09-19検証：`bun run check` 成功（製品1469件・型・lint・本番build等）。全体smokeは578成功／180対象外／1失敗。失敗は既存HEROテストのドラッグ対象が構図ボタンも拾うためで、対象を並べ替えボタンに限定して同じ1件の再検証に成功。全体の再実行はしていない。
- 隔離した本番用サーバーで、HTML/ZIPの種類、HTMLのno-store、オーナー限定公開・sitemap、製品版ZIPと収支ファイルの404を確認。製品自体はChromium・WebKitでファイル往復、別案件の回答拒否、外部HTTP要求なしを確認し、300枚の上限と原本不変も検証。
- この更新は本番の写真・設定・DBを変更しない。公開URLでの最終確認と販売接続の現在地は、作業成果物の「検証と公開状況.md」およびObsidianの「写真セレクト便」に記録する。

### HEROと写真の構図を任意位置で扱う（mainへcommit・push・本番反映済み）

- Libraryの「見せる中心」は9点固定を廃止し、実写真をクリック／ドラッグで任意位置へ置けるようにした。矢印キーは1%、Shift+矢印は10%ずつ動く。写真共通の基準点なので、切り抜きを使う既存の表示にも反映する。
- HEROには `hero_photos` の任意座標上書きを追加。HEROタブで選んだ写真だけ、専用の大きいプレビューから寄せを保存でき、「写真の基準点に戻す」で上書きを消せる。Galleryやシリーズの構図は変えない。
- Turso/PostgreSQL両方にmigrationを追加。2026-09-19に `bun run check` とHEROのPC smoke（4件）を成功。commit `818026f` をmainへpushし、本番 `/api/health` build `818026fb` と公開TOP・`/api/hero-photos`を確認。

### 見つける・見る・作家を知る・相談する（main、push・本番反映まで完了）

- 依頼: 写真の魅力と静かな品位を保ったまま、初めての人が作品を見つけ、十分に見て、気になった作品について相談できるようにする。オーナーが少ない操作でそれを保てるようにする。**判断と実測の正本は `docs/specs/find-view-ask-2026-09-19.md`**（何を変えなかったかとその理由も書いてある）。
- 先に測った本番（読み取りのみ、build `eaefe997`）: 公開写真133枚（standalone 0）、Series 1本32枚・Work 1本101枚、題・説明・副題・作家の言葉はすべて0件、デジタル94／フィルム39。Top は `topWorksMode=random`・`homeGalleryCount=29`・`topWorksLayout=grid`・`topWorksColumns=8`。
- 変更:
  - `31f079c` フィルムの `shotAt` は複写日時なので「○年○月に撮影」と言わない（本番の該当39枚）。ビューアが既に "Scanned" と区別していたのに説明文だけがずれていた。`d0f8bf8` で sitemap 側にも同じ判定を渡した。
  - `416efe3` About の Journal の抜粋を文の終わりで切る（本番3件中2件が語の途中で切れていた）。
  - `a6575a5` Work 棚の写真から作品ページへ戻れるようにする。対応表を Series 棚だけで作り、行き先を `/series/<slug>` と決め打ちにしていたため、**公開写真133枚のうち101枚（76%）が拡大しても作品名もリンクも持たなかった**（`/series/rintaro` はサーバーが404を返す）。
  - `36a7592` 「トップに出す写真」に「選んだ順」を作り、前へ／後ろへ／外すを付けた。1操作＝1回の取り消し。トップ専用の設定を触ったらプレビューをトップへ向ける。編集するのは `topWorksIds` の並びだけ。
  - `f33e2c0` 作品の導入から相談へ（`?work=<slug>` で公開作品の識別子だけを渡し、参考作品は外せる）。本文欄に消えない記入案内、対応地域と流れを同じ枠へ、件名が Portfolio Kit なら案内だけ切り替え。
  - `864a184` タイル画像を枠の大きさで頼む（先頭8枚は先読みのURLのまま）。
  - `17318ed` Contact に別の事業の案内（StudioBridge）を出さない。販売導線は他のページに残す。
- **測って「やらない」と決めた**: Top の「さらに見る」（390pxで3,376px＝約4画面、Series の帯 y=2,379・Work リンク y=850 で到達性の問題は無かった）、Contact のラベルの英語表記（サイト全体の意匠）、件名の選択肢「テンプレートについて」（2026-07-08 承認の計測用）、縦横比の予約と遅延読み込み（既に十分だった）。
- 性能の実測（390×844・DPR2、本番トップ、スクロール前）: 画像 2,308,083B → 974,503B（-57.8%）。タイルは 1,809,482B → 475,902B。74px の枠に 640px を配っていた（必要 148px の4.32倍）。LCP 1,284ms（Top）／1,356ms（`/work/rintaro`）、CLS はどちらも 0。
- 2026-09-19 検証: `bun run check` 成功。全体 `bun run smoke` は最初 565成功／177スキップ／失敗13（13件は既知）。**その13件もこの日にすべて直し、最終は 575成功／180スキップ／失敗0**（合計755は同じで、10件が成功へ、3件が project 指定によるスキップへ）。証拠・測定スクリプト・比較画像は `scratch/site-improve-20260919/`。

### 既知だった smoke の13件を解消（backlog S-3 を削除）

- 内訳は spec の古さ5件・スマホ導線の見落とし3件・**製品の不具合5件**。1件ずつの原因と直し方は `docs/archive/audits/adobe-comparison-verification-2026-09-17.md` の 2026-09-19 追記。
- 製品側の修正: 矢印キーを写真一覧が常に奪っていた（キーボードだけで使うとナビから写真へ引き戻される）／畳んだナビのメニューで最初の項目に焦点が当たらない／写真のダブルクリックで詳細と「拡大」（PC）・節の切替（スマホ）が二重に開く／スマホの一括メニューが長いシリーズ名で幅504pxになり375pxの画面から左へはみ出す（x=-212.6）。
- 期待値は弱めていない。目次の幅は固定値をやめ「ナビの内側と一致し、読める幅がある」を測る形にした（寸法の再設計で腐らない）。
- **残る差（仕様の判断が要る・直していない）**: 選択したまま並べ替えへ入り「並べ替えを終了」すると、スマホでは選択が消える。PCは並べ替え→選択へ直接戻れて復元する。スマホにはその道が無い。
- 管理画面の変更なので、**本番の認証済み画面での目視はこの環境ではできていない**（回帰は全体smokeと単体テストで見ている）。
- 残した判断（コードでは決められない）: 作品の題・説明・撮影地が0件、Work の副題・作家の言葉が空、件名の選択肢を残すか、本番の `topWorksMode`（random は60枚上限）。**問い合わせ数・売上への効果は未測定。**

## Current State — 2026-09-17 JST

### Adobe比較監査の第1段階と smoke の本番接続の遮断（ローカルブランチ `audit/stage1-fixes`、未push・未統合）

- 依頼: `claude_code_akieguchi_handoff.md`（監査原文 `akieguchi_adobe_portfolio_audit_2026-09-17.md`）。第1段階を実装・テスト。続けてオーナー指示で B-29（smoke の本番接続）を最優先に対応。本番書き込み・migration・push・mainへの統合・デプロイはしない指定。
- オーナー判断（2026-09-17）: `site-and-data-direction.md` の現行方針を維持。版管理・公開履歴・限定公開・多対多・保存前の変更一覧は実装しない。第2段階の文書（`docs/specs/publishing-and-placement-2026-09-17.md`）は未承認の検討案。ゴミ箱の一覧と削除の分離（`docs/specs/trash-list-and-purge-separation-2026-09-17.md`）も未承認の案。
- 第1段階（worktree `/Users/chiaki/wt-audit-stage1`）: 複製で撮影日時・由来・元ファイル情報を保つ `8dfdff2` / Work の404・通信失敗の戻り先 `7a18fe7` / 作品詳細とトップ写真の公開応答 `95f0843` / 設定プレビューで Work と各作品を選ぶ `72554f8` / 文書 `4b033f9`。判定と根拠は `docs/archive/audits/adobe-comparison-verification-2026-09-17.md`。
- smoke の遮断: `7bd685c`・`847a824`・`86f65e2`。`bun run smoke` は `scripts/smoke/isolated-server.ts` が実行ごとの一時SQLite＋人工データ（`packages/web/src/test-fixtures/smoke-site.ts`）＋127.0.0.1の偽ストレージ＋`smoke-only-` パスワードで起動し、`.env` と親の接続情報を使わない。接続先を確認できない・ポート使用中なら止まる。spec は `./fixtures.ts` の自動 fixture（外部・別ポート・書き込みを止める）を使う。残りのブラウザの外向き接続は遮断プロキシが拒否・記録し、Google Fonts への preconnect（CONNECT 2宛先の完全一致・テスト中）以外は失敗にする。番人の検証は `bun run test:smoke-guard`（`check` に含む）。
- 以前の helpers.ts の書き込みの番人は、最上位の `beforeEach` のため最初の spec にしか効いていなかった。
- 2026-09-17 検証（コミット `86f65e2`）: `bun run check` 成功（製品1433・ツール60・番人34、型・lint・build）。全体smoke 726件＝成功542・スキップ171・**失敗13**。13件は `1ef90fc` に安全対策 `7bd685c` を載せた基準でも、同じ隔離条件・同じ要素と値で再現した（原因確認済8・未調査5、backlog S-3）。基準に安全対策を含むため、ブランチ全体に回帰が無いことの証明ではない。遮断228件はすべて WebKit からの Google Fonts preconnect。記録は worktree の `scratch/smoke-evidence/2026-09-17T05-15-23-111Z/` と `scratch/audit-stage1-20260917/`。PostgreSQL・スマホ実機・本番は未検証。
- 外部レビュー（`ee38df9` の差分、2026-09-17）の R1〜R4 を限定修正: R4 `5fd840e`、R1〜R3 `d6b34aa`。R1 `page.request`・`context.request`・request fixture は route を通らないので fixtures.ts が送る前に止め、spec の API 読取は `api`（同一オリジン・GET/HEAD・リダイレクト不追従）だけ。R2 終了時の判定はサーバー側の遮断記録を必ず読み、1件でも・読めなくても失敗。R3 実行中テスト名は context の前に付け、閉じた後に外す（成功・失敗・時間切れ）。R4 番人テストは `node scripts/smoke/guard/run.mjs`（22.12〜22.17 は型除去フラグを付与、22.12 未満は理由を出して止める）。R1〜R3 は本物の fixture で動かす `guard/playwright-probes.test.ts` が `ee38df9` では失敗し修正後に成功。Node 22 の確認（公式配布を一時フォルダで使用、既定の Node 24 は変えていない）: Playwright 1.61.0 では 22.18.0 で相対 import を含む TS を読めなかった（Node が require の conditions を Set で渡すため）。2026-09-18 にオーナーの許可で Playwright を 1.61.1 に更新（`6564aa8`、`@playwright/test`・`playwright` を 1.61.1 に固定。lockfile は Playwright の3件だけ）し、22.18.0 を拒否する判定を外した（`2c64c7b`）。更新後は global-setup.ts が smoke-site.ts を import() で読むと spec が読めなくなったため、静的 import にした（`32a47d9`）。`32a47d9` で番人テスト48件と関連 smoke（成功14・スキップ13・失敗0）が 22.12.0・22.18.0・24.16.0 のすべてで通り、子プロセスも同じ Node だった。結果は `docs/archive/audits/adobe-comparison-verification-2026-09-17.md` の追記。
- ~~**main 統合前の注意**: main の `bun run smoke` はまだ本番接続のまま。~~ **2026-09-19 追記: `audit/stage1-fixes` は main へ統合・push 済み（`eaefe99`）で、main の `bun run smoke` も隔離済み。** 見出しの「未push・未統合」も同様に解消している。
- 触れていない判断事項: FAQ・販売文書の「24時間／3日」（B-28）、ゴミ箱GETの削除（B-29 製品側）。

## Current State — 2026-09-16 JST

### シリーズ閲覧・Galleryの入口・TOPの作品導線（main へ統合）

- 依頼: 初めての人が「シリーズを選ぶ→写真を見る→別の作品・作家情報へ進む」をスマホ・PCで気持ちよく行えるようにする。Gallery は `galleryExcludeSeries`（2026-08-09 オーナー依頼）を保ったまま扱う。本番の写真・設定は変更しない。
- シリーズ: 公開写真0枚の組を公開の棚・ナビ・TOPの帯・Nextから外す（URL・公開状態・管理画面は不変）。表紙の見開きは「選んだ表紙が1枚目と別の写真」のときだけで、それ以外は題名＋「点数 ／ 期間」から始める。editorial 配置はPCで縦写真を画面の高さに収め、スマホは組を縦に積む（全幅＋72%）。見終わりにプロフィールへのリンク。
- Gallery: 並べる写真が0枚のあいだ共通ナビから入口を外し、URLで来た人には中身のある Series・Work を案内する。件数は `/api/photos/availability`（total / standalone）だけを読む。
- TOP: 「作品を見る」導線は Gallery に写真があれば従来どおり。0枚と確認できたときだけ中身のある棚へ（Series の帯が出ていればシリーズは重ねない）。取得中・失敗は0枚として扱わない。規則は `web/lib/work-entries.ts` に1つ。
- 既存テスト: 写真一覧は閲覧モードで開く仕様に期待値を更新。Gallery の smoke は本番データ依存をやめ、固定データで写真あり・なしを確認する。
- 2026-09-16検証: `bun run check` 成功（製品1381件・ツール60件・型・lint・本番build）。公開smoke public-site + public-scroll-stability 316成功／23対象外／1失敗（`/start` 横スクロール、単独再実行で成功）。PC・スマホで前後比較、複数シリーズはローカルの合成データとモックで確認。画像と検証スクリプトは `scratch/series-view-20260915/`。本番反映後の確認は同日のObsidian開発ログへ記録する。

## Current State — 2026-09-13 JST

### Adminの視覚的な仕上がりを再設計（実装・検証完了）

- 前回は操作整理に偏り、見た目の品質が足りないという指摘への対応。写真付きの構図比較、見出し・ラベル・補足の強弱、入力欄と操作グループ、明確な現在地・選択枠、プレビューの面と余白を統一した。
- 日本語の入口を「写真一覧」「トップの写真」「プロフィール」「分類」「シリーズ」「料金・プラン」「サイトデザイン」へ変更。構図も「順に切り替え」「均等に並べる」など結果の分かる名称にした。選び方の説明はラベル横のヘルプから開く。
- 一覧の新規表示サイズはPC160px／スマホ110px。保存済みのサイズは保持する。使用状況ラベルはホバー・フォーカス時に表示し、詳細画面には残す。スマホの操作帯は3行内へ収めた。
- 古い共通CSSが選択枠・余白を上書きしていたため、ブラウザーのcomputed styleと実写真で確認して修正。新しい選択枠の描画とキーボードでの変更・取り消しを明暗両方で検査するsmokeを追加。
- 2026-09-13: 最終`bun run check`成功（製品1349件、ツール60件、型・lint・本番build）。全体smokeは516成功／167対象外／9失敗／1再試行成功。旧文言・寸法の期待値、初期セットアップと再試行の検証条件、スマホの余白を修正。関連再検証は55成功／43対象外／0失敗（3.2分）。公開スクロールの一時的な読み込み待ちも変更なしで再検証成功。
- 9画面の390px／1440px、選択・詳細・拡大の320〜1440px、明暗を表示確認。540枚の検証用一覧は64タイル描画で選択・最後の写真を保持。公開とプレビューは1440×900・390×844・1080×720で写真と文字の座標・書体が一致。写真原本・本番データの書き込みなし。
- 仕上がり画像: `scratch/admin-design-20260913/`。構造と検証の詳細は `docs/specs/admin-studio-2026-09-13.md`、操作は `docs/admin-guide.md`。commit・push・本番反映後の実操作は同日のObsidian開発ログへ記録する。

### Adminを写真管理・サイト編集の2つの作業画面へ再設計（実装・検証完了）

- 写真管理とサイト編集を同程度使うという回答に基づく。既存の画面構成は変更可、完成まで実装・検証・反映する依頼。
- 左上／スマホ下部を「写真」「サイト編集」の同じ重さの入口へ。設定21節を4群へ整理し、PCの左一覧と⌘Kの目的別検索から直接移動できる。選択肢は図と名称の行や標準selectに整理し、過剰な箱と画面切替の遅延を減らした。
- 写真は選択が既定。Spaceで拡大、矢印で前後、Enter/Eで編集、Gで一覧へ戻る。現在位置・前後写真の帯を表示し、選択枚数と最後に見た写真へのフォーカスを保持する。スマホは選択中から並べ替えへ進め、選択終了後の操作帯も3行内に収まる。
- 設定は保存前のUndo/Redo、プリセット単位の取り消し、⌘/Ctrl+Sに対応。プレビューの編集中／保存済み比較、ページ切替、任意の幅・高さを追加した。
- 旧プレビューは狭い欄で内部画面が1280×1467〜1919へ縦長になっていた。1440×900など実際の寸法を固定して縮小する方式へ変更。
- 2026-09-13検証: 最終`bun run check`成功（製品1349件・ツール60件・型・lint・本番用build）。全体smokeは523成功／167対象外／3失敗。スマホの操作帯と並べ替え入口、旧ナビ名の期待値を修正し、関連再検証は34成功／37対象外／0失敗（PC・スマホ・Pixel・Safari）。
- 本番用ビルドで同じ写真・設定を使用した1440×900、390×844、1080×720の比較は、公開ページとプレビューの写真・名前の書体と位置が一致。540枚の検証用一覧で全選択から20枚の前後確認、編集、一覧復帰と設定検索を確認。DOMは116タイル、JS例外・書き込みなし。
- 設計: `docs/specs/admin-studio-2026-09-13.md`。画像・検証ログ: `scratch/admin-redesign-20260913/`。反映後の本番buildと認証済みAdminの確認結果は同日のObsidian開発ログへ記録する。
- 写真原本・本番データは変更していない。前回のTIFF日時・取り込み・日付なし末尾・一括選択の修正を保持。

### TIFFの日付とAdminの取り込み・確認操作を改善（実装・検証完了）

- 依頼: TIFで日時が空欄になり撮影日の新しい順の先頭へ出る問題、フィルム複写のスキャン順、Adminの重さ・分かりにくさを改善。
- 日付順が空欄を追加日時へ置き換えていた処理を修正し、公開・Adminとも日時なしは新旧どちらも末尾にする。「最近追加」の先頭固定は手動順だけに限定。日時順の取り込み後は並び順を保ったまま新しい写真へ移動する。
- 元TIFFのIFD/XMPから日時を読み取る。フィルムでもカメラのDateTimeOriginalを複写日時として採用し、DateTimeDigitizedを次点とする。更新日時や現在日時を自動で撮影日時にはしない。
- 取り込み前の確認画面でデジタル／フィルム、読めた日時、日時なしの扱いを確認できる。TIFは既存の300MB上限で対応し、サイト用JPEGを作成。原本は手元で保持する。
- 既存写真は「元ファイルから日時を読み直す」で端末内の元ファイルと照合し、確認後に日付・出所だけを更新できる。画像の削除・再アップロードは不要。同名が曖昧な場合は更新対象外。
- 一覧の並び順・日時なしの件数を常設し、ファイル名の自然順を追加。掲載順への保存は表示メニューに分離。写真確認には位置／ファイル名／日時の出所を表示し、クイック移動を常設。既存の範囲・全選択、矢印・Space操作は維持。
- 拡大・編集で生成済みmediumを共有し、前後2枚を先読み。仮想表示の余分な行と1pxごとの全体再描画を減らした。同じローカル製品ビルドでDOM画像161→118、スクロールp95 14.6→12.9ms（限定した測定）。
- 2026-09-13検証: `bun run check` 成功（製品1347、ツール60）、最終lint・build成功。全体smoke507成功／166対象外／旧UI前提の8失敗。前提修正と新規ケースを含む関連再実行は55成功／25対象外／0失敗（PC・スマホ・Pixel・WebKit）。最終320〜1440px明暗の横はみ出し・JS例外・書き込み0。
- 実ファイル `IMG_6609.tif` は撮影・デジタル化日時がなく、XMPの編集日時だけだった。日付のないすべてのTIFが同じ原因とは未確認。元撮影／複写ファイルからの復元か手動指定が必要。本番の既存写真・日付・設定は変更していない。
- 初回push `554ecc2` のRailwayビルドは、最後の短文化に追従しなかった旧「手動順」のテスト期待値1件で型検査が失敗。本番は旧版のまま稼働。期待値を修正後、本番と同じ `bun run --filter=@template/web build` とi18nテスト6件を成功確認した。
- 証拠: `scratch/admin-workflow-20260913/`。反映後の本番buildと認証済みAdminの確認結果は同日のObsidian開発ログへ記録する。

### Adminの写真選択と操作デザインを改善（実装・検証完了）

- 依頼: AI感のあるAdminを抑え、Lightroomのように迷わず複数写真を扱いたい。一括選択の不具合を修正。
- 原因: 写真ボタンの `onFocus` がクリックより先に `lastClicked` を上書きし、Shiftの範囲が1枚に縮んでいた。範囲の起点とフォーカスを分離し、Shiftクリック／Shift+上下左右で範囲の拡大・縮小を可能にした。
- 「すべて選択」「選択解除」を常設。全選択は仮想表示の画面外も対象で、絞り込み外の既選択も保持する。⌘/Ctrl+A、⌘/Ctrl+Shift+A、選択中のSpace拡大に対応。選択タブを押し直しても選択は失わず、文字入力とモーダルの操作を優先する。
- 選択の手描きアニメーションを静かなチェックへ、丸いモード切替を下線タブへ変更。Adminの角丸を抑え、作業ダイアログの透過・ぼかしを外した。操作キー「?」は上部へ、ヘルプは平易な日本語へ。
- スマホ一括操作欄を写真一覧と別の下端行に配置。スクロール範囲も縮めるため、最後の写真とナビを隠さない。カテゴリー／シリーズのラベルを短くし、メニューは写真側へ上向きに開く。
- 実写真確認で、写真編集の選択済みボタンが指示子を不透明背景で覆い、暗色文字が読めない既存不具合も修正。CSSの対象を分離し、指示子の2pxのずれも修正。色の値だけでなく、背景の透過と位置一致を回帰テストで確認する。
- 検証: `bun run check` 成功（1337 pass / ツール60 pass）、最終build・lint成功。全体smokeは502 pass / 166 skip / 5 fail（18.2分）。旧選択マーク・旧背景・旧固定位置の前提と、スマホで「?」が1行増やす問題を修正し、該当5件＋新しい選択操作・表示検証を再実行して12 pass / 8 skip / 0 fail（45秒）。全体初回を全件成功とは扱わない。
- 320〜1440px・明暗・日英、実写真スナップショットと全9タブをローカル確認。写真原本・保存データは保持し、書き込みはモックで検証。本番Chromeの既存ログインと554枚の表示を確認済み。commit・push・反映後の実操作結果は同日のObsidian開発ログを参照。証拠: `scratch/admin-library-20260912/`。


### admin 黒ベース化 + 操作のカクつき + 本番の隠れたReact devモード混入（実装・検証・本番反映まで完了）

- Codex司令塔・実装Claude。依頼「adminは黒ベースで見やすく、黒/白/好きな色にできる完成度を上げる。admin操作のカクつきも」。調査→設計→実装→複数回のレビューと修正を経て完了。
- **admin だけの明暗**（新規・DBキーなし）: `useAdminSurface`（端末ローカル `localStorage`、既定 **dark**）。単一44pxトリガー→ラベル付きメニュー（明るい/暗い/サイトに合わせる、現在の選択に✓、Arrow/Home/End/Escapeキーボード対応、選択後トリガーへフォーカス復帰）。色相はCMSの明/暗パレットから従来どおり派生、公開サイトの表示・プレビューは不変（実測確認済み）。
- **配色コントラスト**: `adminThemeFromSettings` の `ensureContrast` が黒/白どちらを目標にするか「背景輝度>0.5」だけで決めていたバグを修正（中間輝度の背景で片方の極でしか基準に届かないのに届かない方を選ぶことがあった。実測 #999999 で2.85:1→6.6:1に改善、回帰テスト追加）。意味色（危険/注意/成功/情報）も `muted` と同じく最も明度が近い面（paper-deep）に対して基準を満たすよう修正。暗テーマの面の段差（paper-soft/deep/line）を拡大。
- **カクつき**: contact-sheet の各セルを photo.id キーの絶対配置へフラット化（密度変更で `<img>` を作り直さなくなった）。スライダーは1フレーム1回のrAFプレビュー+release/blur/keyup/180msアイドルで確定。
- **本番に隠れていた別バグを発見・修正**: CPUプロファイルで密度スライダーの残カクつきの77.6%がReactの開発用JSXランタイム(`jsxDEV`)の自己時間と判明。ローカルの `packages/web/.env`(`NODE_ENV=development`, gitignore対象)を `vite.config.ts` が無条件に `process.env` へマージしていたことが原因と考えたが、**本番(Railway)にも同じ現象が実際に出ていた**（deploy前の本番 `react-vendor` バンドルがローカルの壊れたビルドと同一ハッシュ・同一バイト数で、`jsxDEV` 呼び出しを含んでいた）。`package.json`/`packages/web/package.json` のbuildスクリプトへ明示的に `NODE_ENV=production` を追加して解消。**本番の `react-vendor` バンドルが365.9KB→185.7KB(-49%)に縮小したことをdeploy後に実測確認**（全訪問者に影響する未知の性能劣化が本番から消えた）。
- 検証: `bun run check`（1337 pass, build確認）、全体 `bun run smoke`（665件, 502 passed/163 skipped/**0 failed**, 事前の7件の失敗は「admin既定がdarkになったことで、既定lightを前提にしていた既存の見た目アサーションが変わった」ことが原因と特定し、smoke共通の `loginAsAdmin` ヘルパーでlight固定に修正して解消。加えてOLD_DARK_COLORS回帰ガードとの数値衝突、rowGapのCSS前提の陳腐化も修正）。
- 実写真検証: `scratch/admin-color-performance-20260909/final-look-real.cjs` で本番写真URL（読み取り専用フェッチ、書込み一切なし）+ prod-snapshot実設定を使い、Library(PC dark/390 light)・写真編集・全9タブ巡回を実施。76/76画像 naturalWidth>0・白抜け0・JS例外0。
- commit `fca2879` → push → Railway反映確認（`/api/health` build `a736e957`→`fca2879c`、約90秒）。本番の公開ページ(200)・admin未認証画面(200)を確認、本番配信中の `react-vendor` バンドルを実際にダウンロードして jsxDEV 0件・サイズ半減を確認。**本番の認証済みadmin目視は未実施**（認証済みセッション無し、Save/Delete等の書込み操作は一切していない）。
- 証拠・測定値・限界: `scratch/admin-color-performance-20260909/result.md`、開発ログ 2026-09-12。


### 最新: スマホの使用感 — 公開＋adminを実操作で改善

- Codex司令塔・実装Claude。公開/adminのスマホを 320/390/430px タッチで実操作（Library=スクロール→サイズ変更→写真を開く→編集→戻る、条件パネルで実カテゴリ選択→閉じる→解除、写真編集のフォーカス＋モック保存、HERO拡大、Settings入力→保存）。本番の写真・設定・DBは未変更（全モック）。
- **拡大ビューア（Lightbox）**: 拡大／撮影情報ボタン 38→44×44の当たり判定（アイコン18pxのまま、位置・見た目・PC不変）。情報パネル下オフセット 52→64px。
- **Library サイズスライダー**: 64px固定（20段で3.2px/step）→ `clamp(64px, calc(60vw-118px), 180px)`。320px=**74px**で見出し1段（singleRow実測）、390→116px(5.8/step)、430→140px(7/step)。機能追加なし。
- **HERO 候補ボタン**: 写真ごとに濃い塗り円が2つで「ボテっ」→ 目ボタンは40/44pxの当たり判定を保ち描画は約27pxの半透明ディスクに、未選択の「+」印を21→18px・半透明・muted。選択済み✓は accent 濃色を維持。dark/高コントラストも確認。
- **サイズ・列数のアンカー**: 速いドラッグ/連打で見ていた写真を見失う（旧: 上端が約370–530px逸脱で画面外）→ `admin.tsx` を書き直し。ジェスチャー開始時に写真を`{id,offset,pinnedTop}`で1回捕捉し調整中保持、密度値変更ごとにアンカー行を復元（effect依存は密度値4つのみ＝ループ回避）、offsetを新row高でクランプ（縮小しても必ず画面内）。終端=pointerup/cancel/lostcapture/blur・key-up・単発changeは220msアイドル（ドラッグ中は時間切れで消さない）。手動スクロールで基準がずれたら次のkeydownで再捕捉。空/絞り込みゼロは捕捉せず（-1固着を除去）。実写真390/430の全ケースで開始写真がinView（grow −20→−17 / keyboard −83→−80 / grid列数 −101→−57・−21→−18）。回帰テストを `admin-library-contact-sheet.spec.ts` に追加（thumbドラッグ/track-click/rows→grid/列数/keyboard連打+Tab/手動スクロール後の新gesture、desktop+mobile-touch緑）。
- **公開 Series 札**: 2列スマホで期間の「月」だけが孤立して折り返す→ isMobile時は点数と期間を別行（区切りを行末に残さない）。期間は `PeriodText` で年月ごとの nowrap inline-block にし、ダッシュは前の年月に付けその後ろにだけ `<wbr/>`。狭い札で「2024年8月–」／「2025年8月」と日付単位で改行、"月"や"–"は単独行にしない。期間全体の無条件nowrapには戻さず、年月単位で折り返す。PCは「点数 ／ 期間」1行のまま。`series-scale.render.test.tsx` 他の文字列・意味は不変。320px画像で確認。
- **フッター "X" リンク**: 当たり判定 横25px→ `.footer-sns-nav` に coarse時 リンク間隔（横gap）24→36px＋`.tap-target::before` 横±17.5px。実測 **44×54px**、Instagram 95 / note 61、隣接1pxクリア。グリフ不変、リンク間隔のみ12px広がる（タッチのみ）。方針リンク(Privacy 56/利用条件 62)は未変更。
- 検証: `bun run typecheck`(tsc -b, ビルド情報再生成) 成功 / `bun run lint` 成功 / `bun test packages/web/src/web` 724 pass 0 fail / `bun run build` 成功 / `bun run smoke -- admin-library-contact-sheet.spec.ts`(desktop+mobile-touch) 6/6 pass / **`bun run smoke`(全体, foreground完了) 501 passed・160 skipped・0 failed（17.6分, flake/retryなし）**。
- 証拠 `scratch/mobile-claude-20260908/`（flows.cjs / shoot.cjs / measure.cjs / footer-measure.cjs / filter-flow.cjs / anchor-drag.cjs / prod-verify.cjs、before/after 画像、progress.md、result.md）。
- commit・push・本番反映・本番ブラウザ確認（Chromium/WebKit×320/390/430、実画像・拡大・Series期間・SNS当たり判定・写真/設定スナップショット比較で不変）まで完了。実施日時・commit・build・比較結果は 2026-09-09 開発ログを正本とする。本番 admin の認証済み目視はこの環境ではできず未実施（回帰は全体smokeとテストでカバー）。編集ヘッダー閉じるボタン実測h=40のみ低優先の残点。

### 公開の言葉とAdminの操作欄を軽くする

- 公開スマホGalleryの入口を「写真を探す」に変更。件数は「枚」、カテゴリー欄は「テーマ」、解除は「すべての写真」とし、条件選択の塗りつぶしを細い下線へ変更。Adminの検索・絞り込み操作は保持する。
- Adminの操作欄のボタン枠・選択時の影を整理。PCの展開サイドバーは200px、スマホLibraryの見出し・行組み／グリッド・密度調整・プレビューは1段。写真に題名／分類がないという常設行を外し、同じ条件は絞り込みパネルへ集約。保存・取り込みの主操作、入力文字とタップ領域は保持する。
- 配色は既存の `adminThemeFromSettings` による公開設定との同期を維持する。写真のdecode・仮想表示・保存経路に新しい処理を加えない。
- 2026-09-08の同じ実写真で、PC1440pxの一覧枠は1192×701pxから1240×749px、スマホ390pxは390×631.75pxから390×655px。全9タブ×PC2幅、320〜1440pxの密度切替、明暗・高コントラストの表示を確認。証拠は `scratch/slim-admin-20260908/`。最終検証・commit・push・本番確認は同日の開発ログへ記録する。

### 公開・Adminの不要な外枠余白を縮小

- 2026-09-08夜の「謎の無意味な余白は全体的に無くす、adminも」を反映。公開のスマホ写真一覧と、PCのclean／縦横グリッド・masonry・justifiedは表示領域いっぱいに広げる。文字枠とは分離し、左ナビ・スクロールバーを除いた幅で計測する。列数上限・写真サイズ・写真間隔・配置の既存設定は保持。作品配置のための内部余白は一括で消さない。
- 本文の外側はスマホ16px／PC24px、ページ上部の基準は24px／48pxへ縮小。Gallery・Series・Profile・トップ作品欄の過大な節間も縮め、ページ上部・節間の倍率設定は維持する。
- Adminは共通本文12px、Library写真一覧の左右0px、HERO候補の二重の内側余白を削減。Settings本文・プレビューの外枠を縮小。タップ領域、写真の縦横比、保存帯・ナビのための下部余白は保持。
- Safariでキャッシュ済みの写真が拡張前の幅だけで画質判定される問題も修正。既存の一覧幅計測を画像へ渡し、幅が確定・変更したときに必要な写真だけ生成済みmediumへ更新する。写真ごとのResizeObserverは追加しない。
- 検証証拠は `scratch/spacing-20260908/`。12配置×スマホ／左ナビPCの24条件で横はみ出しなし、Admin全9タブ×PC2幅の18条件で見出し・操作領域を確認。最終テスト・commit・push・本番表示確認は同日の開発ログへ記録する。

### 静かな公開表示とAdmin設定への連動を復元

- 同日15時台、ユーザーが公開側の装飾・機能増加と、Admin設定に準拠しない表示を却下。「元の静けさ」を基準とする。以下の写真アプリ統合と13:55の再配置は過去の経緯であり、現行の採用方針ではない。
- 公開側の別レンダラー・列数／撮影日ソート／ガラスの操作欄・表示切替を撤去。既存のHERO5構成、配色、書体、余白、ギャラリー、ナビ、シリーズ、動きの設定が公開とプレビューへ反映される表示に統一。試作は `docs/design-studies/` に保持。写真・本番設定の書き換えは行わない。
- Adminの行組み・密度・HERO候補の検索／ソート・編集復帰・設定プレビュー・保存を保持。共通ボタンの選択表示、丸み、検索欄の不統一を修正。HERO候補の枠を抑え、拡大アイコンはマウスではhover／focus時、タッチでは常設。高コントラスト時の選択表示も確認。
- 2026-09-08の `bun run check` 成功（製品1,319件・ツール60件、既存Wiki鮮度警告8件）。最終製品差分の型・lint・build成功。全体smokeは484成功・161対象外・12失敗。新しく加えたWebKit公開検証のキー移動／ホイール操作が原因で、実操作を保った検証方法へ修正後、該当ファイル74成功・8対象外。全体smokeの再実行はしていない。
- 実写真のChrome1440px／WebKit390pxで保存済みの書体、HERO構成、列数とサイズ、ナビ位置、画像読込、拡大・前後・閉じるを確認。JS例外・書き込み・横はみ出しなし。同じMacでスクロール中央値8.3ms／17ms、50ms超0（限定した自動ブラウザー計測）。AdminのLibrary・HERO・SettingsをPC／タッチで目視確認し、PCプレビューの実画像と祖先のopacityも確認した。
- 証拠は `scratch/quiet-portfolio-20260908/`。commit・push・本番build・データ保持の最終確認は同日の開発ログへ記録する。設計と操作は `docs/specs/public-design-review-2026-09-08.md`、`docs/admin-guide.md`。
- 15:49に本番build `2d773f3b` を確認し、公開428枚・既存設定175項目の反映前後ハッシュが一致。PC／WebKitの本番実画像・拡大・復帰も成功。認証済みChromeのLibraryでは、画像読込とopacityが正常でも一部が空白に見え、タブ再訪で解消する現象を目視した。追加修正で一覧サムネイルの透明からのフェードを廃止し、直接表示する。関連smoke11成功・4対象外、うち読込待ち不足で対象外になったSafari再マウント検証は待機を直して1成功。lint・build成功。追加修正の本番反映結果も同日の開発ログへ記録する。
- 16時台の追検証で、フェード停止だけでは空白が再現すると判明。実写真428枚を使ったChromeの同条件比較で、非同期decodeは再読み込み時に可視58枚中12／11枚が空白、同期decodeは0枚。仮想表示範囲の小さいサムネイルだけを `decoding="sync"` に変更。製品ソースのPC表示・4回の再読み込み・深いスクロールでも空白0、JS例外・書き込み0。関連smoke9件、型・lint・build成功。Macの同条件スクロール中央値は両方式16.7ms。遅いフレームは両方式に残り、全端末での速度改善とは扱わない。最終反映後の実ブラウザー結果は同日の開発ログへ記録する。

### 写真主役の配置への修正とAdminの仕上げ

- 同日13:55の「Adminが未完成」「公開側も元のように写真が主役」の指摘を反映。公開側は名前と小さな横ナビ、代表写真、作品一覧へ再構成。Liquid Glassは小さな操作欄に使用し、写真の面積を確保する。`publicExperience=photo-app` を「写真中心のポートフォリオ」と表示する。
- Admin全9タブをシステム書体へ統一し、Libraryの見出し・密度調整・プレビューを整理。HEROの候補を小さいスマホでも2段見せる。写真編集終了後のフォーカス復帰を修正。Settingsは項目一覧をまとめ、本文360px以上と、PC幅1280px以上を枠へ縮小したプレビューを両立。拡大プレビューの保存・編集復帰、従来表示用の設定の案内を追加。
- 2026-09-08の実画面確認で、320/390pxタッチのLibrary写真開始Y216px、1024pxで189px。全9タブの横はみ出し・画像失敗・JS例外なし。公開側はChrome/WebKitで拡大・密度変更・履歴・フォーカスを確認。スクロールは同じMacのwarm比較で8.3ms中央値、ガラス無効時と同程度。
- `bun run check` 成功（製品1,337件・ツール60件）。最終差分の型・lint・buildも成功。全体smoke初回は438成功・166対象外・11失敗。失敗は旧書体・見出し・従来公開表示を前提にした検証で、更新後は関連99件と見出し3件を成功確認。全体smokeの再実行はしていない。入力エラー時にプレビューから編集へ戻す経路はPC・スマホで再検証。Darkの対象操作欄もPC・タッチで確認済み。
- commit・push後の本番buildと実画面は同日の開発ログへ記録する。証拠は `scratch/photo-first-20260908/`、`scratch/admin-workspace-20260908/apple-review/`、`scratch/photo-app-production-20260908/review-portfolio-*`。設計と操作は `docs/specs/public-design-review-2026-09-08.md`、`docs/admin-guide.md`。

### 先行実装: 写真アプリ案の製品統合・公開仕上げ

- ユーザーがLiquid Glass改訂案の完成を承認し、動作の軽さを優先するよう指示。試作の固定データを実際のCMSへ置き換え、公開TOP・Gallery・シリーズ／Work・プロフィール・問い合わせへ組み込んだ。`publicExperience=photo-app` をAdminの「作風を選ぶ」でプレビュー・保存できる。従来の配色・書体・レイアウト値を保持し、配布先の初期表示は従来のポートフォリオのまま。
- 3pxの行組み、列数変更、検索・ソート・絞り込み、常設の戻る／前後／情報、ブラウザー履歴とフォーカス復帰を実装。HERO・トップ掲載・公開順・本文・英語・既存連携をCMSから取得する。原本・認証・料金・公開範囲は変更しない。
- 428枚でも画面付近だけを描画。実測のPC初期35枚、連続スクロール時最大65枚。ガラスの描画負荷を面別に測り、PCのスクロール中だけぼかしを停止、静止180ms後に復帰。描画間隔中央値約34→8.3ms、スマホも8.3ms（同じMacのwarm自動ブラウザー内比較）。WebKitの縦バー後出しによる位置ずれも修正。
- `bun run check` 成功（製品1,337件、ツール60件、型・lint・build・DB台帳整合）。新しい公開操作テスト27件が成功、設定の未保存プレビュー→表示だけ保存→従来表示への復帰も人工APIで成功。既存wiki鮮度警告8件は保持。全体smokeは449成功・162対象外・失敗なし。最終差分の型・lint・build、言語・誤ったシリーズURL・絞り込みの履歴遷移も成功。本番buildと公開切替後の実測は同日の開発ログを参照する。
- 仕様・使い方は `docs/specs/public-design-review-2026-09-08.md` と `docs/admin-guide.md`。実測画像・速度比較・検証ログは `scratch/photo-app-production-20260908/`。公開切替の日時・コミット・本番画像の確認結果は同日の開発ログへ記録する。


### Appleの写真アプリを参考にしたWeb試作

- 同日追加の「品質・こだわり不足」「Liquid Glassが入っていない」を受けて同じ5082を改訂。写真が背後を流れるガラスの操作欄、外周反射・屈折、選択表示の連続移動、暗い拡大画面、写真を縮めない情報表示、スマホ下部の前後操作を実装。Chrome系は外周屈折、Safari系はCSSの透過・ぼかし。headless WebKitの光学描画には制約があり、実機Safariの見た目は未確認。
- 改訂版は基本8件＋追加4件＋ガラスの操作6件を検証。独立レビューの指摘からフォーカスの隠れ、シリーズ表紙の灰色面、スマホの前後ボタンの配置を調整。新しい画像・動画・単体HTMLは `scratch/liquid-glass-20260908/`。本番の公開デザインとAdminはこの改訂でも変更していない。
- ユーザーの「まずAI感をなくす」「Appleのアプリの使用感・デザイン・モーション」「安っぽくしない」を受け、Apple公式のHIGとPhotosを調査して試作を組み直した。
- `docs/design-studies/photo-app-20260908/`、ローカル5082。PCサイドバー／スマホ下部ナビ、セレクト・全写真、検索・撮影日順・列数、開閉・前後・再配置の動き、ページ状態とブラウザー履歴の保持を実装。以前のA/B試作は保持。
- 基本8ケース、追加4ケース（Chrome/WebKit、スマホ・PC、動きを減らす設定、画像エラーを含む）と独立レビュー。写真を見ながら操作できるWeb試作が対象で、本番の公開デザイン・Admin・CMSは変更していない。原本・DBへの書き込みもない。証跡と操作動画は `scratch/photo-app-20260908/`。

### 公開サイト自体のデザイン再検討

- ユーザーはAdminの操作改善に加え、公開サイトそのものの根本的な見直しを希望。公開TOPを実測し、実写真を用いたA「写真集」・B「写真一覧」の全体デザイン試作を作成した。トップ・全写真・シリーズ・プロフィール・拡大閲覧が動作する。
- 試作は `docs/design-studies/public-20260908.html`。設計判断と確認範囲は `docs/specs/public-design-review-2026-09-08.md`。ローカル閲覧は5081、以前のAdmin試作5080は保持。
- Chromium/WebKit × 320/390/1440px × A/Bの12ケースを確認。製品コード・公開デザインは変更していない。推奨はAの組写真の入口とBの一覧密度調整の組み合わせ。最終セレクト、既存CMSとの統合、本番実装は残る。

### Lightroom参考の行組み一覧・密度調整

- 追加のLightroom画像を受け、Libraryの初期表示を縦横比を保つ行組みに変更。写真間は3px、公開側S/M/Lの強弱は管理一覧へ持ち込まない。写真の色・構図を確認できるよう、非公開写真も行組みでは通常の濃さで見せ、非公開の表示を保つ。
- 一覧直上に「行にそろえる／列をそろえる」とサイズ調整を常設。固定列はPCで2〜12列、タッチ端末は2〜6列（実際に収まる列数を上限）。閲覧・選択で使用でき、並べ替えは操作領域を確保する既存グリッドへ切り替える。設定変更は公開順・写真原本を変更しない。
- 行単位の仮想表示・先読み・末尾移動、見ている写真を保つ密度変更、見た目の行に沿う矢印移動を追加。取り込み直後のまとまり、選択・一括操作、未保存確認を保持。見出しと一覧外側の重複した余白を削減。
- 5080の試作と自己完結HTMLも同じ行組み・サイズ／列数調整へ更新。試作は428枚を表示し、先頭80枚制限を解除。試作の編集は引き続きブラウザー内だけ。本番公開サイトの構成案は今回変更していない。
- 検証資料: `scratch/library-contact-20260907/`。全体検証・公開build・画面確認の最終実測は当日の開発ログへ記録する。

### HERO候補選びと一覧密度の再検討

- ユーザーから「1枚ずつ大きく見せすぎる」「HEROにソートがない」「単純すぎてAI感が残る。動きやデザインも欲しい」と追加指摘。大きく表示すること自体を使いやすさの指標にしない。
- HEROの候補に検索、追加日・撮影日・名前・Library保存順のソート、カテゴリー・向き・選択済み絞り込みを追加。候補の表示順はHEROの公開順を書き換えない。一覧密度は3段階、スマホの標準は3列。写真の構図を保って表示し、拡大を選択操作から分離した。
- 選択済みHEROを横の帯へまとめ、候補一覧を独立スクロール。選択マークと追加に短い反応、順番変更に位置移動のアニメーションを加え、動きを減らす設定に対応。保存・再取得が終わるまで競合する操作を止め、取得失敗時は空や削除済みと判断せず再読み込みを出す。
- 根本刷新の試作は `scratch/atelier-rethink-20260907/`。一覧を高密度にし、写真を押すと一覧を残して横・下で確認する構成へ変更した。これは本番Libraryの変更ではない。公開サイトの2構成比較も引き続き試作内のみ。
- 検証資料は `scratch/hero-contact-sheet-20260907/`。最終検証、commit・push、本番buildの実測は当日のObsidian開発ログへ記録する。

### 同日夜：Libraryを写真中心の作業画面へ変更

- スマホの絞り込みが写真を覆って閉じ方が見えず、PCでも写真・操作が狭くなるという追加指摘。Lightroomのように写真を中心に使いたいとの希望を受け、管理役が継続実装した。
- PCの絞り込みは一覧横の独立欄、スマホは上下に出口を持つ専用パネルへ変更。基本3条件・追加条件・保存した条件を整理。残りの幅に合わせて操作バーを二段にし、検索欄を確保する。
- 写真編集は作業領域全体へ切り替え、PCは大きな写真と右の編集欄、スマホは写真と下の編集欄。「一覧に戻る」・前後移動・拡大・保存を常設し、一覧位置と未保存確認を保持する。
- `bun run check` 成功（製品1,311件・ツール60件、型・lint・build・DB整合）。全体smokeは401成功・152対象外・失敗なし。320pxから1440px、タッチ・Safari、日英、絞り込みの開閉とフォーカス、拡大からの復帰、保存失敗を確認した。既存wiki鮮度警告8件は残る。
- 実測画像・ログ・変更前後の比較は `scratch/library-workspace-20260907/`。手順は `docs/admin-guide.md`、設計は `docs/specs/usability-review-2026-09.md` の同日夜の追加依頼節。commit・push・本番buildとログイン済みAdminでの最終実測は当日のObsidian開発ログへ記録する。
- 本番の写真原本・保存済み情報・DB・認証設定は保持。検証の書き込みはモックで処理。Chromeの既存ログイン状態を使えることを確認し、パスワードの再入力や変更は行っていない。

### 管理画面・スマホ・公開サイトの見直し（実装・検証完了）

- オーナーが全体の使いやすさと品質の見直しを依頼し、「途中で終わらせない」「公開サイト側も改善案を模索」と継続を明示。管理9画面を390px/1440pxで点検し、写真操作・設定検索・編集画面・公開ナビを修正した。
- スマホのLibraryは選択・並べ替え・取り込みを常設し、検索幅を確保。表示メニューの固定位置を廃止し、押した位置から開いて外側タップとEscapeで閉じる。公開順の設定により並べ替えできない場合は、その設定へ直接進める。
- 写真編集で写真とファイル名を表示し、保存帯と下部ナビを分離。390×667pxで重なり25px→0pxを実測。変更前を「変更なし」と表示し、未保存確認・保存失敗と再試行を維持。
- 下部ナビの「設定」から直接移動。21節を日英の目的語で検索でき、サイト基本情報を名前・問い合わせ・公開と検索に整理。説明文は複数行で編集する。
- 公開Galleryは点数と絞り込みの入口に集約。カテゴリーと撮影方式を区別し、URL・総点数・解除・フォーカス復帰を確認。公開メニューは大きな行と現在地を持ち、背景操作を止める。プロフィール本文直後に既存設定に連動する撮影相談リンクを追加。
- 体験版のガイドと並べ替え設定を整合。390pxの案内帯は約143px→71px。日英とスマホのサイトプレビューを確認。
- `bun run check` 成功（製品1,311件、ツール60件、型・lint・build・DB台帳整合）。wiki鮮度は既存8件の警告のみ。全体smokeは384成功・152対象外・5失敗。失敗5件は旧操作経路・旧表示寸法・先読み開始時点を前提にしていた検証を更新し、関連7件の再検証で成功した。全体smokeの初回結果自体を全件成功とは扱わない。
- 設計・実測・公開側の次の構成案は `docs/specs/usability-review-2026-09.md`。改修前後の画面比較は `scratch/ux-review-20260907/review.html`。公開のcommit・push・本番buildと画面確認は、当日のObsidian開発ログへ実測を記録する。
- 写真原本・保存済み設定・公開順・本番データを保持。検証の変更操作はAPIモックを使用。他作業の名刺ページ `acc142a` を取り込み保持した。
- Sparkは専用worktreeで利用上限に達したため、継続依頼を受けて管理役が実装を引き継いだ。途中成果は `scratch/ux-review-20260907/spark-public-partial.tgz` とpatch、実行ログは既存Spark記録先へ保管。未検証の途中コードは採用していない。
- 次の編集候補は、TOPの代表作を8〜12点で比較する案と、実案件の用途・担当範囲を示すWorkの編集。今回の共通操作改善とは分けて設計書に整理した。

### 2026-09-06の公開済み状態

- **Status:** 制作の料金比較・無料相談に加え、検索向け解説と管理画面の実演動画を追加。公開反映・検索登録の実測は同日のObsidian開発ログを正本として確認する。
- **Current owner:** Codex / **Handoff readiness:** ready
- `/portfolio-kit` は公開設定30,000円と写真・文章編集付き総額69,800円の一つの制作サービス。旧Studio別ブランドのカードを除去し、所有者ナビ・フッターから同じ料金比較へ進む。
- `/portfolio-kit/consult` は本体内の無料相談。既存SitesのD1へ保存後、本人の既存Formspreeへブラウザーから通知する。送信内容・送信先は2026-09-06に本人が明示承認。Cookieや写真・カード情報を送らない。
- 受付番号を保持した再送、通知失敗と保存失敗の分離、所有者以外の配布先でフォームを出さない制限、公開ルートのHTTP状態、個人情報を含めない相談クリック・保存完了計測を追加。
- 2026-09-06検証：`bun run check` 成功（製品テスト1,300件・ツール60件）。全体smokeは360成功・149対象外、追加の相談経路はPC/390px各4件、計8成功。wiki鮮度は既存8件の警告のみ。
- 素材受領後の納期はプランと案件に応じて合意。見積条件と入金は人が確認する。実顧客の受注・納品・月10万円は未実証。無料ツールと運営手順は別作業フォルダの `INTEGRATION.md`。
- 同日午後の継続修正：購入後ガイドはURLの `thanks` / `checkout_session_id` だけで支払い済み・領収書送付済み・購入金額を表示しない。両プラン共通で合意済み日程を案内し、以前の契約条件を優先する。素材依頼・引渡し文書も更新。関連レンダーテスト120件、型・lint・build成功。検索管理画面では制作ページが検出済み・未登録だったため、登録リクエストを実施し受理を確認。掲載済みとは扱わない。

### 同日夕方：名前を知らない人向けの入口

- `/portfolio-kit/guide` に写真家向けの作り方ガイドを追加。写真選び、作品・プロフィール・問い合わせ、初期費用と継続実費、公開後の更新、公開前の確認を説明。作者自身のサイトを顧客実績と混同しない。
- 本文を画面と非JS向けHTMLで共有。個別のタイトル・説明・正規URL、公開可否、サイトマップ、販売ページからのリンクを連動させた。体験版・料金・無料相談へ進める。
- `/portfolio-kit#admin-video` に29秒の実操作動画を追加。体験版だけで撮影し、本番への更新通信は0件。自動再生なし、字幕と文章説明付き。Chrome系・Safari系で再生確認。既存の日英スクリーンショットも保持。
- `bun run check` 成功（製品1,311件、ツール60件）。全体smoke初回は386成功・148対象外・1失敗。既存取り込みテストの初回取得待ち不足を修正し、別ポートで該当検証を3回連続成功。全体smoke自体を全件成功とは記録しない。変更箇所のlint・`git diff --check` も成功。
- Sparkを2回使用して実装・構造修正、管理役が本文を編集して統合。残量は未確認。作業成果は `scratch/portfolio-discovery-spark-20260906.tgz` に保管、実行ログは既存Sparkの記録先に残る。
- ユーザー指定によりXは投稿しない。告知文は `docs/portfolio-kit-launch.md` のローカル下書き。最初の目標は30日で相談3件・有料制作1件（目標であり実績ではない）。検索掲載・順位・受注は保証しない。

### 2026-09-05までに統合した土台（履歴）
- 写真詳細ページと検索対象の選別、HERO画像の配信改善、公開画面の日英対応、
  個人情報を含めないアクセス計測、管理画面保存後の公開キャッシュ更新を統合。
- 公開済みのStudio導線・管理画面を主役にしたPortfolio Kit販売ページを保持。
- 予約やIvyの設定は別リポジトリ。Portfolioの写真原本・本番DB・決済設定を変更していない。
- Privacy・サイト利用条件は日英で用意。未採用の販売条件は
  `docs/specs/portfolio-kit-sales-policy-draft.md` に保管し、公開ページへ掲示しない。
  未採用案を理由に全サイトの購入導線を止める固定フラグも除いた。
- 写真追加中は共通フッターとStudio案内を末尾へ先出ししない。ギャラリーの最後で表示する。
  Escで新しく開いた確認画面を、同じキーの既定動作で閉じないようにした。
- 本番確認で写真詳細の初回表示が透明なままになる問題を検出。API取得後に
  表示処理を開始し、初回アクセス時の画像と親要素の不透明度をブラウザーで検証する。
- `AGENTS.md` を作業方針の正本とし、通常の改善は検証後のcommit・push・本番確認まで進める。
  PM2設定を読んだだけでDB変更・buildは実行しない。

### 検証と公開状態の確認

- `bun run check`、全体smokeと失敗箇所の修正後の再検証を実施。
  全体smokeの最初の失敗も記録し、成功として置き換えない。
- 本番は <https://akieguchi.com/>。`/api/health` のbuildとGit HEADを照合する。
  本番反映の実測・検証ログは同日の開発記録と統合レポートを参照。

### 保管した作業

- `prototype/b2-uneven-rows`: 2026-07-28時点の段違い行レイアウト試作。未統合。
- `prototype/finder-contact-sheet`: 2026-07-28時点のFinder風一覧試作。未統合。
- 統合済みworktree3つと完了ブランチを整理。個別設定・検証資料・AI履歴は
  `~/.local/share/worktree-archives/2026-09-05/` へ保存し、移動前後を照合済み。
- 販売条件の未確定項目は相談用資料に残る。通常の製品改善を止める条件にしない。
<!-- CURRENT_STATE_END -->

統合前の詳細: [2026-09-05の統合前記録](docs/archive/task-before-2026-09-05-integration.md)。
過去の記録: `docs/archive/task-handoffs.md`。
