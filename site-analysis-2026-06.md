# akieguchi.com 不足点考察レポート
_2026-06-19 作成_

---

## まず：現状サイトの強み

調べた結果、技術的な土台はかなりしっかりしている。

- OGP（og:image / og:title / Twitter Card）✅
- JSON-LD（WebSite + Person + ImageGallery + BreadcrumbList）✅
- sitemap.xml（画像サイトマップ含む）✅
- GA4 トラッキング ✅
- srcSet でレスポンシブ画像 ✅
- コンタクトフォーム（バリデーション・ハニーポット付き）✅
- 料金プラン表示 ✅
- ヒーローカルーセル（キーボード・スワイプ・reduced motion 対応）✅
- シリーズページ（独自 JSON-LD 付き）✅
- Note ジャーナル連携 ✅

技術 SEO・アクセシビリティ・UX の基礎は一般的な写真家サイトより明らかに上。

---

## 足りないもの（優先度順）

---

### 🔴 P0：社会的証明（Social Proof）がゼロ

**これが最大の欠落。**

調査した Boaz Walma、Brady Perron などのトップサイトに共通しているのは、「このひとは誰かに認められている」という証拠が視覚的に並んでいること。

- クライアント企業ロゴのスクロールバー
- 「〇〇の広告撮影を担当」「△△誌掲載」などの実績テキスト
- 過去クライアントの声（テキスト1〜2行でも十分）
- 展示歴・受賞歴・掲載媒体一覧

訪問した見込みクライアントが「依頼して大丈夫か」を判断する材料がいま皆無。プロフィール文と写真だけでは信頼を形成するのに時間がかかる。

**実装案：** About ページに "Clients / Press" セクション追加。ロゴ画像 + 媒体名のグリッドで十分。管理画面から追加できると理想。

---

### 🔴 P0：全ページのメタディスクリプションが同一

現状、トップ・ギャラリー・About・コンタクトすべてが
`「東京を拠点に活動する写真家・江口秋のポートフォリオ。宣材・ポートレート撮影のご依頼を受け付けています」`
という同じ文章。

Google はページ固有の説明がないと低品質とみなす可能性があり、クリック率にも直結する。

**実装案：**
- `/gallery` → 「ポートレート・宣材・風景など、江口秋の作品ギャラリー」
- `/about` → 「東京・江口秋について。使用機材・撮影スタイル・活動歴」
- `/contact` → 「撮影依頼・コラボレーションのお問い合わせ」
- `/series/:slug` → シリーズタイトル + 説明文

---

### 🟠 P1：ドメイン上に読まれるテキストコンテンツがない

Note との連携があっても、記事は note.com に存在する。Google が akieguchi.com でインデックスするのは写真の alt テキストくらい。

競合との差別化や長尾キーワードでの発見可能性を上げるには、**ドメイン内にテキストが必要**。

**実装案：**
- シリーズページの説明文を充実させる（撮影場所・テーマ・コンセプト 200〜400字）
- Note の記事を akieguchi.com/journal に埋め込み表示（今の実装はプロフィールページにしか出ない）
- 少なくとも About の statement セクションに 300 字以上の文章を入れる

---

### 🟠 P1：ギャラリー写真の alt テキストが弱い

現状のコード：
```
alt={photo.title || photo.meta || photo.filename || `Photograph ${idx + 1}`}
```

`photo.meta` は EXIF フィールドで、`photo.title` が未入力だとファイル名（`_ZFP4857.jpg` 的なもの）がそのまま使われる。Google 画像検索は写真家にとって重要な集客チャネルで、画像検索の全検索シェアの 22.6% を占める。

**実装案：** 管理画面のメタ編集に「alt テキスト」専用フィールドを追加し、`photo.title` → `photo.altText` → `photo.filename` の順でフォールバック。

---

### 🟠 P1：予約導線がコンタクトフォームだけ

フォームで「撮影したい」と送ってから返信まで時間差がある。トップサイトでは **Calendly / cal.com などのカレンダー埋め込み**が標準になっており、空き日時を確認しながらその場で仮予約できる。

見込みクライアントの離脱率に直結する。

**実装案：** コンタクトページに Calendly の iframe 埋め込み、または「空き確認はこちら」リンクを追加。

---

### 🟡 P2：画像フォーマットが JPEG のみ

現状は mozjpeg q92 で最適化済みだが、2026 時点では WebP が「どのブラウザでも動く標準」になっている（JPEG 比 25〜35% 軽量）。

ページ速度は Core Web Vitals に直結し、LCP（最初の大きな描画）が SEO ランキング要因。

**実装案：** sharp の `.toFormat('webp')` + `Content-Type: image/webp` の切り替えを画像プロキシ (`/api/images`) に追加。`Accept` ヘッダーで WebP 対応ブラウザにだけ WebP を返す方式が最もシンプル。

---

### 🟡 P2：動画・動きのあるコンテンツがない

調査したトップサイトの多くがショーリールやプロセス動画を持っている。静止画専門でも、**撮影風景の 15 秒クリップ**があるだけでサイトの滞在時間と「人間らしさ」の印象が大きく変わる。

**実装案：** ヒーローセクションか About ページに YouTube/Vimeo 埋め込みオプションを settings に追加。

---

### 🟡 P2：シリーズが発見しにくい

シリーズページ（`/series`）は実装済みだが、ナビゲーションや Top から自然に辿り着く動線が弱い。写真家の「作家性」を伝えるのに最も有効なコンテンツなのに、もったいない。

**実装案：** Top ページに "Series" セクションを追加（Works の下、シリーズサムネイル 3〜4枚）。ギャラリーにもシリーズ別フィルタ（カテゴリとは別軸）を追加。

---

### 🟢 P3：フッターが素っ気ない

トップサイトのフッターには、ナビゲーション・連絡先メールアドレス・著作権表示・SNSリンクが最低限入っている。akieguchi.com のフッターは現状非常に薄い。

---

### 🟢 P3：ダークモード

写真家サイトでは黒背景が写真を際立たせる定番手法。特に夜間閲覧での体験が大きく違う。`prefers-color-scheme: dark` に対応するか、サイト設定からダーク/ライト切替を追加する。

---

## 比較まとめ

| 項目 | akieguchi.com | 上位サイト群 |
|---|---|---|
| 技術 SEO（OGP・JSON-LD・sitemap） | ✅ 充実 | ✅ |
| ページ固有メタ説明文 | ❌ 全ページ共通 | ✅ |
| 社会的証明（クライアント・実績） | ❌ なし | ✅ 必ずある |
| テキストコンテンツ量 | △ 少ない | ✅ |
| 画像 alt テキスト品質 | △ ファイル名フォールバック | ✅ |
| 予約・スケジュール導線 | △ フォームのみ | ✅ カレンダー連携 |
| 動画・ショーリール | ❌ なし | ✅ 多数 |
| 画像フォーマット（WebP） | ❌ JPEG のみ | ✅ |
| シリーズ発見動線 | △ 弱い | ✅ |
| フッター | △ 薄い | ✅ |

---

## 推奨着手順

1. **社会的証明セクション**（About に Clients / Press 追加）← 営業効果直結
2. **ページ別メタディスクリプション**（SEO・クリック率）
3. **Calendly 導線**（コンタクトページ、コンバージョン改善）
4. **シリーズ発見動線**（Top ページ + ギャラリーフィルタ）
5. **WebP 対応**（画像プロキシに Accept ヘッダー分岐）
6. **動画埋め込みオプション**（settings に YouTube/Vimeo URL 追加）
