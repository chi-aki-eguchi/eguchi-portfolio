# Task Log

<!-- CURRENT_STATE_START -->
## Current State — 2026-09-02 JST（たまに落ちる検査の正体）

- **Status:** `public-scroll-stability` の「ゆっくり読む速さ」が通し実行の半分で
  落ちる件。**測る側の揺れではなく、製品の不具合だった。**原因を実物で特定し、
  手元で必ず出せる手順まで作った。**直しは未着手（オーナー判断待ち）。**
- **Current owner:** Claude Code / **Handoff readiness:** ready
- **Branch:** `claude/gracious-lamarr-f970da`（ワークツリー）

### 分かったこと

**送るのが写真の追加より速いと、サイト共通の奥付が高さ1つぶん押し下げられる。**

読み手がいま並べ終えたところまで届くと、その下の `<footer>`（`Layout.tsx`）が
画面に入る。そこへ次の束が差し込まれ、奥付が画面から押し出される。
desktop 126px / mobile 128px。**それだけで 0.14〜0.16。**上限は 0.1。

**噂ではなく数で分かる。**過去の失敗記録を数えると、落ちた値は
desktop `0.1400` が4回、mobile `0.15763546798029557` が10回。**17桁まで同じ値が
繰り返される。**混んだ機械のノイズではありえない。`0.15763546798029557` は
ちょうど 128 ÷ 812（mobile の画面高）＝**画面ぜんぶが 128px 動いた**。

**手元で必ず出せる**（3回中3回・記録と同じ値）: 画像の応答を 400ms 遅らせて
`/gallery` を同じ速さで送る。空いている機械では毎回間に合うので、単独で回すと
何回やっても通る。詳しくは `docs/agents/backlog.md` の **B-26**。

**B-23 は間違っていた。**あの項目は同じ失敗を「骨組みが 61px 押し下げられる」
せいだと書いていたが、そのズレは**検査が数え始める前に終わっている**（健全な回
でも同じズレが出る）。B-23 は訂正して、開いた瞬間の話だけを残した。

### やったこと

| 変更 | 内容 |
|---|---|
| `public-scroll-stability.spec.ts` | 落ちたとき**何が動いたか**を一緒に出す。閾値・retries は触っていない |
| 同上のコメント | 「測る側の揺れ」という記述を、実測に合わせて書き直した |
| `backlog.md` B-26 | 原因・筋道・再現手順・直す2案 |
| `backlog.md` B-23 | 誤った原因特定を訂正 |

### 検証

- `bun run check` = exit 0
- `bun run smoke` = **この回でも desktop / mobile 両方で1回ずつ落ちて再試行で通った。**
  直していないので当然で、これは狙いどおり。**閾値は緩めていない。**

### 次の一手

**オーナー判断が要る。**奥付が飛ぶのを止める案は2つ（B-26 に詳しい）:

1. **場所を先に取る。**まだ描いていない写真のぶんの高さを実寸から出して確保する。
   合計を一定に保てば奥付は1pxも動かない。**見え方は変わらない。**作業は大きめ
2. **まだ続くあいだは共通の奥付を出さない。**奥付・撮影のご依頼と同じ扱い。
   小さく確実だが、**497枚のギャラリーでは奥付にたどり着けなくなる**

**推し: 1。**2 は検査を通すが、読み手から SNS リンクと著作表示を取り上げる。

### 触ってはいけない範囲

- 本番DB・Turso・R2・Railway・環境変数・`.env` / `shotAt` / 公開API応答形
- **この検査の閾値 0.1 は緩めない。**Core Web Vitals の「良好」の線で、
  0.14〜0.16 は画面の高さの16%が一度に飛ぶ大きさ＝人の目に見える
<!-- CURRENT_STATE_END -->

---

## 過去記録

過去 Handoff（133本）と過去 Current State は `docs/archive/task-handoffs.md` へ移した。
**このファイルには Current State だけを置く。**履歴を戻さない。
新しい Handoff が必要なときは、archive 側の末尾へ追記する。
