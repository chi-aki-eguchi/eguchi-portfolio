// 販売ページの既定のFAQと値段。**画面と構造化データの正本を1つにするため**
// ここに置く。以前は既定値が `web/lib/service-config.ts` にだけあり、
// `servicePageConfig` が未保存の本番では、画面にはFAQが出ているのに
// `<head>` の FAQPage は空、という食い違いが起きていた（2026-09-01 実測）。
//
// `src/shared/` にあるのは、API層（`api/ogp.ts`）と画面（`web/lib/`）の
// 両方から読むため。API層から `web/` を import すると層が混ざる。

export type ServiceFaqItem = { q: string; a: string };

export type ServicePlan = {
  name: string;
  price: string;
  sub: string;
  points: string[];
  stripeUrl: string;
  cta: string;
  primary: boolean;
};

export const DEFAULT_SERVICE_PRICE_JPY = 30000;

export const DEFAULT_SERVICE_FAQ: readonly ServiceFaqItem[] = [
      {
        q: "購入したあと、すぐサイトが自動でできますか？",
        a: "自動生成ではなく、一件ずつこちらで設定します。決済後24時間以内に素材のお願いをお送りし、素材が揃ってから3日以内に、公開した状態でお渡しします。",
      },
      {
        q: "自分のドメインを使えますか？",
        a: "はい。yourname.com のような自分のURLで公開できます。すでにお持ちの場合は接続まで行います。まだの場合は、取得から一緒に行います（年1,500〜2,000円程度・あなた名義）。",
      },
      {
        q: "維持費や月額料金はいくらですか？",
        a: "Portfolio Kit自体の月額料金はありません。かかるのは外部サービスの実費だけで、公開場所が月500〜1,000円程度、独自ドメインが年1,500〜2,000円程度が目安です（料金改定や使用量により変わります）。",
      },
      {
        q: "あとから写真や文章を変えられますか？",
        a: "はい。管理画面から写真、並び順、プロフィール、連絡先などを更新できます。大きなデザイン変更や個別カスタムは、内容に応じて別途ご相談になります。",
      },
      {
        q: "購入すると、どこまで利用できますか？",
        a: "1回の購入につき1サイトで利用できます。2サイト目以降は追加購入が必要です。テンプレートとしての再販売・再配布はできません。テンプレート本体の更新は現時点では無償で提供しますが、今後変更する場合があります。操作方法の相談は、当面は期間・回数の制限なく受け付けます（今後、期間制に変更する可能性があります）。デザイン変更・個別カスタム・作業の代行は、内容に応じて別途お見積もりします。",
      },
      {
        q: "やめたいときはどうなりますか？",
        a: "公開場所の契約を止めれば、月々の実費も止められます。プロジェクトを削除すると写真や設定も消えるため、必要な写真や文章は先に手元へ保存してください。",
      },
    ];

/**
 * 既定のプラン。FAQ と同じ理由でここに置く——**`servicePageConfig` が未保存の
 * 本番では、画面はこの既定値を描いているのに、サーバ側の読み手は空を返す。**
 * 値段（Offer）にも、販売ページ本文の HTML にも、同じ内容が要る。
 */
export const DEFAULT_SERVICE_PLANS: readonly ServicePlan[] = [
      {
        name: "公開おまかせ",
        price: "¥30,000",
        sub: "設定はすべてこちらで行い、公開した状態でお渡しします。あなたがするのは、写真を入れることだけ。",
        points: [
          "公開場所・管理画面の設置をすべて代行",
          "独自ドメインの取得を一緒に行い、接続まで対応（あなた名義・実費別）",
          "名前・プロフィール・連絡先を初期設定して納品",
          "納品後は管理画面から写真を入れるだけ",
          "操作方法の相談つき（当面は期間・回数の制限なし）",
        ],
        stripeUrl: "https://buy.stripe.com/aFa14n0BIcNB0rScvegrS01",
        cta: "公開おまかせを申し込む",
        primary: true,
      },
    ];
