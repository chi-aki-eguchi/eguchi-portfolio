# 決定ログ 2026-07-17（Claude / Fable5）

## タスク: i18n 2c-4/2c-5 完了（実装=Sonnet5 subagent直列、指揮・検証=Fable5）

- クレジット判断: hookは[credit closing]だがオーナーがチャットで「続き。」と直接指示。
  Claude側残量は5h 80%/週83%と健全、指示の主旨(Codex温存)は「Codex新規委任しない」で
  遵守できるため、Sonnet5委任で続行した。Codexは未使用。
- 2c-4: 設定タブ「連携・販売」「デザイン・文言」「撮影情報プリセット」+ライブプレビュー
  ツールバーをEN化。フォント見本文字列とDB保存値は据え置き(理由付きで自己申告)。
- 2c-5: 掃討の結果、残っていた日本語の大半は「未到達フォールバック」「DB比較値」
  「辞書上書き済み定数」と精査判明。実修正1件+LP注記差し替え。
  「Japanese-first」→「available in English and Japanese」をLP FAQ/本文・スタート
  ページ・購入後メール(2箇所)・英語手順書で統一。renderテスト期待値は仕様前進に追随。
- ゲート: tsc/test 461 pass → bun run check 成功 → smoke 35 pass・27 skip・0 fail。
- これで admin EN/JP 切り替えは全タブ完成。Phase 3(公開サイトのナビ/プロフィール/
  問い合わせEN)のみ未着手(settingsキー追加を伴うため個別スライスで)。

## タスク: i18n Phase 3 スライス1 — 公開サイト /en/about・/en/contact + JP|EN切り替え（実装=Sonnet5 subagent）

- ゴール通り、settingsキーは新規追加せず実装完了。
  - `app.tsx`: `/en/about`(ProfilePage)・`/en/contact`(ContactPage language="en") ルート追加。
  - `contact.tsx`: `language?: "ja"|"en"` prop追加、ハードコード日本語2箇所(リード文/準備中)のみ英訳。
    フォームラベル等 settings 由来文言は既定が英語のため不変更。ProfilePage には prop 追加せず(後続スライス)。
  - `Layout.tsx`: JA_TO_EN_PATH/EN_TO_JA_PATH の対応表で言語ペア判定。ペアがある時だけ
    デスクトップnav末尾+モバイルメニューに JP|EN 切り替えを表示(現在言語は非リンク濃色、
    他方はnav-link-luxuryリンク)。ENページ表示中は About/Contact のリンク先のみ /en/* に
    差し替え(Gallery/Series/Portfolio Kitは対象外、指示通り)。isActive は "about"/"contact" の
    正規化セクション名で判定するよう書き換え、/en/about・/en/contact でも正しくハイライトされる。
    スキップリンクをENページで "Skip to content" に。
  - `ogp.ts`: PAGE_TITLES/META_DESCRIPTION_KEYS/KNOWN_ROUTES に追加。新設
    `publicPageLanguageAlternates()` で /about↔/en/about・/contact↔/en/contact の hreflang を
    双方向出力(/profileはjaを/about扱いに正規化、既存canonPathと同じ扱い)。
    genericPageDescription に /en/about・/en/contact の英語フォールバック文を追加。
  - `server.ts`: buildSitemap の静的pathsに /en/about・/en/contact を追加。
- 判断（指示に明記のない拡張、影響は小さいので実施）:
  - `<html lang>`・`og:locale` の英語切り替えを、既存のサービスLP専用条件から
    /en/about・/en/contact にも拡張(ENGLISH_PUBLIC_PATHS)。指示は明記していなかったが、
    hreflangまで足しておいて html lang が ja のままは片手落ちになるため、既存パターン
    (SERVICE_OG_EN 側の実装)を素直に踏襲。
  - モバイルメニューの `max-h-52`(208px)は、About/Contact ページでJP|EN行が1段増えると
    最大構成(Gallery/Series/About/Contact/Portfolio Kit + JP|EN)で実測211px超になり得たため
    `max-h-64`(256px)へ拡張。ブラウザで scrollHeight=211px(Portfolio Kit非表示時)を実測して
    範囲内に収まることを確認。
- 実ブラウザ確認(vite dev, localhost:5173): /contact→EN切替→/en/contact に遷移し
  About/Contact リンク先が /en/about・/en/contact に切り替わり、JP|EN の濃淡が反転することを
  アクセシビリティツリー+computed style で確認。/en/about のタイトルが "About | ..." になる
  ことも確認。モバイル幅でハンドラー経由のクリックがブラウザツール側の制約で発火せず
  開閉の視覚確認は取れなかったが、DOM上のリンク先とscrollHeightは確認済み。
- `bun run check`: tsc -b（強制フルビルドでも別途確認）→ lint → bun test 475 pass →
  build すべて成功。
- push なし。git status はこのタスクで触った7ファイルのみ(意図外の差分なし)。

## タスク: i18n Phase 3 スライス2+3 — プロフィール・問い合わせの英語文settingsキー（実装=Sonnet5 subagent、指揮・レビュー=Claude Fable5）

- 新規 settings キー6個（Phase 3 仕様の「着手時に個別リスト」を作成して委任）:
  profileBioEn / profileStatementEn / contactIntroEn / contactNoteEn / contactFlowEn /
  contactEnglishNote（既定 "English inquiries welcome."、空欄で非表示）。
- §0 4箇所同期の確認結果: 台帳 `shared/settings-keys.ts` 追加 + API GET /settings default 追加。
  provider.tsx はテキスト系キーを JS_PREVIEW_KEYS 経由の汎用ブリッジで処理するため
  個別コード不要（grep で個別参照ゼロを確認）。デモadminも `SETTINGS_PREVIEW_KEYS.map` で
  自動追随のため変更不要。
- 画面: /en/about・/en/contact は En 値を表示し、未入力なら日本語へフォールバック
  （英語ページが空白にならない）。contactEnglishNote は JP/EN 両方の Contact 見出し下に
  常時表示（success画面のみ非表示）。
- admin: Profileタブに Bio (EN) / Statement (EN)、設定「基本」に contact系EN 4欄
  （admin-i18n辞書 ja/en 両対応）。
- ゲート: bun run check 成功（479 pass）。bun run smoke は 34 pass・27 skip・1 fail →
  fail は admin-library-swipe の hover演出テスト（今回未変更領域）で、単体再実行で pass。
  flaky と判断（期待値変更などはしていない）。
- 実ブラウザ確認(vite dev): /en/contact に "English inquiries welcome." + JPフォールバック文、
  /en/about は日本語bioフォールバック表示、JP|EN トグル往復（/en/about→/about）と
  スキップリンク文言連動を確認。
- 要オーナー確認（次回）: 配布テンプレートでも /en/about・/en/contact と hreflang が
  常時有効になる（EN未入力時は日本語文がENページに出る）。問題視するなら
  表示条件のガード（例: EN文未入力なら hreflang を出さない）を検討。
- commit: 8306f8c（3-1）/ c5f692d（3-2+3-3）。push なし（オーナー承認待ち）。
